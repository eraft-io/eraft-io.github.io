# Lec. 3： Architectures, Hyperparameters

生成时间: 2026-03-08 15:07:48

---

## 段落 1

**英文**: As you may have noticed, I'm a little bit less innovative in my lecturing than Percy, so you're going to get PowerPoint slides rather than executable Python ones. But you should be able to find the PDFs on the website as well. So I've titled this lecture, everything you didn't want to know about LMR Techsure and training, because we're going to get into some of the nitty-gritty details that I think most other classes would spare you the details of, you know, like, what should my hyper parameters be in those kinds of questions? Some minor logistics. Also, if you're doing the assignments, we are updating other assignments as we find some mostly minor bugs. Make sure you pull updates to the assignments as you go along. Okay, so what we're going to do, we're going to start with a quick recap of a transformer. And I'll give you two variants of a standard transformer. One that's probably coming from the standard transformer, lectures that you might see in 224N. And then I'll talk about what you implement, and the modern consensus variant of a transformer. And then we're going to take a much more data-driven perspective to understanding transformer architectures.

**中文**: 各位可能已经注意到，我在授课方式上不如珀西那么富有创新性，因此大家将看到的是PowerPoint幻灯片，而非可执行的Python代码。不过，相应的PDF文件同样可以在网站上找到。本次讲座的标题是“关于LMR Techsure与训练，那些你本不想了解却不得不知道的内容”，因为我们即将深入探讨一些非常细致的技术细节——而这些细节，其他大多数课程通常都会略过不讲，比如：超参数究竟该如何设置？此外，还有一些琐碎的事务性说明：如果大家正在完成作业，我们会随着发现（主要是些细微的）Bug而持续更新作业内容，请务必在学习过程中及时拉取作业的最新版本。好了，接下来我们将首先快速回顾一下Transformer模型，并为大家介绍两种标准Transformer的变体：一种源自大家可能在224N课程中见过的标准Transformer讲解；另一种则是实际实现中采用、且目前已被业界广泛接受的现代共识版Transformer。随后，我们将从更加数据驱动的角度出发，深入理解Transformer架构。

## 段落 2

**英文**: So the question that we're going to ask is, people have trained lots of LLMs at this point, and you can go and read all of those papers and try to understand what has changed, what has been in common. And from that kind of almost an evolutionary analysis, you know, try to understand what are the things that are really important to make transformers work, right?. So today's theme is, the theme of the class is the best way to learn is hands-on experience, but the theme of this lecture, because we can't train all these transformers, it's to learn from the experience of others. So the starting point is the original transformer, right? So just as a review, right? Hopefully you all remember this from 224N or your other MLP classes. You know, you've got some simple position embeddings at the bottom, you've got multi-head attention, you've got layer norms afterwards, you've got a residual stream going upwards, you've got an MLP,. and then a soft knacks at the very end. And we're going to see variants to all these different pieces until we get to basically the most modern variants of the transformer. And the latest one, I'll talk about, will be just a few months before. So what you implement in is not, you know, the vanilla transformer variant from the original paper. We've modified a few things.

**中文**: 因此，我们将提出的问题是：目前人们已训练了大量大语言模型（LLM），你可以去阅读所有相关论文，尝试理解其中哪些方面发生了变化、哪些要素保持一致。通过这种近乎“演化分析”的方式，我们试图厘清究竟哪些因素对于Transformer的有效运行真正至关重要。  
今天课程的主题是“实践出真知”，即动手实践是最佳学习方式；但本讲的主题则有所不同——由于我们无法亲自训练所有这些Transformer模型，因此要转而借鉴他人的实践经验。  
我们的起点是原始Transformer架构，对吧？这仅作为复习之用，希望各位还记得在224N课程或其他机器学习基础课程中学过的内容：最底层是简单的位置编码，其上是多头注意力机制，接着是层归一化，再往上是残差连接通路，之后是多层感知机（MLP），最后是Softmax输出层。接下来，我们将逐一考察上述各个组件的各类变体，直至梳理出现代最前沿的Transformer架构。我将介绍的最新变体，距今仅数月之久。因此，你实际实现的并非原始论文中提出的“标准版”Transformer，而是对其若干部分进行了改进的版本。

## 段落 3

**英文**: You know, we've put the layer norm in front of the box, so you can see on this slide over here that, you know, the norm is over here right before each of these blocks in the residual stream. We've asked you to implement rotary position embeddings. The feed forward layers use something called a SWIGLU,. and then linear layers, you know, now emit these biosters. And you might ask, why have you forced us to implement this weird variant of a transformer instead of the original transformer, is all you need transformer? And so we're going to go through some of those questions. And then yesterday I was thinking, okay, I should catch up on all the developments that have happened in architecture last year. And Percy warned me about this because he said you're going to have to redo the lecture every year. And so I started looking and I was like, all right, yeah, there's a couple good papers recently. There's Command Day, there's two ol' Motu Furious, there's, you know, small LM and 5. 4.

**中文**: 你知道，我们将层归一化（LayerNorm）放在了每个模块之前，因此你可以看到，在这张幻灯片上，归一化操作就位于残差流中每个模块之前的这个位置。我们要求你们实现旋转位置编码（RoPE）。前馈网络层采用了一种名为SwiGLU的结构，而线性层如今则输出这些“生物特征”（biosters）。你可能会问：为什么非要我们实现这种看似奇怪的Transformer变体，而不是原始的Transformer？难道仅靠标准Transformer还不够吗？接下来，我们将逐一解答这些问题。  
昨天我还在想：“好吧，我得赶紧补上去年架构领域所有的重要进展。”珀西（Percy）事先提醒过我这一点，他说：“你每年都要重写一遍这堂课的讲义。”于是，我开始查阅相关资料，结果发现：“好吧，确实有几篇近期不错的论文——比如Command Day、Two-OL’ Motu Furious，还有Small LM和5.4。”

## 段落 4

**英文**: And then you go looking and you're like, wow, yeah, there's Gemma 3 and Quent 2. 5 and Internal LM. And then there's, you know, more, I can't even sort of, you know, cover the screen with these guys, right? There's a lot of models. There were about 19 new dense model releases in the last year. Many of them with minor architecture tweaks. And on the one hand, it's kind of annoying to go through all these papers and say like, you know, what is happening in all of these. But also it's like a actually wealth of information because not all of them do the same thing. And you can kind of see, you know, not all of you can, especially in the back, can see the details of this slide. But I put together a little spreadsheet of, you know, what all of these models are doing. And starting with, you know, all the way from 2017, the original transformer, all the way to 2025, what the newest models are doing.

**中文**: 接着你开始查找，然后你会惊叹：“哇，确实有Gemma 3、Qwen 2.5和内部大模型。”此外还有更多——我甚至无法在屏幕上一一罗列出来，对吧？模型数量实在太多了。仅在过去一年里，就发布了约19种新型稠密模型，其中许多仅在架构上做了细微调整。一方面，通读所有这些论文并弄清每篇论文究竟在讲什么，确实令人头疼；但另一方面，这也恰恰构成了极为丰富的信息资源，因为这些模型并非千篇一律。大家可能未必都能看清这张幻灯片的细节，尤其是后排的观众；不过，我已整理了一份简要电子表格，汇总了所有这些模型的功能与特点，时间跨度从2017年最初的Transformer模型，一直延伸至2025年最新发布的模型。

## 段落 5

**英文**: And we'll talk about this as we go. But you kind of see sort of certain kinds of architecture changes, sort of being explored. Like so here on this column is position embeddings. People used to do all sorts of stuff like absolute relative rope. There was a sort of alibi phase for some people. But then now starting around 2023, everyone just does rope, right? So you can kind of see this conversion evolution almost of neural architectures. And we're going to talk about all of these different kinds of things, right? So the parts that I'll cover, so this is a preview of the three major sections of this lecture. And if I have time, I'm also going to talk about different attention variants at the end. The first thing is going to be architecture variations. That's what I'm going to talk about.

**中文**: 我们将在讲解过程中讨论这一点。但您大致可以看到，某些类型的架构变化正被不断探索。例如，这一列展示的是位置编码（position embeddings）。过去人们尝试过各种方法，比如绝对位置编码、相对位置编码、RoPE（旋转位置编码）等。对某些人而言，还曾经历过一段“ALiBi”（Attention with Linear Biases）阶段。然而，自2023年左右起，大家几乎都转向采用RoPE了，对吧？因此，您基本可以观察到神经网络架构的这种渐进式演进过程。我们接下来将逐一探讨所有这些不同类型的方案，对吧？以下是我本次讲座将涵盖的内容概览，即本讲三大主要部分的预告；若时间允许，我最后还会简要介绍各类注意力机制的变体。第一部分将聚焦于架构变体，这正是我接下来要讲解的内容。

## 段落 6

**英文**: So activations, feed forwards, attention variance, position embeddings, all of those things. And then having nailed down the architecture, what do we have to do? Well, we have to pick hyper parameters, right? Like how big do we make the hidden dimension? How big do we make the sort of inner projection layer inside of MLP? What do we do about the number of dimensions? How many vocab elements? Those are all sort of important things that you have to choose when you're actually training your language model. And you don't want to just sort of pick these out of a hat, right? You want to select them in some fairly intelligent way. So we're going to start with architecture variations. And the two things that I'll mention right here, and I'll go back to them as I talk. The first one is there's not that much consensus in a lot of the choices. There's been sort of convergent evolution in the last few years, what I'll call like lama-like architectures at the very bottom here. But people do all sorts of things. They swap between layer norm and RMS norm. They do serial versus parallel layers.

**中文**: 因此，激活函数、前馈网络、注意力方差、位置编码等所有这些组件。在确定了模型架构之后，我们接下来需要做什么呢？显然，我们需要选择超参数，对吧？例如，隐藏层维度设为多大？MLP内部投影层的维度又该设为多大？词向量维度应如何设定？词汇表规模应为多少？这些全都是训练语言模型时必须慎重抉择的关键因素。你当然不能随意拍脑袋决定，对吧？而应以某种相对合理、明智的方式加以选择。因此，我们将首先探讨架构变体。此处我先简要提及两点，并在后续讲解中再回过头来详细展开：第一点是，目前许多设计选择尚无广泛共识；近年来虽出现了一定程度的趋同演化，比如我在图最底部所称的“类Llama架构”，但研究者们仍在尝试各种各样的方案——例如在层归一化（LayerNorm）与均方根归一化（RMSNorm）之间切换，或采用串行结构与并行结构等不同层组织方式。

## 段落 7

**英文**: There's one choice that basically everyone does, but since the first, very first GPT, I'll talk about that in a bit. But there's lots of different variations that we can learn from here. The big one, I've already talked about this guy in 224N, so if you remember that lecture, this will be review for you rather than being totally new, I think that one thing basically everyone agrees on and agreed on almost from the very start is the use of pre-norm versus post norm. That terminology will get a little bit more confusing. But the original transformer paper did, you know, this thing on the left over here, where you had your residual stream in the gray. In addition to the residual stream, you had these layer norms after every subcomponents. You would do your multi-head attention, you would add back to the residual stream, and then you would layer norm that. Then you would do the same thing with your fully connected layer, and then you would layer norm it. Very, very early on, people realized that moving this layer norm to the front of this sort of non-resigial part, so this block on the right, did much better in many different ways. Basically, almost all modern LMs that I know of use this kind of pre-norm.

**中文**: 有一种选择几乎是所有模型都会采用的，但我会稍后再谈最早期的GPT模型。不过，从这里我们可以学到许多不同的变体。其中最重要的一种——我在224N课程中已讲过这位“先生”，因此如果你还记得那堂课，这部分内容对你而言将是复习而非完全陌生——几乎所有人从最初起就基本达成共识的一点，就是采用“前置归一化”（pre-norm）而非“后置归一化”（post-norm）。这一术语可能会稍显混淆。原始Transformer论文采用的是此处左侧所示结构：灰色部分为残差流；除残差流外，每个子模块之后都设有层归一化（LayerNorm）：先执行多头注意力机制，再将结果加回残差流，随后对该和进行层归一化；接着对全连接层同样操作，再对其输出进行层归一化。非常早期的研究者便发现，若将层归一化移至此类非残差部分的前端（即右侧所示模块），则在诸多方面性能显著更优。事实上，据我所知，几乎所有现代大语言模型均采用这种前置归一化结构。

## 段落 8

**英文**: There have been some sort of new innovations recently that I'll touch on in two slides. But lots of models have moved to this. The one exception is OPT 350M, which I'm guessing they kind of messed that one up, and that was sort of orphaned when they were training. That was a fun find in my survey of architectures. This pre-versus post-norm thing, if you look into why it was originally developed, the arguments were that if you wanted to use this post-norm stuff, it was much less stable. You would have to do some careful learning rate warm-up style things to make it train in a stable way. So if you look at some of the earlier papers arguing for this pre-norm approach, Salazar and Yen, and also this Xiong in 2020 paper, you almost always see this comparison of, hey, if we use pre-norm and we do some other stability inducing tricks, then we can remove warm-up, and these systems work just as well if not better than the post-norm layer norm with careful warm-up type approaches. When you see this in sort of machine translations that are in here, you see this as well on the right on various other tasks, especially using BERT, which was trained with post-norm. So there were many arguments about why this was helpful. There were arguments about gradient attenuation across layers.

**中文**: 最近出现了一些新创新，我将在接下来的两张幻灯片中简要介绍。但目前大多数模型已转向这种架构，唯一的例外是OPT-350M——我推测他们在训练该模型时可能出了些差错，导致其在训练过程中被“遗弃”了；这在我对各类架构的调研中是一个有趣的发现。关于“预归一化”与“后归一化”的问题，若追溯其最初开发原因，相关论点指出：若采用后归一化方式，模型训练稳定性会显著降低，必须借助精心设计的学习率预热等策略，方能实现稳定训练。因此，在早期主张采用预归一化方法的论文（如Salazar与Yen的研究，以及Xiong等人2020年的论文）中，几乎总能看到此类对比论述：“若采用预归一化并辅以其他提升稳定性的技巧，则可完全省去学习率预热步骤，且此类系统的表现至少不逊于、甚至优于需谨慎实施预热策略的后归一化层归一化方案。”在当前所列的机器翻译任务结果中，右侧亦可见类似现象，尤其体现在BERT等采用后归一化方式训练的模型所执行的各类其他任务上。学界曾就此提出诸多有益性论证，其中一项关键论点即涉及梯度在各网络层间的衰减问题。

## 段落 9

**英文**: If you do pre-norm, then the gradient sizes would remain constant, whereas if you did post-norm, without warm-up, then it would sort of blow up in this orange way. So reasonable argument. But I think a maybe more closer to modern intuition would be this argument that pre-norm is just a more stable architecture to train. And so some of the earlier work by Salazar and Yen identified all these lost spikes that if you were training with pre-norm kind of in blue here, you would see a lot more lost spikes and the training would be kind of unstable as you were training. So you see the gradient norm here, spiking and generally higher than the one with pre-norm. And so today, you see pre-norm and other layer norm tricks being used essentially as stability inducing aids for training-largenate neural networks. And so this brings us to one new fairly, I think, recent innovation. I think this didn't exist when I gave this lecture last year, which is this variant that I don't think really has a great name, but I'm just going to call it the double norm for the moment here. So this is the original figure that I showed you at the very beginning, and we know that putting layer norms in the residual stream is bad. But actually, someone in 224N this year asked, well, why do you have to put the layer norm in the front? Why can't you put it after the feed-forward network? And of course, you can, and not only that, sort of recent people have gone around and just add the layer norm after the blocks as well.

**中文**: 如果你采用预归一化（pre-norm），梯度规模将保持稳定；而若采用后归一化（post-norm）且不使用学习率预热，则梯度会以图中橙色所示方式剧烈发散。这一论点有一定道理。但或许更贴近现代直觉的理解是：预归一化本身即是一种训练更稳定的网络架构。Salazar 和 Yen 等人早期的研究就已发现诸多“梯度尖峰”现象：若采用图中蓝色所示的预归一化方式进行训练，反而会出现更多此类尖峰，导致训练过程不稳定。如图所示，此时梯度范数频繁尖峰，且整体高于采用预归一化的方案。因此，如今预归一化及其他层归一化技巧，本质上已被广泛用作提升大规模神经网络训练稳定性的辅助手段。由此引出一项较新、我认为也相当近期的创新——据我所知，去年讲授本课程时尚未出现该方法。这一变体目前似乎尚无公认的名称，此处暂且称之为“双重归一化”（double norm）。这正是我在课程伊始展示的原始结构图：我们已知，将层归一化置于残差流中是有害的。然而，今年 224N 课程中有人提问：“为何必须将层归一化置于前端？为何不能将其置于前馈网络之后？”答案当然是可以的；不仅如此，近期研究者们还进一步在各模块之后也添加了层归一化。

## 段落 10

**英文**: And so, Grock and Gema 2 both take this approach of layer norms both in front and after. Almost two does only the layer norm after the feed-forward in the multi-head attention. And so this is actually kind of an interesting change. Pre-norm has just been kind of dominant and the only thing for a while, but things have been changed up a little bit. So now there's a new variant, and this has actually, you know, there's been some evaluations of this kind of approach. People have argued it's a little bit more stable and nicer to train on these larger models. Anyway, feel free to stop me and ask me questions as well. I have a tendency to sort of keep going if no one stops me. So yes. Why is layer norm in the residual bad? Why is layer norm in the residual bad? That's a good question.

**中文**: 因此，Grock 和 Gema 2 均采用在前馈网络和多头注意力机制前后均添加层归一化（LayerNorm）的方式。而 Almost Two 仅在多头注意力机制后的前馈网络之后应用层归一化。这实际上是一种颇为有趣的变化：此前，“预归一化”（pre-norm）方式长期占据主导地位，几乎成为唯一选择，但如今情况已略有改变。目前出现了一种新变体，且已有若干研究对这一方法进行了评估；人们认为，该方法在训练更大规模模型时稳定性略高，训练体验也更佳。此外，大家随时可以打断我、向我提问——若无人打断，我往往会一直讲下去。好的。那么，为何将层归一化置于残差连接内部是不妥的？为何将层归一化置于残差连接内部是不妥的？这是个好问题。

## 段落 11

**英文**: I don't think I can give you like a, you know, this is the proof of why it's bad. I think one, you know, intuitive argument for why this might be bad is that the residual gives you this identity connection all the way from almost the top of the network, all the way to the bottom. And so if you're trying to train really deep networks, this makes gradient propagation very easy. Right. So there's lots of arguments about how, you know, LSTMs and these other kinds of, you know, state space models have difficulty propagating gradients backwards on identity connection does not have any such problems. And so putting layer norms in the middle, you know, might mess with that kind of gradient sort of behavior. And that, you, of course, you see back here. Right. This is exactly the kind of plot you expect to see if that's happening. Okay.

**中文**: 我不认为我能给出一个明确的、能证明它为何不好的证据。不过，我认为一个直观的理由是：残差连接提供了一条从网络几乎最顶层一直贯穿到底层的恒等映射通路；因此，在训练极深网络时，这种结构使得梯度传播变得非常容易。没错。关于这一点，已有大量讨论指出：LSTM及其他类型的（如状态空间模型）在反向传播梯度时存在困难，而恒等连接则完全不存在此类问题。因此，在中间插入层归一化（LayerNorm）可能会干扰这种梯度传播行为。而这，当然也正是你在此处所观察到的现象——没错，这正是当上述情况发生时，你所预期看到的典型曲线图。

## 段落 12

**英文**: Cool. The other thing that people now do is in the original transformer, people did, you know, layer norm. And so layer norm is this equation over here. What you do is you have, you know, the activation X coming in, you subtract the empirical mean. So that's the average of the X is up top. And then you divide by, you know, the standard or the variance plus a little fudge factor epsilon. And then you square root that so you can roughly think about as a standard deviation. Right. So that's going to, you know, standardize your activation X. You're going to scale it up by a gamma that's a learnable parameter and then shifted by a beta.

**中文**: 很好。现在人们做的另一件事是：在原始的Transformer中，人们使用层归一化（LayerNorm）。层归一化的计算公式如上所示：输入激活值X，先减去其实证均值（即X在该层上的平均值），再除以标准差（即X的方差加一个微小的平滑因子ε后的平方根），从而大致将其标准化为标准差形式。这样便可对激活值X进行标准化；随后再用一个可学习参数γ对其进行缩放，并用另一个可学习参数β进行平移。

## 段落 13

**英文**: Right. So this makes sense. You're going to normalize, you know, your activations, and then you're going to shift them around to whatever point you want. And many models use this layer norm thing and it worked quite well. But many models have sort of now moved on to RMS norm. And this is one of the consensus changes, like basically all the models have switched to using RMS norm. And now what do you do? You just drop the mean adjustment. So you don't subtract the mean. You don't add a bias term. And many notable models do this.

**中文**: 没错，这很有道理。你会对激活值进行归一化，然后将其平移至你所需的任意位置。许多模型采用层归一化（LayerNorm），效果相当不错。但如今，许多模型已逐步转向RMS归一化（RMSNorm）。这一转变已成为业界共识，几乎所有模型都已改用RMS归一化。那么具体该怎么做呢？只需省略均值调整步骤：既不减去均值，也不添加偏置项。许多知名模型均采用这种做法。

## 段落 14

**英文**: The llama family, Palm, Chinchilla T5, they've all moved to RMS norm. And what's the reason for this? One reason is that it doesn't really make a difference. Turns out if you train models with RMS norm, does just as well as training with, you know, layer norm. And so there's a simplification argument. But really, I think the argument that's often given in these papers, and I think it's good to appreciate kind of the details of this argument, is that going to RMS norm is, you know, it's faster and just as good. So in one way is a faster. Well, if I don't subtract the mean, it's fewer operations. If I don't have to add that bias term beta back, it's fewer parameters that I have to load from memory back into sort of my compute units. Right. So I don't have to retrieve this sort of state.

**中文**: 骆马（Llama）系列、Palm、金丝熊（Chinchilla）T5等模型均已转向RMS归一化（RMS Norm）。原因何在？其一在于，实际效果并无明显差异：实验表明，采用RMS归一化训练的模型，性能与采用层归一化（LayerNorm）训练的模型相当。因此，这是一种简化方案。但真正常被相关论文所强调、且值得深入理解的理由是：RMS归一化速度更快，且效果不打折扣。具体而言，由于无需减去均值，计算操作更少；又因无需将偏置项β加回，需从内存加载至计算单元的参数数量也相应减少，从而避免了这类状态数据的读取。

## 段落 15

**英文**: And some of you might be thinking, but wait, you told me in 224N that nothing but matrix multiplies matter for the purpose of runtime. Right. And this is not a matrix multiply. And so I shouldn't care about, you know, any of this. And that's a reasonable perspective to take. If you think about, you know, the number of the percentage of flops that is taken up by different operations in a transformer. This table, there's a nice paper by even all in all in 2023. I think the title is like memory movement is all you need or something that does profiling of all the different components of a transformer. And you see that, you know, tensor contractions, which are like matrix multiplies, that's like 99. 8% of the flops that happen in a transformer.

**中文**: 你们中有些人可能会想：“等等，你之前在224N课程中告诉过我，就运行时间而言，唯一重要的是矩阵乘法。”没错。而这里并不是矩阵乘法，因此我似乎无需关注这些细节。这种看法是合理的。如果考虑一下Transformer中各类运算所占浮点运算（FLOPs）的比例，可以参考2023年一篇由Even等学者撰写的优秀论文，其标题大致为《内存移动才是关键》之类，该文对Transformer的各个组件进行了性能剖析。从中可以看到，张量收缩（即矩阵乘法）占据了Transformer中全部浮点运算的99.8%。

## 段落 16

**英文**: And so, you know, saving 0. 17% of your flops doesn't seem like a huge win. But I think one of the things that's important for architecture design now is to not just think about flops because, you know, flops are important, but that's not the only resource that you have to think about. It's also that you have to think carefully about, you know, memory movement. And so even though, you know, tensor contractions. So this is things like matrix multiplies that's like 99. 8% of the flops. You know, if you have things like the softmax operation or layer norms, all these like normalization operations that happen in a transformer, there is 0. 17% of the flops, actually there are 25% of the runtime. And a big reason for that is because, you know, these normalization operations still incur a lot of memory movement overhead.

**中文**: 因此，你知道，节省0.17%的浮点运算量似乎并非重大突破。但如今架构设计中一个关键点在于，不能只关注浮点运算量——诚然，浮点运算量很重要，但它并非唯一需要考量的资源；还需审慎考虑内存数据搬运。例如，张量收缩（如矩阵乘法）占据了99.8%的浮点运算量，而像Softmax操作、层归一化（LayerNorm）等Transformer中常见的各类归一化操作，虽仅占浮点运算量的0.17%，却消耗了25%的运行时间。造成这一现象的重要原因在于：这些归一化操作仍会带来巨大的内存搬运开销。

## 段落 17

**英文**: Right. And so it does actually matter to try to optimize some of these like lower level things because it's not just about flops. It's also about memory movement. I'm going to emphasize this quite a bit more as I get into the systems lecture, like when we talk about GPU architectures, it's going to become very, very, very important to think about memory, not just about flops. And so this is one of the reasons why RMS norm has now become sort of much more popular. And so I went back and looked at some of the earlier RMS norm papers. I think the sad thing is that there aren't quite as many papers published by industry labs with big nice oblations. And so many of the oblations that I'll show you are going to be from a couple years back. But Narang at all in 2020 had this very nice ablation showing, you know, here's the vanilla transformer, here's the RMS norm version. And you kind of see the exact thing I told you, you know, the number of steps per second that you can do in a vanilla transformer, 3.

**中文**: 没错。因此，实际上确实有必要去优化其中一些底层环节，因为这不仅仅关乎浮点运算次数（FLOPs），还涉及内存数据搬运。随着后续系统课程的展开——尤其是讨论GPU架构时——我将更加强调这一点：思考内存问题将变得极其、极其、极其重要，而不仅仅是关注FLOPs。正因如此，RMS Norm如今才变得愈发流行。为此，我回溯查阅了一些早期关于RMS Norm的论文。令人遗憾的是，工业界实验室发表的、带有详尽消融实验（ablation）的高质量论文数量仍显不足。因此，我接下来展示的许多消融实验结果，大多来自几年前的研究。不过，Narang等人在2020年发表了一项非常出色的消融研究：例如，这里展示了标准Transformer模型，以及采用RMS Norm的版本；你大致可以看到我之前所讲的现象——标准Transformer每秒可完成的训练步数为3。

## 段落 18

**英文**: 5 per second. With RMS norm, you got 3. 6, you know, not a huge game, but that's in some sense for free. And you get, you know, a final loss that's lower than the blue and yellow transformer. So that's great, right? In some sense, we've gotten run time improvements. And we've gotten, in fact, at least in this case, loss improvements. And so that's a win win for us. The final thing that I'll say, which is very much in line with this RMS norm thing in terms of theme, is that most modern transformers do not have bias terms. So the original transformer, if you look at the FFN, will look something like this, right? You have your inputs X, you're going to do a linear layer with a bias term, and then you'll rel you it, and then you'll have a second linear layer wrapping around it. But most implementations, if they're not gated units, which I'll talk about in a moment, look actually something like this, they just drop the bias terms.

**中文**: 每秒5次。使用RMS归一化时，你得到的是3.6——你知道，提升幅度不算巨大，但某种程度上这是“免费”获得的。而且，最终损失也低于蓝色和黄色的Transformer模型。这很棒，对吧？从某种意义上说，我们既获得了运行时性能的提升，又（至少在此例中）实现了损失值的降低，可谓双赢。最后我要提的一点，与前述RMS归一化思路高度一致：当前大多数Transformer模型已不再使用偏置项。例如，原始Transformer的前馈网络（FFN）结构大致如下：输入为X，先经过一个带偏置项的线性层，再经ReLU激活，最后再接一个线性层。但如今大多数实现（除非采用我稍后将介绍的门控单元）实际上都直接省略了这些偏置项。

## 段落 19

**英文**: You can just make this argument from basically the same kinds of underlying principles, you know, they perform just as well. Matrix multiplies are apparently all that you need to get these guys to work. And the other thing, which is maybe more subtle, is actually optimization stability. I don't quite have the deepest understanding of why the bias terms are particularly bad for stability. But there's been sort of really clear empirical observations that people have made that basically dropping these bias terms often stabilizes the training of these largest neural networks. So now a lot of the implementations now emit bias terms entirely and train only on these like pure matrix multiply kind of settings. So that's the that's the layer norm bit. And so there is kind of two things that you know you should kind of think of this is nice because the story is pretty clear. Everyone does something, and so you should just kind of know this, right? Basically everyone does pre norm or at least they do the the layer norms outside of the residual street like that's kind of the iron rule, right? You know you get nicer gradient propagation, you get much more stable training. It just doesn't make sense to do it the other way.

**中文**: 你完全可以基于本质上相同的基本原理来提出这一论点——众所周知，这些方法的表现同样出色。矩阵乘法似乎正是让这些模型正常运转所需的全部运算。另一点或许更为微妙，即实际的优化稳定性。我对为何偏置项特别不利于稳定性尚无最深入的理解，但人们已通过大量清晰的实证观察发现：通常情况下，移除这些偏置项往往能显著提升超大规模神经网络的训练稳定性。因此，目前许多实现方案已完全省略偏置项，仅在纯粹依赖矩阵乘法的设置下进行训练。以上便是层归一化（LayerNorm）部分的内容。由此可知，有两点值得你注意：首先，这一设计思路十分清晰明了，因而颇具吸引力；其次，业界普遍采用这种做法，你理应对此有所了解——事实上，几乎所有模型都采用预归一化（pre-norm）结构，至少会将层归一化置于残差连接之外，这几乎已成为一条铁律。如此设计不仅可获得更优的梯度传播效果，还能大幅提升训练稳定性；反其道而行之则毫无意义。

## 段落 20

**英文**: Most people or most almost everybody does RMS norm. Practice it works almost as well has fewer parameters to move around. And this idea of dropping bias terms just broadly applies. A lot of these models just don't have bias terms in most places. I think the one exception to this RMS norm one as I was reading yesterday is I think co here both command day and R plus use layer norm. Not quite sure why. Okay, any questions on kind of the layer norm RMS norm and bias terms stuff before I move on. Yes question. You think there are some long term lessons you can take away from these details that are more future proof potentially or do you think these are. Yes, so the question was is there is there something more future proof and I think it's hard to have like the biggest picture in many ways.

**中文**: 大多数人，或者说几乎所有人，都采用RMS归一化（RMS Norm）。实践表明，它的效果几乎与层归一化（Layer Norm）相当，且需要调节的参数更少。而省略偏置项（bias terms）这一思路则具有更广泛的适用性——目前许多模型在大多数地方干脆就不使用偏置项。不过，我昨天阅读时注意到一个例外：据我所知，Command R和R+这两个模型在RMS归一化中仍采用了层归一化。至于具体原因，目前尚不明确。好了，在进入下一话题之前，大家对层归一化、RMS归一化以及偏置项这些内容还有什么问题吗？  
是的，请提问。  
您认为，从这些细节中能否提炼出一些更具长期价值、更面向未来的原则？还是说这些细节本身只是阶段性方案？  
是的，您的问题是：是否存在更具未来适应性的原则？对此，我认为从宏观层面给出明确结论其实非常困难。

## 段落 21

**英文**: Deep learning has been very empirical and like bottom up rather than top down, but I do think there's some generalizable lessons that you could sort of draw from here. I think the lesson of you know have very direct identity map residual connections is sort of a story and a lesson that has played out in many, many different kinds of architectures not just you know in these kinds of architectures. The effectiveness of layer norm will see once again later on in this lecture has been very effective and so not letting your activations drift in sort of scale is another thing that I think generally has been very effective for training stability. Those two seem like fairly generalizable lessons. We will also kind of see sort of the systems concerns come into play again. This is another generalizable lesson of sort of thinking really carefully about the impact of your architecture on the systems components of your design. Okay, so now there's this other component which is the activations and there is a whole big zoo of activations. There are value, gellu, swish, lu, gellu and then there's I mean these aren't activations. There are different kinds of MLPs, gellu, gellu, cellu, swigglu and ligglu. And yeah, I think this is exactly the kind of thing that I didn't originally want to learn when I got into doing deep learning.

**中文**: 深度学习一直具有很强的经验性，其发展路径更像是自下而上而非自上而下；但我认为，从中确实可以提炼出一些具有普适性的经验教训。例如，“采用非常直接的恒等映射残差连接”这一理念，便是一个反复在众多不同架构中得到验证的经验——而不仅限于我们当前讨论的这类架构。层归一化（LayerNorm）的有效性也将在本讲后续内容中再次体现，它已被证明极为有效；因此，避免激活值在尺度上发生漂移，是提升训练稳定性的另一条普遍有效的经验。上述两点似乎都具有较强的普适性。此外，系统层面的考量也将再度浮现——这同样是一条普适性经验：即必须审慎思考架构设计对系统组件所产生的影响。好了，现在来看另一个组成部分：激活函数。目前激活函数可谓种类繁多、五花八门，包括ReLU、GELU、Swish、LU、GeLU等；不过严格来说，这些并非全是激活函数——其中有些实为不同类型的多层感知机（MLP）结构，例如GELU、GeLU、CELU、SwiGLU和LiGLU。没错，这类内容恰恰是我当初投身深度学习领域时，原本并不想深入学习的部分。

## 段落 22

**英文**: I don't care about activations, it's going to train anyway, but it really does matter unfortunately for both you and me that swigglu and other gellu variants just consistently work well. And so I will explain those to you and you should think about them carefully because they do work and internalize that. So I think the value and maybe the gellu you all should already know, right, the value you learn in like some of the most basic deep learning classes, right, you just take the max of zero. And in the case of MLP, right, you've got your drop the bias terms here, you know, x dot w1, you take the value and then you do w2 fairly easy, right. A gellu is a gouching error linear unit. This one multiplies the linear with a cdf of a gouching. And so it's basically going to be like the relu, but with a little bit of a bump here, hopefully you can see that over here. This is not just flat at the very bottom. This makes things a little bit more differentiable, which may or may not help. And the GPT family of models, 123 and GPT J and so on, all use the gellu.

**中文**: 我并不在意激活函数的选择，模型反正都会训练，但遗憾的是，SwiGLU及其他GeLU变体能否持续表现良好，对您和我而言都至关重要。因此，我将向您详细解释这些激活函数，您需要认真思考，因为它们确实有效，请务必深入理解并内化。我想大家应该已经熟悉GeLU（高斯误差线性单元）的价值了，没错，这是最基础的深度学习课程中就会学到的内容：即取输入与零之间的最大值。而在多层感知机（MLP）中，如您所知，此处省略了偏置项，先计算输入x与权重矩阵w1的点积，再对结果应用激活函数，最后乘以权重矩阵w2——整个过程相当简单。GeLU是高斯误差线性单元，它将线性变换结果与高斯分布的累积分布函数（CDF）相乘。因此，其函数图像大致类似于ReLU，但在零点附近会呈现一个平滑的“隆起”，希望您能清楚地看到这一点——它并非在最底部完全平坦。这种设计使函数在零点附近更具可微性，可能带来一定益处，也可能没有。GPT系列模型（包括GPT-1、GPT-2、GPT-3以及GPT-J等）全部采用GeLU激活函数。

## 段落 23

**英文**: And the original transformer and some of the older models used the relu and really almost all the modern models have switched to the gated linear units like swigglu and the gellu and others, right. And really, I think this is, you know, the Google folks really pushed for this like palm and p5 and others. But since it's sort of been tried and true, basically almost all the models post 2023 use a gated linear unit. And so, you know, going back to that earlier question of like what generalizable architecture things can we learn from this lecture. You know, there are some things that have really consistently been very useful residual connections later norms. Gating is yet another one, right. And so this is another place where gating just appears and is a very good way of doing things. So originally, this is our fully connected layer right here, right. This is with a relu. Now, instead of doing just linear and a relu, what I'm going to do is I'm going to gate, you know, the output here with an entry wise linear term.

**中文**: 原始的Transformer模型及一些较老的模型使用的是ReLU激活函数，而实际上几乎所有现代模型都已转向门控线性单元（如SwiGLU、GeLU等），对吧？我认为，这主要是谷歌团队大力推动的结果，例如PaLM、P5等模型。由于该方法已被实践验证有效，因此2023年之后发布的几乎所有模型均采用门控线性单元。因此，回到之前那个问题——即从本讲座中我们能学到哪些具有普适性的架构设计原则？确实存在一些长期被证明极为有效的设计：残差连接、后置归一化，而门控机制则是另一项重要原则。换言之，门控机制在此类架构中反复出现，且被证实是一种非常有效的设计方式。最初，这里的全连接层正是采用ReLU激活函数。而现在，我们不再简单地进行线性变换后再接ReLU，而是改为对当前输出进行逐元素的门控操作，即引入一个逐元素线性项来实现门控。

## 段落 24

**英文**: So x dot v is going to give me a vector. And I'm going to multiply that entry wise with my original inside term of the mlp. And then I'm going to multiply the whole thing with w2, right. So the way to think about this is I've gated sort of the hidden part of the mlp. Right. So I've got my original activation that takes my inputs and puts it into the sort of hidden space. And then I'm going to gate that with x dot v. And then, you know, I'm going to project that back into sort of the hidden dimensionality using w2. Right. So there's this gating operation that happens entry wise.

**中文**: 因此，x 与 v 的点积将得到一个向量。接着，我将该向量与 MLP 原始内部项按元素相乘，然后再将整个结果乘以 w2，对吧？可以这样理解：我对 MLP 的隐藏层部分进行了某种门控操作。具体而言，我首先通过原始激活函数将输入映射到隐藏空间；然后，再用 x 与 v 的点积对该隐藏表示进行门控；最后，再利用 w2 将门控后的结果投影回隐藏维度。因此，这一门控操作是按元素进行的。

## 段落 25

**英文**: And that's really, you know, the basic thing that's happening here. And this is the the GLU plus the relu. So the red glue. And then we have an extra parameter that we've added here for the gating. This is v. And so when someone says something like, oh, it's a gaggle. There's nothing to laugh about that. There's the gaggle fully connected layer. What I've got here is, you know, I've got the gaggle, sort of, for the nonlinearity. And I've still got the exact same gating here, what x dot v.

**中文**: 而这实际上正是此处发生的基本情况。此处为GLU（门控线性单元）加上ReLU激活函数，即红色的门控单元。此外，我们还额外引入了一个用于门控的参数，即v。因此，当有人提到“gaggle”（门控）时，这并非一个可笑的说法——它指的就是门控全连接层。我在此处所采用的，实际上是一种用于非线性变换的“gaggle”结构，而门控机制本身则完全保持不变，即仍为x与v的点积（x·v）。

## 段落 26

**英文**: Right. And this is the architecture that was used by many of the Google models like t5v1. 1, gamma 2, gamma 3. And then another variance, there's a swig glue. And this has been very, very popular. Swishes x times the sigmoid. And this is the nonlinearity. And you can kind of, you know, a sigmoid is like this and x is like this. So it will look, you know, just like the Gaussian error unit. And then, you know, you do the same thing here.

**中文**: 没错。这种架构被许多谷歌模型（如T5 v1.1、Gamma 2、Gamma 3）所采用。另一种变体是Swish激活函数，它已变得极为流行：即x乘以sigmoid(x)。这是一种非线性函数。你可以这样理解：sigmoid函数呈S形，而x是一条直线，因此其组合曲线看起来类似于高斯误差线性单元（GELU）。接着，你在此处执行相同的操作。

## 段落 27

**英文**: You have a gating over the swish. And then you get a fully connected layer here. Yes. The question below a certain negative value, the swish function and also the also the gating function. It's not nonlineically increasing. In fact, it's decreasing. Right. And a lot of the argument about how gradient descent works in like input out permission learning is like, OK, you want to do gradient descent. But here it seems like you would go in opposite direction. If you use gating words or swish or their gated version.

**中文**: 你对Swish函数施加了一个门控机制，随后在此处接上一个全连接层。是的。当输入低于某个负值时，Swish函数以及门控函数均不再呈非线性递增，实际上反而呈递减趋势。没错。而关于梯度下降在诸如输入输出映射学习中如何起作用的诸多讨论指出：好的，我们希望执行梯度下降；但此处的情形似乎表明，若采用门控机制、Swish函数或其门控变体，优化方向反而会与预期相反。

## 段落 28

**英文**: So yeah. So the question was, you know, this isn't monotonically decreasing. You know, there's a, there's a bit on the very left of this zero here that's kind of flipping in the, in the derivative. And isn't that going to be a problem? I think intuitively you could, you could have argued that this would be a problem. You might trap a bunch of activations at zeros. I think in practice, you know, if you look at kind of like neural network optimization dynamics, what's actually happening is often you're throwing very high learning rates with momentum into the optimizer. And so you're not really going to converge to the zero point, right? Like these activations are going to be all over the place. And so in practice, I don't think this little tiny negative piece is really an effect that's going to be huge for the model. If that makes sense. Okay.

**中文**: 所以，是的。问题在于，如你所见，该函数并非单调递减。你看，在这个零点最左侧附近，导数存在一段轻微的振荡现象。这难道不会造成问题吗？从直觉上讲，你可能会认为这确实会是个问题，比如可能导致大量激活值被困在零点。但实践中，如果你观察神经网络的优化动态过程，实际情况往往是：我们通常会采用很高的学习率并结合动量（momentum）来优化模型。因此，模型实际上并不会收敛到零点，对吧？这些激活值会四处波动、变化剧烈。所以，在实际应用中，我认为这一段极小的负值区域对模型整体性能的影响微乎其微。这样解释是否清楚？好的。

## 段落 29

**英文**: And then going back to this, the Swigloo is basically most models today, like the Lama family, Palm, Omo. And I'll show you the big table later. But you'll see that the Swigloo is very, very popular. And one thing to know, I'll talk about this again in the hyper parameters part is, you know, now remember I've added this, this V term, this extra parameter, right? And so I want to, you know, think about how to size this extra parameter. And what people do is gated models usually make this like hidden size, you know, the basically output dimensionality of W slightly smaller by a factor of two thirds. In order to make sure that the total number of parameters of this whole thing remains the same as the non gated counterparts. And that's a convention thing that most people do. If that you don't quite understand what that is, I'll go back over that again later. But you can just kind of keep in mind that basically for the gated linear units, you just make everything a little bit smaller to make sure things are remain parameter matched. So, oh yes, question.

**中文**: 然后回到这一点，Swigloo 实际上是当前大多数模型（如 Llama 系列、PaLM、Omo 等）所采用的架构。稍后我会向您展示一张汇总大表，届时您会发现 Swigloo 极其流行。另外需要了解的一点是——我将在超参数部分再次详述——请记住，我已在此处新增了一个 V 项，即这个额外参数，对吧？因此，我们需要思考如何设定该额外参数的规模。通常，门控模型会将该参数设为隐藏层尺寸，即让权重矩阵 W 的输出维度略微缩小，约为原尺寸的三分之二，从而确保整个模型的总参数量与非门控模型保持一致。这是一种业界普遍采用的惯例。若您目前尚不完全理解这一点，稍后我还会再作详细说明。但您只需大致记住：对于门控线性单元（GLU），我们只需将各部分尺寸略微调小，以确保参数总量与非门控模型严格匹配。哦，对了，有人有问题吗？

## 段落 30

**英文**: Maybe obviously one of the best. One of the best is that it's very easily differentiable by input. But if you have the derivative of the CDF, the Gaussian, you have like a squared with X, does that not really so things down? That's a very good question. I'm not 100% sure what the internal like CUDA implementation of the Swigloo or the the Gellu Gellu is. I think it's entirely possible that like internally they might be implemented with like lookup tables. Go ahead. So, you want really manages the memory pressure here and like you'll be the exact same as you're reading the same level elements. Sure, right. So, the extra computed negative role on the new. That's actually, yeah, that's probably a better argument that like basically flops wise, this is negligible anyway.

**中文**: 也许显而易见，它确实是最佳方案之一。其中一大优势在于其对输入的可微性极佳。但若你拥有高斯累积分布函数（CDF）的导数（即高斯概率密度函数），其表达式中包含关于变量X的平方项，这难道不会显著拖慢计算速度吗？这是个非常好的问题。我对SwiGLU或GeLU在CUDA内部的具体实现方式并非百分之百确定。我认为，它们在内部完全有可能采用查表法（lookup tables）来实现。请继续。因此，你真正需要关注的是此处的内存压力，而由于读取的是相同层级的元素，其内存访问模式实际上完全一致。没错，正是如此。那么，新增的负向计算开销呢？实际上，是的，这或许是一个更有力的论据：从浮点运算量（FLOPs）角度看，这部分开销本身几乎可以忽略不计。

## 段落 31

**英文**: And actually the memory calculus is the same. So, okay, cool. All right. So, duGated linear units work. I will have more modern evidence for this as well. But I thought, you know, I should take you straight to the the horses mouth, no, I'm shizier's original paper, where he, you know, evaluates all these GLU variants. And, you know, this is somewhat older stuff. So, you're seeing cola and SST2 performance. But you do see basically that the GLU variants consistently perform better, right? GLU is 84. 2, 84.

**中文**: 实际上，内存计算是相同的。所以，好的，不错。好吧。因此，门控线性单元（GLU）是有效的。我还将提供更现代的证据来支持这一点。不过，我想——你知道的——我应该直接带你们去看“源头”，不，是希泽尔（Shazeer）的原始论文，他在其中评估了所有这些GLU变体。而你们知道，这属于较早的研究成果。因此，你们看到的是CoLA和SST-2任务上的性能表现。但你们确实可以明显看出，各类GLU变体的性能始终更优，对吧？GLU得分为84.2、84。

## 段落 32

**英文**: 12, 84. 36, 84. 67. And, you know, wow, it's 2020s. They even give you the standard deviation. So, you can sort of figure out how significant those results are. And they, in fact, are significant, right? And so, this is some nice evidence to see here. There was also, you know, the Naurang at all in 2020 paper, which is a very nice paper studying all sorts of architecture variants. I think in the context of T5 style models. And once again, you see that the Gated linear unit variants consistently achieve kind of lower losses than their counterparts, right? Like you see that the bold lines are exactly at the GLU variants.

**中文**: 12、84.36、84.67。而且，你知道，现在已是2020年代了，他们甚至提供了标准差，因此你大致可以判断这些结果的显著性。事实上，这些结果确实具有显著性，对吧？因此，此处呈现了一些不错的证据。此外，还有2020年发表的《Naurang et al.》论文，这是一篇非常优秀的研究论文，探讨了各种架构变体——我认为是在T5风格模型的背景下展开的。同样，你也会发现门控线性单元（GLU）变体始终比其对应模型实现了更低的损失值，对吧？例如，图中加粗的线条正对应着GLU变体。

## 段落 33

**英文**: And this pattern has basically held up. So, for gating and activations, you know, there are lots of, lots of variants across different models. But the Gated linear unit has become basically widespread and dominant. And I think for good reason. Of course, the GLU isn't necessary for a good model. Like, it's important to separate the two, right? Just because it's probably the slightly better and everyone does it doesn't mean it's necessary. And you do see examples of very high performance models not using a GLU. Like GPT-3 is one example. More recent one. NimoTron 340B uses a squared relu, which I had not seen before.

**中文**: 这一模式基本保持不变。因此，在门控和激活函数方面，不同模型存在大量变体。但门控线性单元（GLU）已基本成为主流并占据主导地位，我认为这是有充分理由的。当然，GLU并非构建优秀模型的必要条件——这一点很重要，必须明确区分：仅仅因为GLU可能略优且被广泛采用，并不意味着它不可或缺。事实上，我们也能看到一些高性能模型并未采用GLU，例如GPT-3就是一个例子；更近期的例子是NimoTron 340B，它采用了平方ReLU（squared ReLU），这是我此前未曾见过的。

## 段落 34

**英文**: And Falcon 2 11B uses a relu. Both of those are relatively high performance models. So you can kind of see that it's not really necessary. And so, you know, evidence does point towards consistent gains from Swigloo and Gagglue. And that's why we ask you to implement exactly that variant. Cool. Okay. The final thing that I want to talk about for architectures. And this is one kind of final major, I want to say, variation that we've seen. Normally, the transformer block is serial, right? In the sense that, you know, you're for each block, the outputs come in from the bottom.

**中文**: 而Falcon-2 11B使用的是ReLU激活函数。这两者均为性能相对较高的模型，因此可以看出，（使用SwiGLU或Gated Linear Unit）实际上并非必需。现有证据确实表明，采用SwiGLU和Gated Linear Unit能带来持续的性能提升，这正是我们要求你实现该特定变体的原因。很好。好的，关于架构，我最后要讨论的内容如下：这也是我们迄今所观察到的一种最终且重要的变体。通常情况下，Transformer模块是串行的，即每个模块的输入均来自其下方（前一模块的输出）。

## 段落 35

**英文**: And then you do your attention. And then you pass the result of that computation forward. And then you do your MLP. And then you pass that computation forward. And so this is inherently serial. You do attention and then MLP. But of course, this might have certain like parallelism constraints. If you want to paralyze this over gigantic, you know, sets of GPUs, it might be harder to do so if you have the serial connection, you know, the systems concerns might also be more difficult, right? You might get lower utilization from your GPUs. And so a few models have done this thing that I'll call parallel layers. Where basically, instead of having serial computation of attention and then MLP, they will do them both at the same time, right? So you will get your X, you know, from your previous layer, you will compute both the MLP and the attention side by side.

**中文**: 接着，你执行注意力计算，然后将该计算的结果向前传递；再执行多层感知机（MLP）计算，再将该计算结果向前传递。因此，这一过程本质上是串行的：先执行注意力计算，再执行MLP计算。但显然，这种结构可能带来某些并行化限制。例如，若想在数量庞大的GPU集群上实现并行化，串行连接会使并行化更难实现，系统层面的问题也可能更加棘手——比如GPU的利用率可能因此降低。因此，一些模型采用了我称之为“并行层”的设计：即不再按顺序依次执行注意力计算和MLP计算，而是同时进行这两项计算。换言之，你从上一层获得输入X后，会并行地同时计算MLP和注意力部分。

## 段落 36

**英文**: And then you will add them together into the residual stream. And then that will be your output. And this was pioneered by GPTJ, which was kind of this open source replication effort. And the folks at Google doing palm were kind of bold enough to do this at the really big scale. And many others have kind of followed since. So if you're implementing this right, you can share a lot of stuff like the layer norms and the matrix multiplies can get fused together. And you can get some systems efficiencies out of that. It hasn't been quite as popular since then, at least in the last year. I think most of the models that we've seen have been serial layers rather than parallel ones. I think the only exceptions to this are like, here, command A, command R plus, and a Falcon 2, 11 B.

**中文**: 然后，你将它们相加到残差流中，这便是你的输出。这一方法最早由GPT-J开创，它本质上是一项开源复现工作；而谷歌开发PaLM的团队则大胆地将其应用于超大规模模型中，此后许多其他研究者也纷纷跟进。因此，若正确实现该结构，你便能共享大量组件，例如层归一化（LayerNorm）操作，且矩阵乘法也可融合执行，从而获得一定的系统效率提升。不过，自那以后，该方法的流行度有所下降，至少在过去一年中是如此。据我们所见，目前大多数模型采用的是串行层结构，而非并行层结构；唯一的例外包括：Command A、Command R Plus以及Falcon-2 11B。

## 段落 37

**英文**: So now I think we have the ability to kind of go back to, you know, this big, you know, heart to see chart and then see what I was sort of pointing at at the very beginning. So this column here, you know, you don't really need to be able to read any of the text because I think the colors will tell you everything you need to see. This check mark here, this is basically pre versus post norm. The only two models I really know of in the early days that did post norm. This is the original transformer and GPT and BERT, if you want to include that into this table. And then almost everybody else, I think basically everyone else has done pre norm. The only other non-check boxes here are models that are proprietary and I don't have details for. This column here on the left most thing, this is RMS norm versus layer norm. The gray boxes are the layer norm, the blue ones are RMS norm. Basically most people have converged to RMS norm, as I said.

**中文**: 因此，现在我认为我们能够回到之前那张大型的心形图表，从而回顾我在最开始所指出的内容。此处的这一列，您其实无需阅读任何文字，因为颜色本身已能说明一切。此处的对勾符号，基本表示“预归一化”与“后归一化”的对比；在早期模型中，我所知仅有的两个采用后归一化的模型是原始Transformer以及GPT和BERT（若您希望将BERT也纳入此表）。而其余几乎所有模型均采用了预归一化。表中其余未打勾的模型均为专有模型，其具体细节我无从获知。最左侧的这一列，则用于区分RMS归一化与层归一化：灰色方框代表层归一化，蓝色方框代表RMS归一化。如前所述，目前绝大多数人已趋同于采用RMS归一化。

## 段落 38

**英文**: This column next to it is serial and parallel layers. Once again, most people do serial, but you see other variants. What I'm going to talk about next is going to be position embeddings and that will be kind of more interesting in a moment here. Any questions about any of this architecture stuff before I move on? Hopefully that gives you a bit of an overview of at least the major variations and architectures that we see. Yes. So the question was whether serial is more efficient than parallel. It should be actually the reverse that parallel is more efficient than serial. And that's why you're kind of willing to do this. So in some sense you might expect serial to be more expressive because you're composing two computations rather than just adding them together. But the benefit of parallel in theory is that if you write kind of the right kinds of fused kernels, a lot of these operations can be done in parallel or the computation is shared across the different parallel parts.

**中文**: 紧邻该列的是串行与并行层结构。同样，大多数人采用串行方式，但你也能看到其他变体。接下来我将介绍位置编码（position embeddings），这部分内容稍后会变得更为有趣。在我继续讲解之前，大家对上述架构相关内容还有任何问题吗？希望这至少能帮助大家概览我们目前所见的主要变体和架构。  
是的。刚才的问题是：串行结构是否比并行结构更高效？实际上恰恰相反，并行结构比串行结构更高效，这正是我们倾向于采用并行结构的原因。从某种意义上说，你可能会认为串行结构表达能力更强，因为它是将两种计算进行组合，而非简单相加；但并行结构在理论上的优势在于：若编写出恰当的融合内核（fused kernels），许多运算便可并行执行，或不同并行分支之间可共享计算资源。

## 段落 39

**英文**: So the last thing I want to talk about in architecture land, I think this is the last thing, is variations in position embeddings. I think this one's interesting because in the first few years of sort of LM land, there were a lot of different things that people were trying. So in the main embeddings were from the original transformer, you should have learned this in 224N, there's sign and cosine positions. Many others did absolute embeddings like the GPT's and OPT, all basically just added a position that learned position vector to the embedding. And some others like T5 and Gofer did various kinds of relative embeddings that add vectors to the attention computation. And then I think most models have converged to rope, which is relative position embeddings. And this I think actually started in GPTJ, once again another open source contribution, it has really rapidly been picked up by most of the models. And so the high level thought process behind rope is that the thing that matters is relative positions of these vectors. And so if I have an embedding f of x of i, where x is the word I'm trying to embed, and i is my position, then I should be able to write things down in this way. So if x is the f, such that f of x i and f of y j, if I take the inner product of these embeddings, then I can write this down as some different function g, which is a function of the two words and the difference in their positions.

**中文**: 因此，我想在架构领域讨论的最后一点——我认为这也是最后一点——是位置嵌入（position embeddings）的变体。我认为这一点很有趣，因为在大语言模型（LM）发展的最初几年里，研究者们尝试了大量不同的方法。主流的位置嵌入源自原始Transformer模型（你们在224N课程中应该已学过），即正弦与余弦位置编码；而许多其他模型（如GPT系列和OPT）则采用绝对位置嵌入，即简单地将一个可学习的位置向量加到词嵌入上；还有一些模型（如T5和Gopher）则采用了各类相对位置嵌入，将向量直接加入注意力计算过程。此后，大多数模型逐渐收敛到RoPE（旋转位置嵌入），即一种相对位置嵌入方法。这一方法实际上最早由GPT-J提出——又一次源于开源社区的贡献——并迅速被绝大多数模型所采纳。RoPE背后的核心思想在于：真正重要的是这些向量之间的相对位置关系。因此，若用f(x, i)表示对单词x在位置i处的嵌入，则可将相关表达式写作如下形式：对于任意两个单词x和y及其对应位置i和j，其嵌入f(x, i)与f(y, j)的内积可表示为某个函数g，该函数同时依赖于这两个单词以及它们位置之差（j − i）。

## 段落 40

**英文**: So this is a definition that enforces basically position invariance or absolute position invariance. So you only pay attention to the how far apart these two words are. So you do a brief check and see, what happens with signs? Well, you get these cross terms that are not relative. So you do still leak absolute position information, absolute positions, like it's in the name, it's not a relative position embedding. Well, it is relative, but it's not an inner product, so it sort of violates this constraint. And so rope is this kind of clever observation that we do know one thing that is invariant to sort of absolute things, which is rotations. And so we're going to exploit that structure to come up with our position embeddings. Right. We know that inner products are invariant to arbitrary rotation. So we're going to leverage that.

**中文**: 因此，这是一种本质上强制实现位置不变性（即绝对位置不变性）的定义。也就是说，你只关注这两个词之间的距离有多远。我们简单检验一下符号会发生什么情况：结果会得到一些非相对性的交叉项。因此，你仍然会泄露绝对位置信息——正如其名称所示，它并非一种相对位置嵌入。虽然它具有相对性，但并非基于内积，因此在某种程度上违反了这一约束条件。而RoPE则基于一个巧妙的观察：我们确实知道一种对绝对量保持不变的变换，即旋转变换。因此，我们将利用这种结构来构建位置嵌入。没错，我们知道内积在任意旋转变换下保持不变，我们将充分利用这一性质。

## 段落 41

**英文**: So on the left, this is the starting point. Let's say my embedding for the word we is this arrow over here. And my embedding for the word no is this other arrow over here. Now, I want to embed this sequence. We know that. And I only, you know, I look at the word we and no. So how do I do that? Well, we in position zero. So I'm not going to rotate that guy at all. No is in position one. So I'm going to rotate him by, you know, one unit of rotation.

**中文**: 因此，在左侧，这是起始点。假设“we”这个词的嵌入向量是此处的这个箭头，而“no”这个词的嵌入向量则是此处的另一个箭头。现在，我想对序列“We know that”进行嵌入。而我仅需关注其中的单词“we”和“no”。那么该怎么做呢？“we”位于位置零，因此我完全不对其旋转；“no”位于位置一，因此我将其旋转一个单位的角度。

## 段落 42

**英文**: And so now I have this embedding for we know. And now let's say I want to embed this sequence. Of course, we know now we and no have the same relative positioning to each other. And so let's look at what happens. We get shifted by two positions. I rotate we by, you know, I start, you know, in this vertical position and I rotate him twice one and two. And then I rotate no by three positions because it's one, two, three, sorry, zero, one, two, third position. Right. And so now if you look at these two arrows, they have the same relative angle, right. So their inner products are preserved.

**中文**: 因此，现在我得到了“we know”的嵌入表示。现在假设我想嵌入这个序列。显然，“we”和“know”此时具有相同的相对位置关系。我们来看一下会发生什么：二者均发生两位偏移。“we”被旋转两次（即从初始的垂直位置开始，依次旋转一次、两次）；而“no”则被旋转三位（因为其位置索引为零、一、二，即第三位）。对吧？因此，现在观察这两个箭头，它们之间的夹角保持不变，对吗？也就是说，它们的内积得以保持。

## 段落 43

**英文**: And so this is kind of the nice fun idea about rope. You just rotate the vectors and the rotation angle is determined by the position of each word. And rotations, you know, the inner products don't care about relative rotations. And so these inner products are only going to look at sort of the difference in distance. Now it's easy to think about in 2D because rotations are kind of obvious in 2D. There's only one way to rotate a vector. But in high dimensional spaces where we operate, it's not obvious at all how we are going to do this rotation. So the rope folks came up with, you know, in some ways the simplest but also effective way of doing this. And the way to do it is you take your high dimensional vector in this case D. And I'm just going to cut it up into blocks of two dimensions.

**中文**: 因此，这就是关于旋转位置编码（RoPE）的一种巧妙而有趣的想法：你只需对向量进行旋转，而旋转角度则由每个词的位置决定。众所周知，内积对相对旋转是不敏感的，因此这些内积仅会关注某种意义上的距离差。在二维空间中，这种思路很容易理解，因为二维中的旋转非常直观——向量只有一种旋转方式。但在我们实际操作的高维空间中，如何实现这种旋转却远非显而易见。于是，提出RoPE方法的研究人员找到了一种在某种程度上既最简单又行之有效的实现方式：具体做法是，将此处的高维向量（维度为D）按每两个维度一组进行分块。

## 段落 44

**英文**: And every two dimension is going to be rotated by some theta. So there's going to be a rotation speed. And I'm going to rotate the pairs of dimensions. And so now every pair of dimensions is encoding, you know, all these relative positions. And much like in signing cosine embeddings, I'm going to pick some set of theta such that some embeddings are rotated quickly. And others are rotated much more slowly. So they can capture both high frequency information or like close by information and very far away sort of lower frequency positioning information. And the actual rope math here is, you know, if you're going to think about rotations, it's just going to be multiplying with various signing cosine rotation matrices. Hopefully you remember this kind of from linear algebra and trig. And so you can think about this as an operation where you multiply, you know, your embedding vectors with these, you know, block two by two block matrices.

**中文**: 每两个维度都将按某个角度θ进行旋转，因此会存在一个旋转速度。我将对维度对进行旋转，从而使每一对维度都能编码所有这些相对位置信息。这与正弦-余弦位置编码类似，我将选取一组特定的θ值，使得某些嵌入向量旋转得较快，而另一些则旋转得慢得多，从而既能捕捉高频信息（例如邻近位置信息），也能捕捉低频信息（例如相距很远的位置信息）。此处RoPE（旋转位置编码）的实际数学运算，即若考虑旋转变换，则仅需将向量与各类正弦-余弦旋转矩阵相乘——希望你还能回忆起线性代数和三角学中的相关内容。因此，你可以将此操作理解为：将嵌入向量与这些分块的2×2矩阵相乘。

## 段落 45

**英文**: And there's no sort of additive or cross terms that sort of appear here. This is all purely relative. One thing that is different, if you're used to sort of absolute position embeddings or signing cosine embeddings here is that the rope is going to operate at the actual attention layer, right? You're not going to add position embeddings at the bottom, whenever these attention computations are going to be done, you're going to intervene on that layer. And then that's going to give you your position information. And so, you know, I pulled this from, I think, the long implementation of rope, you know, you've got the initial normal attention stuff at the very top, like query, keys and values. These are, you know, your normal linear projections. And then, you know, you're going to come up with cosine and sign angles. These are rotation angles telling you how much to rotate different blocks of the query and key. And then so you take your query and your key. And you're going to rotate them by the cosines and signs.

**中文**: 此处不会出现任何加性项或交叉项，全部都是纯粹的相对关系。有一点不同之处在于：如果你习惯于使用绝对位置嵌入（absolute position embeddings）或正弦-余弦位置嵌入（sinusoidal cosine embeddings），那么RoPE（旋转位置嵌入）则直接作用于实际的注意力层——你并非在模型底层添加位置嵌入，而是在执行注意力计算的该层上直接进行干预，从而获得位置信息。如我所引自（我认为是）RoPE的长实现版本所示：最上方首先是标准的注意力计算流程，即查询（query）、键（key）和值（value），它们均通过常规的线性投影得到；接着，你会计算出余弦角与正弦角，这些旋转角度决定了查询向量和键向量各分块需旋转的程度；最后，你便利用这些余弦值和正弦值对查询向量和键向量分别实施旋转。

## 段落 46

**英文**: And now you've gotten rotated query and rotated key. And that's going to be what's going to go into the rest of your attention computation. Right? So you don't do this at the bottom. You do it whenever you generate your queries and keys. Hopefully that's, that's clear. That's really critical to enforcing kind of this relative positioning on me information. Okay? Good. So, one of the things I want to highlight is that rope is actually one of the things that it seems like everyone has converged on. I, you know, went through all 19 of those papers over the weekend. And basically all of them now use rope for various different reasons.

**中文**: 现在，你已经得到了旋转后的查询（query）和旋转后的键（key），而这些将直接用于后续的注意力计算。对吧？因此，你并非在模型底层执行这一操作，而是在生成查询和键时即刻完成。希望这一点已足够清晰——这一步对于在信息中强制引入相对位置编码至关重要。好的？很好。我想特别强调的一点是，RoPE 实际上已成为业界普遍采用的方法之一。我周末通读了全部19篇相关论文，结果发现，目前几乎所有论文都基于各种不同原因采用了 RoPE。

## 段落 47

**英文**: And so, you know, the reason that rope has now many different algorithms for extrapolating context length. And that's an important part of sort of the modern productionized language model. But also it seems to be empirically quite effective, even at fairly small scales and small context length. So it's kind of one out on this, what's it called position embedding battle? Okay. Any questions before I move on to some of the hyper prime of your stuff? Yes? Is there any representation consistent across all of these models? I don't think they're all the same. There's some variation in the theta's. Oh, yes. Are the, the theta's like for each pair are those hypergrams or are they trained? So, the, the, the theta's that determine the rotation angles, they're not hyper parameters much like in the, in the signs and cosines here. There's kind of a schedule to the rotation angles that are determined. And it's in the same intuition as in the signs and cosines.

**中文**: 因此，您知道，现在 Rope 拥有多种不同的算法来外推上下文长度。这也是现代已投入生产的语言模型中一个至关重要的组成部分。同时，实证结果表明，即使在规模较小、上下文长度较短的情况下，该方法也相当有效。因此，它某种程度上算是这场所谓“位置嵌入之争”中的一匹黑马。好了，在我转向您那些更前沿的内容之前，大家还有什么问题吗？  
是的？  
这些模型是否采用了一致的表示方式？  
我认为它们并不完全相同，各模型中的 θ 参数存在一定差异。  
哦，是的。那么，每一对对应的 θ 是超图（hypergram）还是经过训练得到的呢？  
实际上，决定旋转角度的这些 θ 并非超参数，而与这里正弦和余弦函数中的参数类似。旋转角度遵循某种预设的调度方案，其设计直觉与正弦和余弦函数中的设定一致。

## 段落 48

**英文**: You want to cover different frequency ranges in order to get higher or lower frequency information. Yes? The rotations create any difficulty with like tracking. I wonder like this like angular issues. The rotations themselves don't really create any issues because one way of thinking about a rotation is that it's just a matrix multiply, right? Since theta's are fixed, right? And the M's here are fixed. This is really just a fixed matrix that multiplies your vector. And so in that sense, it's not really a issue. If you were learning the theta's then maybe you have issues because you're, you know, maybe differentiating through trig functions, but you're not doing that here. So, okay, cool. So now I think we go even one more level into the details here. And we're going to talk about hyper parameters.

**中文**: 您希望覆盖不同的频率范围，以获取更高或更低频率的信息，对吗？旋转操作是否会给跟踪带来困难？我担心可能存在角度相关的问题。实际上，旋转操作本身并不会引发任何问题，因为从某种角度看，旋转本质上只是矩阵乘法，对吧？毕竟这里的θ是固定的，对吧？而且此处的矩阵M也是固定的。因此，这实际上只是一个固定矩阵与您的向量相乘的过程，从这个意义上讲，并不存在真正的问题。如果您需要学习θ参数，那么才可能遇到问题，例如在对三角函数进行求导时可能出现困难；但在此处您并未进行此类操作。好的，明白了。接下来，我们将进一步深入细节，讨论超参数。

## 段落 49

**英文**: I feel like when you have to, you know, you're dropped in and you're asked to train a, you know, a new language model. There's a lot of questions you have about hyper parameters because there's quite a few of them. And one of the things that I've realized is that actually only a few of these really get changed across different successful models. There's actually like fairly clear rules of thumb and fairly clear guidelines that people seem to be following. So, you know, there are some things like how much bigger should the feed forward size be or how many heads should I have or what should my vocab size be? And so we'll talk about each of those things and we'll try to constrain the space of hyper parameters that people have. So, you know, the starting point, we're going to look at a simple feed forward layer, you know, just the, you know, with the bias, let's say. This is a relu version of it. And so there's two hyper parameters here. There's D model, which is the dimensionality of X, right? That's the input coming into your MLP. And then you've got DFF.

**中文**: 我感觉，当你不得不——比如，被临时委派去训练一个新语言模型时——你会对超参数产生大量疑问，因为这类参数数量相当多。而我逐渐意识到，实际上，在不同成功模型之间真正发生变化的超参数其实只有少数几个。人们所遵循的，其实是相当明确的经验法则和清晰的指导原则。例如，前馈网络的尺寸应比模型维度大多少？注意力头数应设为多少？词表规模又该设定为何值？接下来，我们将逐一探讨这些问题，并尝试收窄人们实际需要调节的超参数范围。首先，我们从一个简单的前馈层出发——即带偏置项的全连接层（假设采用ReLU激活函数）。该层包含两个超参数：一个是D_model，即输入X的维度，也就是传入多层感知机（MLP）的输入维度；另一个是DFF（前馈网络隐藏层维度）。

## 段落 50

**英文**: So this is the feed forward dimension. This is kind of the output hidden dimension of your MLP. And from there, you're going to project back onto D model, right? So what should DFF be? In general, you know, these things are going to be up projections, right? You're going to have more hidden units than there were inputs. But how much bigger? Well, there is actually just like a consensus. Almost everybody that uses, you know, relu style MLPs are going to pick DFF is equal to four times D model. And this is, I will show you some empirical evidence for why this is a same number later. But as far as I can tell, there's no like, you know, law of nature that says you have to pick four. This is a convention that has really held up. Now, there are a few exceptions to this rule. Remember that the GLU variants are going to scale this down by a factor of two thirds, right? And if you scale it down by a factor of two thirds, you're going to have roughly the same number of parameters.

**中文**: 因此，这就是前馈网络的维度，也就是多层感知机（MLP）输出层的隐藏维度。接着，你需要将该维度再投影回模型维度 $D_{\text{model}}$，对吧？那么 $D_{\text{FF}}$ 应取多少呢？通常而言，这些维度属于上采样（即隐藏单元数多于输入单元数），但具体应扩大多少倍？实际上，业界已形成一种共识：几乎所有采用 ReLU 风格 MLP 的研究者都会将 $D_{\text{FF}}$ 设为 $4 \times D_{\text{model}}$。稍后我将向你展示一些实证依据，说明为何这一数值被广泛采用。但据我所知，并不存在某种“自然法则”强制要求必须取 4；这纯粹是一种经实践检验、长期沿用的惯例。当然，该规则也存在少数例外情形：例如 GLU 变体结构会将该维度缩小为原来的三分之二，而若按此比例缩小，则参数总量大致保持不变。

## 段落 51

**英文**: You can do a little bit of math. And if you scale the GLU variants down by a factor of two thirds, you'll come to the conclusion that the way to do that is to set DFF equal to eight over three D model, right? That's going to be the number that you end up at. And you can sort of convince yourself that that will give you the same number of parameters. And that's the ratio that you would get if you started with a ratio of four. So if you look at many of the models, they actually do follow this rule of thumb. Palm, for example, you know, palm, mistro and llama are slightly larger. These are GLU models, but they don't follow this 2. 6 rule. But if you look out, for example, llama, you know, one, quen, deep seek, e, and t5, they all roughly follow this like kind of 2. 6-ish rule.

**中文**: 你可以进行一点数学计算。如果你将GLU变体的尺寸按三分之二的比例缩小，就会得出结论：实现这一目标的方法是将前馈网络维度（DFF）设为模型隐藏层维度（D_model）的八分之三，对吧？这正是你最终得到的数值。你大致可以验证，这样设置确实能获得相同数量的参数。而若初始比例设为4，你所得到的也正是这一比例。因此，观察许多模型时，它们实际上都遵循这一经验法则。例如PaLM、Mixtral和LLaMA的尺寸略大，这些属于GLU架构模型，但并未严格遵循这一2.6倍规则；然而，若进一步考察LLaMA-1、Qwen、DeepSeek、E和T5等模型，便会发现它们均大致符合这一约2.6倍的经验法则。

## 段落 52

**英文**: And I can sort of put up the, the big table of LMS that I made later with hyper parameters, many, many, many of them fall into this roughly 2. 6 range. And that's the standard parameterization of a GLU union. I'll go through one other exception. I really like this exception because I think in many ways, you know, big, large language model training is a game of copying hyper parameters from other people. So we don't learn very much, right? Like it's very conservative. But t5, I really like, because in some sense it's really bold. And I think Google people actually do some pretty bold stuff. And so if you look at the 11 billion parameter t5 model, they have a pretty, pretty incredible setting. And their hidden them is 1024, but their DFF, you know, their up projected dimension is 65,000, right? And so that's going to give you a 64 times multiplier on the ratio of DFF to D model.

**中文**: 而且，我稍后可以展示我制作的大型语言模型（LMS）超参数大表，其中大量、大量、大量的模型参数值都大致落在2.6左右。这正是GLU激活函数的标准参数化设置。接下来我再介绍另一种例外情况。我特别喜欢这种例外，因为从很多角度看，大规模语言模型的训练本质上就是照搬他人的超参数——我们其实并未学到太多东西，对吧？这种做法非常保守。但T5模型却令我格外欣赏，因为它在某种意义上确实非常大胆；我认为谷歌的研究人员实际上确实开展了一些相当大胆的探索。例如，观察其110亿参数的T5模型，其设定就极为惊人：隐藏层维度（hidden size）为1024，而前馈网络的升维维度（DFF，即上投影维度）高达65,000。这意味着DFF与模型维度（D_model）之比达到了64倍。

## 段落 53

**英文**: And of course, you know, you compare to this where palm is like a factor 4 and everyone else is, you know, much smaller. This is a very large difference. And there's some other recent examples of using much bigger, you know, multipliers like gamma 2 kind of follows in these footsteps and does a factor of eight. And I'll talk a little bit about this exception later. Of course, t5 was a totally fine model. So this should tell you it is possible to train a model with, you know, such a much larger ratio. So one of the things that I think is, you know, quantitative evidence, you know, I saw that 4x multiplier and I thought, is that really the right thing to do? Or is there some more quantitative experiment someone's done to convince me that that is a good idea? So one of the figures from Jared Kaplan sort of scaling law paper and most people know this paper for the scaling law component. But actually, there's also some really useful hyper parameter components this paper. You'll actually see that they do exactly this thing that I'm talking about the DFF to D model ratio. And they plot essentially how much the loss increases as you vary this.

**中文**: 当然，众所周知，与之相比，PaLM 的该比值约为 4 倍，而其他模型则要小得多。这一差异非常显著。此外，近期还有一些其他案例采用了更大的倍数，例如 Gamma-2 模型便沿袭了这一思路，将该比值提升至 8 倍。稍后我将简要讨论这一例外情况。当然，T5 是一个完全成熟、性能良好的模型，因此这表明：训练具有如此大比值的模型是完全可行的。我认为其中一项关键的定量证据是——当我看到这个 4 倍的倍数时，不禁思考：这真的是最佳选择吗？还是已有某些更严谨的定量实验，足以令人信服地证明这一方案的合理性？杰瑞德·卡普兰（Jared Kaplan）关于缩放定律的论文中就有一张图表（该论文广为人知的是其缩放定律部分），但事实上，其中还包含一些极具实用价值的超参数分析内容。你将发现，该文恰恰探讨了我正在提及的“前馈层维度（DFF）与模型隐层维度（Dmodel）之比”，并绘制了该比值变化时损失函数随之增加的具体趋势图。

## 段落 54

**英文**: And you kind of see that there's kind of a sweet spot. This is, you know, a ratio of 1, 2, 3, 4, and then up to like 10 or so here, right? And so there's a pretty wide basin here anywhere between 1 to maybe up to 10 where, you know, you can pick whatever feed forward ratio you want. And it'll be roughly optimal. And 4 is not too far off from your optimal choices over here. It's like 1, 2, 3, 4. It's like right here or maybe right here. Right? So that's a pretty reasonable choice. So what can we learn from all this hyper parameter stuff? I think a lot of the evidence point stores, you know, you can pick the same defaults of, you know, if you're not using a GLU, you can multiply by 4. If you're using a GLU, you can use roughly 2. 66.

**中文**: 你大致可以看出存在一个“最佳区间”。这里展示的是1、2、3、4，直至约10的比值，对吧？因此，在1到约10这个相当宽泛的范围内，你可以任选前馈比例，结果都大致最优。而4与这里的最优选择相差并不大——最优选择大致在1、2、3、4附近，可能就在此处，或稍偏此处，对吧？因此，4是一个相当合理的选择。那么，从所有这些超参数设置中我们能学到什么？大量实证表明，你可以直接采用相同的默认值：若未使用门控线性单元（GLU），则乘数设为4；若使用GLU，则乘数约为2.66。

## 段落 55

**英文**: And they can work pretty well for mostly all the modern LMS. T5, once again, does show that you don't have to follow these rules, right? You can be a rule breaker and do whatever you'd like. There's no hyper parameter choice written in stone. You can get reasonable LMS at many other hyper parameters. That said, I think the really funny epilogue to this story, right, is that T5 has a follow-up model called T5v1. 1. That's improved, and it uses a much more standard 2. 5 multiplier on GLU, right? So, you know, you can read between the lines and say like maybe they looked at, you know, the original T5 and said actually, maybe we want to walk back that 64 times multiplier and pick a more standard one, and they did end up with a better model. So, cool. So, I think that's a good question.

**中文**: 而且，它们在绝大多数现代语言模型（LMS）中都能表现得相当出色。T5再次表明，你其实不必拘泥于这些规则，对吧？你可以打破规则，随心所欲地尝试。并不存在一成不变的超参数选择；在许多其他超参数设置下，你同样可以得到合理的语言模型。不过，这个故事真正有趣的是其后续：T5之后推出了一个改进版本，名为T5v1.1，它采用了更为标准的GLU缩放系数——即2.5倍，对吧？因此，你不妨揣摩一下其中的深意：或许研究人员回顾了最初的T5，意识到“也许我们应该收回原先64倍的缩放系数，转而采用更常规的数值”，而最终他们确实凭借这一调整得到了性能更优的模型。挺酷的，对吧？所以，我认为这是一个很好的问题。

## 段落 56

**英文**: So, the question was, what's the relationship between this ratio that I'm talking about here and generally the impact on the model, right? And so, if we go all the way back here, the ratio is controlling essentially how wide the hidden part of this MLP is. And so, the original justification in the T5 paper for picking 64 was to say, actually, we can get bigger and fatter matrix multiplies if we make that dimension really, really large. And while that is a kind of a true statement, you know, the wider it is, you know, you're getting more parallel computation, so to speak, rather than serial computation. So, you're spending your flops and your parameters in a slightly different way than if you made your hidden units bigger, which would let you pass more information or using more units, which would give you sort of more serial computation, right? So, you're spending your parameters and your flops in a slightly suboptimal way from expressive power, but you might get systems gains if sort of your matrices are wide enough. Okay. Excellent. So, another thing that is a surprising, or maybe not surprising, consensus hyper parameter is the ratio between the model dimension and the head dimension times the number of heads. So, I clipped this from 224N, right? But really, the basically canonical choice is to pick things so that the dimension D, that's a hidden dimension, and if you have multiple heads, you're just going to split up the number of dimensions each head gets, right? So, you're going to keep the dimensions fixed as you add more heads, and you don't have to do that, right? As you add more heads, you could just keep the same number of dimensions per head, and you could just let the attention part take more and more parameters, right? You could do that. That's an option that you have. But most models, once again, do follow this guideline.

**中文**: 因此，问题在于：我此处所讨论的这一比率与模型整体性能之间究竟存在何种关系？如果我们回溯到最初，该比率实质上控制着该多层感知机（MLP）隐藏层的宽度。T5论文中最初选择64作为该比率的依据是：实际上，若将该维度设得非常大，我们便能实现更大、更“胖”的矩阵乘法运算。尽管这一说法在某种程度上确有道理——即维度越宽，意味着并行计算越多，而非串行计算——但这也意味着：你消耗浮点运算量（FLOPs）和参数的方式，与单纯增大隐藏单元数量（从而传递更多信息）或增加单元数量（从而实现更多串行计算）的方式略有不同。换言之，从表征能力角度看，这种参数与FLOPs的分配方式略显次优；但若矩阵足够宽，则可能带来系统层面的性能增益。很好。另一项令人意外（抑或并不意外）的共识性超参数，是模型维度与“头维度×头数”之间的比率。我此处截取自224N模型，但事实上，业界普遍采用的标准做法是：令隐藏维度D保持固定；当使用多个注意力头时，仅将D均分给每个头。也就是说，随着头数增加，各头所分配的维度数会相应减少，而你其实并非必须如此——你完全可以选择保持每个头的维度数不变，仅让注意力模块占用越来越多的参数。这确实是一种可行方案。然而，绝大多数模型仍遵循上述惯例。

## 段落 57

**英文**: We see GPT-3, T5, Lambda, Palm, and Lama-2. They all have a ratio of one or almost exactly one. T5 is the one exception that breaks this rule. They tried the big ratio of 16, but otherwise it is all fairly following this consensus. There's been a couple papers that have argued against this one-to-one ratio. There's a notable one by, I don't know how to pronounce this, Boja Penali at all 2020. If you have more and more heads, they're going to have lower and lower rank, and if you have very few dimensions per head, that's going to start affecting the expressiveness of the attention operation. But in practice, it doesn't really seem like we see too many significant low-rank bottlenecks in practice, and most of the models with this ratio of one seem to do just fine, right? This is really a parameter that's generally been held constant by most of the models that we've seen. If I have time, I'll talk a little bit about different optimizations that people have made on this multi-head component, but hyper parameter rise, things have stayed fairly similar. I think one of the big ones in terms of hyper parameters is the aspect ratio.

**中文**: 我们看到GPT-3、T5、Lambda、PaLM和LLaMA-2，它们的头数与每头维度之比均为1或几乎恰好为1。T5是唯一打破这一规律的例外；他们曾尝试过16的较大比例，但其余模型基本都遵循这一共识。已有若干论文对这种一对一比例提出质疑，其中一篇颇具影响力的论文出自Boja Penali等人（2020年），至于作者姓名发音，我尚不确知。其核心论点是：若注意力头数持续增加，每个头的秩便会不断降低；而若每头维度过少，则会削弱注意力机制的表达能力。但在实践中，我们似乎并未观察到显著的低秩瓶颈问题，且采用该1:1比例的大多数模型表现均相当良好，对吧？事实上，这一参数在迄今所见的多数模型中基本保持恒定。若时间允许，我将简要介绍人们针对多头注意力组件所做的一些优化工作；不过就超参数而言，整体变化并不大。我认为，在诸多超参数中，头数与每头维度之比（即“纵横比”）是尤为关键的一项。

## 段落 58

**英文**: So we can think about deep networks, we can have more and more layers, or we can have wide networks. And generally, if you want one knob to control the width, that would be the hidden dimension of the residual street. That would control essentially the width of almost all the operations at once. And so this seems like a pretty critical thing to tune. You might think that deeper networks are smarter and more expressive, or wider networks are more efficient. There is generally a sweet spot of ratios that people have picked. There have been sort of outliers. Some of the early models used much smaller ratios here. So that means they were much wider than they were deep. And then some models have gone really deep, where they had way more sort of de-, sorry, the other way around, really wide, where they had way more de-model than end layer.

**中文**: 因此，我们可以考虑深度网络（即层数越来越多），也可以考虑宽度网络（即每层神经元数量越来越多）。通常，若想用一个参数来控制网络的宽度，这个参数就是残差支路中的隐藏层维度，它实际上能同时控制几乎所有运算操作的宽度。因此，这似乎是一个非常关键的调优参数。你可能会认为，更深的网络更“聪明”、表达能力更强，而更宽的网络则更高效。实践中，人们通常会选取一个深度与宽度之间的“最佳比例”，尽管也存在一些例外情况：早期某些模型在此处采用了小得多的比例，这意味着它们的宽度远大于深度；而另一些模型则走向了另一个极端——深度极大增加，或者说，其宽度远超深度（原文此处表述有误，已按合理逻辑修正）。

## 段落 59

**英文**: And there's been generally a sweet spot of saying we want about 128 sort of hidden dimensions per layer. And that has been generally stuck to by a lot of the GPT-3 and Lama variant models. And I'll talk a little bit about evidence for that in a second. There's considerations about aspect ratio that are quite important. They will control the amount of sort of parallelism that we can do. So if you're doing something called pipeline parallel, what you're often going to do is you're going to take your different layers, and you're going to cut them up, and you're going to put them on different devices or different blocks of devices, because you'll parallelize within each layer as well. And so there's going to be certain kinds of constraints that you're going to put on your model. And also, if you have really wide models, then you can do something called tensor parallel, where you slice up the matrices, and then you distribute those on GPUs. And one thing that we'll learn in, I think, 1, 2, 3, 4, or 5 lectures, is that these different parallelism paradigms are going to have different constraints. You need really fast networking for tensor parallel, and you can maybe get away with slower networking or higher latency networking for pipeline parallel.

**中文**: 通常，人们普遍认为每层约128维的隐藏维度是一个较优的选择，GPT-3及Llama系列变体模型大多沿用了这一设定。稍后我将简要介绍支持该选择的一些依据。  
关于模型宽高比（aspect ratio）的考量也极为重要，它直接影响我们所能实现的并行程度。例如，在采用“流水线并行”（pipeline parallel）时，通常会将不同网络层进行切分，并将其分别部署到不同的设备或设备组上；同时，各层内部也会进行并行化处理。因此，模型设计需满足某些特定约束条件。此外，若模型宽度极大，则可采用“张量并行”（tensor parallel）策略：将权重矩阵进行切片，并将这些切片分发至多个GPU上。我们将在第1、2、3、4或5讲中了解到，上述各类并行范式具有不同的约束条件——张量并行对网络带宽要求极高，而流水线并行则对网络带宽要求相对较低，甚至可容忍更高延迟的网络环境。

## 段落 60

**英文**: And so your networking constraints might in turn drive some of these like with depth considerations, but setting that aside, you might abstractly ask, you know, what is the impact of aspect ratio model performance? And once again, cappellans at all have a really nice visual sort of aid showing how aspect ratio impacts performance. And so this is three different scales, 50 million, 274 million, and 1. 5 billion parameters, and the x axis, the x effect ratio, y axis, is sort of lost difference in percentage change. And you see that, you know, around the hundred, right, which is, once again, I told you, was the, around the consensus choice of hyper parameters is the minimum across different scales, right?. So this is kind of backed by some of this, like, large scale hyper parameter data that's been published by cappellan at all, and roughly matches that intuition. And a really nice thing here is it seems to be the case that aspect ratio optimard does not shift too much across several orders of magnitude here. So if this holds up even more, that's, that's very good news. You can keep training on one fixed aspect ratio. One thing I will know that is quite an interesting result is on eK and others at Google had this very interesting paper sort of studying impact of depth versus width, both upstream and downstream. And one of the things that they found was that if you're looking at losses, then it doesn't really matter.

**中文**: 因此，您的网络约束可能会反过来影响其中一些因素（例如深度考量），但暂且将这一点搁置一旁，您或许可以抽象地发问：长宽比对模型性能究竟有何影响？同样，卡佩兰等人提供了一幅非常直观的图表，清晰展示了长宽比如何影响模型性能。图中展示了三种不同规模的模型：5000万、2.74亿和15亿参数；横轴为长宽比，纵轴则表示损失变化的百分比差异。您可以看到，在长宽比约为100处（即我之前提到的、被广泛采纳的超参数共识值附近），各规模模型均呈现出性能最低点。这一现象得到了卡佩兰等人所发布的大型超参数实验数据的支持，也大致符合我们的直觉判断。此处一个非常可喜的现象是：最优长宽比在多个数量级范围内并未发生显著偏移。倘若这一规律在更大范围内依然成立，那无疑是个极好的消息——您便可始终采用固定的长宽比进行训练。此外，值得一提的一项颇具启发性的研究成果来自谷歌的E.K.及其同事，他们发表了一篇极具洞察力的论文，系统研究了模型深度与宽度在预训练阶段及下游任务中的影响。其中一项关键发现是：若仅考察损失函数值，则深度与宽度的具体分配其实并无显著影响。

## 段落 61

**英文**: The only thing that matters, deeper models don't help you. But the story is less clear if you're looking at downstream accuracy, at the time they were looking at sort of fine tuned superglue accuracy, they were arguing that for the same amount of flops deeper models might be better. So I'll sort of just leave it at that. There's not quite as much follow up to this work, at least in the open that I've seen, but downstream performance may actually be slightly different in terms of the aspect ratio considerations here. Okay. Cool. The final thing that I want to talk about in this sort of very little hyper parameter world is what are kind of the vocabulary sizes that you might want to pick. And in general vocabulary sizes have been trending upwards. And I think a big part of why is because, you know, LLMs are being deployed out in the wild. They're becoming more useful services.

**中文**: 唯一重要的是：更深的模型并不会带来帮助。但若着眼于下游任务准确率，情况则不那么明确；在他们考察微调后的SuperGLUE准确率时，曾提出：在相同计算量（FLOPs）下，更深的模型可能表现更优。因此，我暂且就此打住。目前尚未见到太多针对这项工作的后续研究——至少据我所见的公开资料而言是如此；不过，在下游性能方面，此处涉及的宽高比（即模型深度与宽度的权衡）考量，实际效果可能略有不同。好的，很好。最后，在这个超参数极少的背景下，我想探讨的最后一个问题是：应选择多大的词表规模？总体而言，词表规模呈上升趋势，我认为一个重要原因在于：大语言模型正被广泛部署到真实环境中，正日益成为更有用的服务。

## 段落 62

**英文**: And that happens. You're going to interact with people speaking different languages, people using emojis, all sorts of other kinds of, you know, almost modalities or languages, than what you might expect. And so I think some of the earlier models and especially model, lingual models, range around in the 30 to 50,000 token vocabulary range. And you can kind of see this in like GBT's, the early LLOMAs. But if you look at the multilingual or I would call like production systems that have come out, they've all sort of been shifting towards the 100 to 250,000 range for their vocabulary sizes. And you know, I looked at Command A, which is one of Cohears models. There are company that emphasize a lot of multilingual stuff. You know, you see very large vocab sizes from them, even with GPT-4. And many others that have copied the GPT-4 tokenizer are going to be around the 100K tokens. Right.

**中文**: 这种情况确实会发生。你将与使用不同语言的人交流，与使用表情符号的人交流，还会遇到各种其他类型的、近乎模态化或语言化的表达方式，远超你的预期。因此，我认为一些早期模型，尤其是多语言模型，其词表规模通常在3万至5万词元之间。这一点在GBDT模型以及早期的LLaMA模型中可见一斑。但若观察近期发布的多语言模型，或我称之为面向实际应用的系统，它们的词表规模普遍已转向10万至25万词元区间。例如，我研究过Cohere公司的一款模型Command R——该公司非常重视多语言能力，其模型词表规模极大，即便GPT-4也不例外。此外，许多采用GPT-4分词器的模型，其词表规模也基本在10万词元左右。

## 段落 63

**英文**: And so that's kind of the standard that a lot of people are operating out roughly at 100K to 200K token size. And I think there's been work showing that as models get bigger, these models can in some sense handle more and more or make good use of more and more vocab elements. And you might see, you know, increasing trends to token counts as models get scaled up or more data is used to train them. Cool. Okay. So the last thing, this is no longer sort of specific hyper parameters, but sort of two other things that you might need to do before you sort of set your model to run, which is drop out in other kinds of regularization. And I think this one was really interesting to me when I was originally doing kind of the research for putting this lecture together. And if you sort of think about pre-training, pre-training is about the furthest place that you might think of from regularization. Right. Because pre-training, you do usually like one epoch, right.

**中文**: 因此，目前许多人的标准大致是将词元（token）数量设定在10万至20万之间。我认为已有研究表明，随着模型规模增大，这些模型在某种意义上能够处理越来越多的词汇元素，或更有效地利用更多词汇元素。你可能会发现，随着模型规模扩大或训练所用数据量增加，词元数量呈现上升趋势。很好。好的。最后一点——这已不再属于具体的超参数范畴，而是另外两类在正式启动模型前可能需要设置的内容：即“丢弃法”（Dropout）及其他形式的正则化。当我最初为准备本次讲座开展相关研究时，这一点令我尤为感兴趣。试想一下预训练：预训练恰恰是你所能想到的、与正则化概念距离最远的场景，对吧？因为在预训练阶段，我们通常只进行一个训练轮次（epoch），对吧。

## 段落 64

**英文**: You can't even go through all of your data because you have too much of it. So you're going to do one epoch training, and you're almost certainly not overfitting the data in that one pass that you're doing. Right. And so you might think, all right, we don't need regularization for pre-training, right. Let's just set your optimizer loose. It's all about minimizing loss. And this is really good arguments for why you shouldn't need to regularize. But then if you look at what people do, the story is actually kind of mixed. And this story actually is maybe even more mixed than what has turned out to be. But early days, people did a lot of drop out.

**中文**: 你甚至无法遍历全部数据，因为数据量实在太大了。因此，你只会进行一轮训练（即一个epoch），而几乎可以肯定的是，仅凭这一轮遍历，你根本不会出现过拟合。对吧？所以你可能会想：“好吧，预训练阶段根本不需要正则化，对吧？我们只需放开优化器让它自由运行即可——目标就是最小化损失。”这确实是支持“无需正则化”的有力论据。但若观察实际做法，情况却相当复杂。事实上，这种复杂性甚至可能比最终呈现出来的还要高。而在早期阶段，研究者大量使用了Dropout。

## 段落 65

**英文**: And then there's a lot of weight decay that also seems to be happening. And these days, I think a lot of the people have stopped publishing details on precisely their training hyper parameters, but drop out has sort of gone out of fashion. But weight decay has really been something that a lot of people continue to do. And why is that? That's like a really odd thing to be doing. Right. So I'll give you a moment to just kind of think about this state of affairs. Right. If you're doing, you know, training a really large neural network for one pass on SGD, vast amounts of data, why would you use weight decay when you're doing that? Right. So maybe some of you know the answer, but I think that's a kind of interesting thing to think about. It's very intuition sort of violating, at least for me.

**中文**: 此外，还存在大量的权重衰减现象。如今，我认为许多研究者已不再公开其训练超参数的具体细节，但“丢弃法”（Dropout）似乎已不再流行，而权重衰减却仍是众多研究者持续采用的方法。那么，这又是为什么呢？这看起来确实是一件颇为奇怪的事情，对吧？我给大家一点时间，稍作思考，审视一下当前这种状况。试想，如果你正使用随机梯度下降（SGD）对一个规模庞大的神经网络仅进行单轮训练，且数据量极为庞大，此时为何还要采用权重衰减呢？或许在座各位中有人已知晓答案，但我觉得这个问题本身便十分耐人寻味——至少对我而言，它严重违背了直觉。

## 段落 66

**英文**: So, okay. So the reason is because, you know, it's not to control overfitting. In the sense that if you look at weight decay, different amounts of weight decay don't really seem to change the ratio of training loss to validation loss. Right. So you can train with different amounts of weight decay if you train for long enough for you, you know, you control your hyper parameters appropriately, you'll end up with the same train to Val-Wos gap. So overfitting, nothing's happening here, even with zero weight decay. But what is interesting is that the weight decay seems to be interacting, you know, someone in a strange way with the learning rate schedules of the optimizers. And so what's happening is that if you look at sort of a constant learning rate, so this is a model train on constant learning rate, and then, you know, you suddenly decrease the learning rate in ten years zero, so you see this drop off as you, you know, decrease the learning rate. And then let's look at different kinds of weight decay that you could do. And what happens is, you know, with weight decay, the model's not training very well at this high learning rate.

**中文**: 所以，好吧。原因在于——你知道，这并非为了控制过拟合。从权重衰减的角度来看，不同大小的权重衰减实际上并未明显改变训练损失与验证损失之间的比值。没错。因此，只要你训练时间足够长，并恰当地调节超参数，无论采用何种程度的权重衰减，最终得到的训练损失与验证损失之间的差距都是一样的。换言之，此处并未发生过拟合现象，即使权重衰减设为零也是如此。但有趣的是，权重衰减似乎以某种奇特的方式与优化器的学习率调度相互作用。具体而言，若采用恒定学习率进行模型训练，随后突然将学习率降至零（例如在第十年），你便会观察到性能骤降；接着我们再考察几种不同的权重衰减设置，结果发现：在高学习率下，加入权重衰减后模型训练效果并不理想。

## 段落 67

**英文**: And then when you decrease the learning rate, it will very rapidly drop off. And when you look at sort of cosine learning rate decay, what happens is that, you know, the models with high weight decay start out very slow, but then as they cool down, that is their learning rate decreases, they very rapidly optimize. And so there's some very complex sort of interaction happening here between the optimizer and the weight decay and some sort of implicit sort of acceleration that happens near the tail end of training that ends up giving you better models. And so the answer to the question I posed you is, you know, you don't weight decay because you want to regularize the model, which is kind of what it was defined for. You're weight decaying in order to get actually better training losses and you end up doing that because of the various learning dynamics at the tail end of training as you decrease your learning rates to zero. It's a very sort of very interesting and complex and in some ways, you know, troubling, you know, a thing to be doing with language models. But now you sort of see why, you know, if you look at the, a lot of the reports, you'll see we use weight decay. This is kind of why that ends up happening. Cool. Okay.

**中文**: 接着，当你降低学习率时，损失会迅速下降。而当你采用余弦学习率衰减策略时，会发现：高权重衰减的模型起初训练速度很慢，但随着“降温”（即学习率逐渐降低），其优化速度却会急剧加快。因此，在优化器、权重衰减以及训练末期出现的某种隐式加速机制之间，存在着相当复杂的相互作用，最终使模型性能得到提升。所以，针对我之前提出的问题，答案是：你施加权重衰减，并非主要为了正则化模型——尽管这原本是其设计初衷；你真正目的是为了获得更优的训练损失。之所以能达成这一效果，是因为在训练尾声阶段，当学习率逐步衰减至零时，各种学习动力学共同作用所致。这对语言模型而言，是一件非常有趣、复杂，甚至在某种程度上令人不安的操作。但现在你大概明白为何许多技术报告中都会注明“我们使用了权重衰减”——这正是背后的原因。很好，明白了。

## 段落 68

**英文**: So putting all that together. So there are certain things that I think are just kind of no brainer. So if you're picking various hyper parameters for your model, you don't really need to think too deeply about them in the sense that they've been validated and basically everyone else does them. And so this is things like, you know, the hidden size of the MLP, the head dimensions of your multi head attention, your aspect ratio, and your choice of regularization through weight decay, like all of those, there's fairly good, I think, consensus evidence of how to pick most of these hyper parameters. And those defaults roughly give you the kinds of things that we suggest in the assignment. So you can kind of follow along and they'll roughly give you something similar to this. Okay. Any questions about the hyper parameter piece? Yes. Is there a reason why I drop out like, gone out of the pan? That's a good question. I don't think I've seen the question was, why did drop out go out of fashion? I haven't quite seen a deep analysis of why drop out is or isn't helpful.

**中文**: 综上所述，有些事情在我看来是显而易见、无需多加思考的。例如，在为模型选择各类超参数时，你其实无需过度深究，因为这些参数已被广泛验证，且业界普遍采用。这类参数包括：MLP的隐藏层维度、多头注意力机制中每个头的维度、宽高比（aspect ratio），以及通过权重衰减实现的正则化方式等。对于如何设置这些超参数，目前已有相当充分的共识性证据。这些默认值大致就能得到我们在作业中所建议的结果，因此你只需参照执行，即可获得与示例相近的效果。  
好了，关于超参数部分，大家还有什么问题吗？  
有，请问为什么Dropout（随机失活）如今似乎不再流行了？  
这是个好问题。我理解你的问题是：为何Dropout逐渐失宠？目前我尚未见到对其是否有效进行过深入、系统的分析。

## 段落 69

**英文**: Like I haven't seen any result that for example shows that it helps for training loss. And as sort of this, you know, both this paper argues and logic with dictate, there's not really training overfitting issue with these models that can't even do one epoch over the training data. Yes. So the multilingual vocabularies actually contribute to performance in one language. So here, is that? Yeah. So the question was, do multilingual vocabularies contribute to the improving performance in one language? When you say one language, do multi-lingual or like, you know, larger vocabularies help performance in English? Is that the right question? Yeah. So I think in your high resource language, the impact is less. Right? So, you know, if you're only thinking about, you know, English language, language modeling, you can get away with smaller vocabularies. This much is kind of, you know, true. But the place where larger vocabularies is really helpful is when you're starting to get at, I want to say the tail of your distribution, but when you get the languages that are sort of more minority.

**中文**: 就像我尚未见到任何结果（例如）表明它有助于降低训练损失。而且，正如这篇论文所论证的以及逻辑所推导的那样，这类连一个训练周期都无法完成的模型，实际上并不存在训练过拟合问题。是的。因此，多语言词表确实能提升单一语言的性能。那么，这里的问题是：多语言词表是否有助于提升单一语言的性能？当你说“单一语言”时，是指多语言词表，还是说更大的词表能否提升英语等语言的性能？这才是正确的问题，对吗？是的。我认为，在高资源语言上，这种影响较小，对吧？也就是说，如果你只考虑英语等语言的语言建模任务，使用较小的词表就已足够，这在一定程度上是成立的。但更大的词表真正发挥作用的地方，是在处理分布尾部——即那些相对小众的语言时。

## 段落 70

**英文**: And one great example of this, if you look at any of the cohere announcements about their models or their tokenizers, they basically always argue that because of the way they have larger vocabularies in the way they train their tokenizer, non-English and like, low resources languages, they are packed into much fewer tokens. And so people using those pay much fewer, you know, much lower cost that inference time, right, which is a great benefit. Oh, yes, question. Plus, if Waitee doesn't have the significant impact on the VALLAW, why do we care about like the treating dynamics or the favorable off-stage dynamics? Right. Okay. So the question was, if it doesn't have an impact on VALLAW, why do we care about training dynamics? The goal is still, I want to get, you know, good training loss, right? This is the game that we're playing. And the surprising thing about WaiteeK is that somehow it gets us better training losses. Like, I think the intuitive thing that makes sense is, you do waiteeK, it gives you better VALLAWs, but that's not what happens. What it's getting you is better training losses, which are also the same as VALLAWs. Yes.

**中文**: 这方面的一个绝佳例证是：查阅 Cohere 关于其模型或分词器的任何公告，他们几乎总强调，由于其分词器采用了更大的词表以及独特的训练方式，非英语语言（尤其是低资源语言）能被压缩为更少的词元。因此，用户使用这些模型时，推理成本显著降低，这无疑是一大优势。哦，是的，有人提问。另外，如果 Waitee 对 VALLAW 并无显著影响，那我们为何还要关注训练动态或有利的离线阶段动态呢？对吧。好的，这个问题是：如果它不影响 VALLAW，我们为何还要关心训练动态？我们的目标始终是获得良好的训练损失，对吧？这正是我们所追求的目标。而 WaiteeK 的出人意料之处在于，它竟能帮助我们取得更优的训练损失。直观上，人们会认为采用 WaiteeK 能带来更好的 VALLAW，但事实并非如此；它真正带来的是更优的训练损失——而该损失本身与 VALLAW 是一致的。没错。

## 段落 71

**英文**: Are there differences in the architecture of hyperfiber-fanner choices people make, because they, where it's like multi-modal architectures, if they're just on its text? Yes. So the question was about multi-modal models. That is a great question. My survey of multi-modal models is very incomplete. What I can say is a lot of the academic and open work that I've seen. They do what you might call like shallow or like later fusion or relief region of the modalities. And the way that works is you kind of bolt the vision modality onto a existing language model. In those cases, the hyper parameter and architecture choices are fixed. One thing I will note, and I will talk about this in just a few slides, is that the multi-modal models pioneer some pretty interesting techniques in stabilizing language model training. And that's been a really big theme, and I'll talk a little bit about those.

**中文**: 人们在选择超光纤-扇形（hyperfiber-fanner）架构时是否存在差异？这种差异是否源于多模态架构——例如，当模型仅处理文本时？是的。这个问题涉及多模态模型，是个非常好的问题。我对多模态模型的调研还很不全面。就我目前所见的大量学术界及开源工作而言，它们大多采用所谓“浅层融合”或“晚期融合”，或仅在模态间进行局部区域对齐。其具体做法通常是将视觉模态“附加”到一个现成的语言模型上；在此类情况下，超参数与架构选择都是固定的。需要指出的一点是——我将在接下来几张幻灯片中专门讨论——多模态模型率先探索了一些颇具启发性的技术，用以稳定语言模型的训练过程。这已成为一个非常重要的研究主题，我稍后也会对此略作介绍。

## 段落 72

**英文**: So what is different is often when you bolt on this new kind of vision piece and you like retrain with that. That's a big shock to the model. And so you have to think carefully about how to stabilize that training process. And those innovations have actually see back into like pure text language model. Okay. Cool. So I went back through and I looked through all these new papers. And as I was trying to think about, what's been new in the last year and sort of what new architecture and related things have happened. Actually, the core architecture hasn't changed much. But I think the one thing that stood out as being very emphasized in a lot of the releases has been what I would call stability tricks.

**中文**: 因此，真正不同的往往在于：当你新增这类视觉模块并重新训练模型时，会对模型造成巨大冲击。因此，你必须仔细思考如何稳定这一训练过程。而这些创新实际上也反向影响到了纯文本语言模型。  
好的，很棒。于是，我重新梳理并通读了所有这些新论文，试图思考过去一年中有哪些新进展，以及在架构及相关领域出现了哪些新变化。事实上，核心架构并没有太大改变。但我认为，在众多发布成果中被高度强调的一点，就是我所称的“稳定性技巧”。

## 段落 73

**英文**: And so these are things where you would like to train your model in much more stable ways. And as you make bigger and bigger models, you train for longer and longer, these kinds of issues start to appear more and more. So I've taken this from the OMO2 paper. And actually that paper is a great sort of set of academic results on LLM training stability. And one thing they start with is kind of this figure. And you look at this blue curve over here. And you look at this, you know, L2 norm of the gradient graph. And this is terrifying graph to look at. Like, you know, your lost curve kind of seems to be behaving okay, but you've got some some bad spikes every now and then. And you open up your gradient.

**中文**: 因此，这些正是你希望以更加稳定的方式训练模型的问题所在。随着模型规模越来越大、训练时间越来越长，此类问题会愈发频繁地出现。我从《OMO2》论文中摘取了这一内容。实际上，该论文是一篇关于大语言模型（LLM）训练稳定性研究的优秀学术成果综述。论文开篇便展示了这样一幅图表：图中这条蓝色曲线表示梯度的L2范数变化趋势。这张图看起来令人不安——你的损失曲线看似表现尚可，但其间却时不时出现一些异常尖峰；而当你进一步检查梯度时……

## 段落 74

**英文**: And it's this horrible plot where you've got spikes everywhere where your norms are completely blowing up. And you know, if you're training models like this, you're going to have a really tough time getting it to converge reasonably. At some point, it's going to hit, you know, gradient norm explodes and like you can't do anything and your training is done, right? So you can't train any further. And so there's been a lot of emphasis basically trying to turn this blue curve into something that looks a lot like the orange curve. And of course this loss is higher, but ignore that fact because I think they just switch data sets in between these two training runs. But this orange curve, you know, has nice low gradient norms throughout. And that's really the kind of pod that you would much rather see. And so you might ask where do stability issues arise in transformers? And of course they can arise basically everywhere. But if you look at the kind of interventions that people are making, there's really one place that really stands out as the kind of problem child. And that's the softmaxes.

**中文**: 而这是一种极其糟糕的训练过程：梯度范数在各处都急剧飙升，完全失控。众所周知，若以这种方式训练模型，将极难使其合理收敛。在某个时刻，梯度范数必然爆炸——此时你束手无策，训练即告终止，无法继续推进。因此，学界投入了大量精力，致力于将这条蓝色曲线改造为更接近橙色曲线的形态。诚然，该曲线对应的损失值更高，但请忽略这一点，因为我认为这两轮训练所用的数据集本身已发生切换。而这条橙色曲线则在整个训练过程中始终保持较低且稳定的梯度范数，这正是我们更希望看到的理想状态。那么，人们或许会问：Transformer 中的稳定性问题究竟出现在何处？当然，理论上它可能出现在任何地方；但若观察研究者们实际采取的各类改进措施，便会发现一个尤为突出、堪称“问题儿童”的环节——即 softmax 层。

## 段落 75

**英文**: And it can be a problem because you're going to be taking exponentials and those can be numerically, you know, badly behaved. You're also dividing two numbers. And so you might have a division by zero. Right? So for many different reasons, this softmax piece is a part that, you know, you might have lots of issues with. And so actually one more thing I want to talk about. So where are the softmaxes in a transformer? Well, there's one at the very end. So you've got to be careful about that output softmax. And also there's softmaxes in your self attention. Right? So there's two softmaxes that we're going to think a little bit about. And for each one, I'm going to mention stability intervention that has, you know, generally seem to be effective.

**中文**: 而且这可能成为一个问题，因为你要进行指数运算，而这些运算在数值上可能表现得很差。同时，你还要进行两个数的除法运算，因此可能会出现除零错误，对吧？所以，由于诸多不同原因，这个Softmax部分往往容易引发各种问题。因此，我还想再谈一点：那么Transformer中的Softmax出现在哪里呢？首先，在模型最末端就有一个Softmax，因此你必须格外注意该输出层的Softmax；此外，自注意力机制中也包含Softmax。也就是说，我们主要需关注这两个Softmax。对于每个Softmax，我都会介绍一种已被普遍证实有效的稳定性优化方法。

## 段落 76

**英文**: So the first one is called the Z loss. And in my desire to cite a paper that's older, I've gone back to Devlin in 2014, where in a machine translation paper, you know, their goal was to try to, you know, make sure that this normalizer was near one. So if you look at P of X, that's the output softmax over here. The output softmaxes two terms, you exponentiate your logits and then you divide by the normalizer Z. Right? The Z is just summing up, you know, the values across all the vocab. And so if you want the Z of X, you want to train the network to have a Z of X close to one. Well, then you can, you know, rewrite your loss. And you can add a little second term here to try to force log of Z of XI to be close to zero. Right? So you're going to end up with a auxiliary loss term. That's alpha log squared Z of XI.

**中文**: 因此，第一个损失函数被称为Z损失。为了引用一篇更早的论文，我追溯到了德夫林（Devlin）2014年的一篇机器翻译论文；在该论文中，作者的目标是确保该归一化因子Z接近于1。具体而言，若观察P(X)，即此处输出的softmax概率分布，其由两部分构成：先对logits进行指数运算，再除以归一化因子Z。没错，Z即为词汇表中所有词元对应值的总和。因此，若希望Z(X)接近1，便可训练网络使Z(X)趋近于1。此时，可重写损失函数，在其中额外添加一项辅助项，以迫使log Z(Xᵢ)尽可能接近零。最终得到的辅助损失项即为α·[log Z(Xᵢ)]²。

## 段落 77

**英文**: Right? You can kind of see that derivation on the right here. And this is, you know, in some sense, what people often call the Z loss. I think, you know, Jacob Devlin and others did this for machine translation for totally different reasons than what it's used for today. But this was, I think the first instance of this in language modeling land was palm, who use this as they called it auxiliary loss of Z loss, 10 to the negative four log squared Z to basically encourage the softmax normalizer to behave nicely. And you can kind of reason through the behavior of this regularizer if it succeeds and it forces log of Z of X to always be zero, then the log and the exponent, the exponential cancels, and you've basically just got you of R of X. And that's a good place to be, right? That's a nice numerically stable operation. So all of these sort of problematic operations kind of go away. And so you can think of the softmax as being well-behaved when Z of X is close to one or log of Z is close to zero, right? And, you know, palm in some sense is very much a pioneer because they did this Z loss trick. And many others didn't really do it for a long time, or at least the ones that had open papers. But then there was a kind of sequence of papers that have done this.

**中文**: 对吧？您可以在右侧大致看到这一推导过程。从某种意义上说，这正是人们通常所称的“Z损失”。我想，雅各布·德夫林（Jacob Devlin）等人当初在机器翻译任务中引入该方法，其动机与如今的应用场景截然不同。但据我所知，在语言建模领域，首个采用该方法的模型是PaLM；他们将其称为“辅助Z损失”，具体形式为10⁻⁴ × (log Z)²，其主要目的是促使Softmax归一化因子Z表现得更加稳定。若您深入分析该正则项的作用机制：一旦它成功发挥作用，并强制使log Z(x)恒等于零，则log与指数运算相互抵消，最终结果便简化为u(R(x))。这正是我们期望的理想状态，对吧？因为此时运算在数值上极为稳定，所有这类潜在的数值问题也就随之消除了。因此，当Z(x)接近1或log Z接近0时，我们便可认为Softmax表现良好、行为稳健，对吧？从某种意义上讲，PaLM堪称先驱——正是他们率先采用了这种Z损失技巧；而此后很长一段时间内，其他研究者（至少那些公开发表论文的研究者）并未广泛采用该方法。但随后，陆续出现了一系列相关论文，开始跟进并应用这一技术。

## 段落 78

**英文**: By Q1, 2 is actually the earliest follow-up that I know of, and then DCLM and all Mo2 and now several others have basically picked up on Z loss. So it's a very nice convenient intervention for improving stability. And then the other trick that we see, so that was on how to stabilize the output softmax. But we've got another softmax we've got to deal with, right? The other softmax we have to deal with is in the attention operation. And so, you know, this is from Nvidia paper, I forgot to put the citation marker. But here, you know, this is a block diagram of how attention works. You know, you've got your layer norm at the beginning. You've got your QKVs. Ignore this for the moment. You might multiply your cues in your case.

**中文**: 到第一季度，2实际上是目前已知最早的后续措施，随后DCLM、所有Mo2以及目前的其他几种方法基本上均已采用Z损失。因此，这是一种非常便捷且有效的稳定性提升干预手段。接下来是另一个技巧：刚才我们讨论了如何稳定输出层的Softmax，但还有一个Softmax需要处理，对吧？这个需要处理的Softmax位于注意力机制中。如NVIDIA论文所述（我忘记添加引用标记了），此处展示的是注意力机制运作的框图：开头是层归一化（LayerNorm），接着是Q、K、V向量；暂时忽略这部分内容；在您的情况下，您可能需要将查询（Q）向量进行相乘。

## 段落 79

**英文**: You'll softmax it, you multiply the V, and then you'll project it, and then that's going to give you your fully connected and your output, right? So if you ignore this little piece over here, you know, this looks just like your normal multi-head attention operation. So what's kind of the difference here? So several folks came up with this idea or this approach called the QK norm where you take the queries and the keys and you pass them through a layer, a normal layer before you, you know, take their inner product for the softmax operation. And this is a, you know, very different kind of approach to controlling the behavior of the softmax. Here, you're not controlling the normalizer Z. Instead, you're controlling the inputs, the softmax, to be kind of bounded in size, and that's going to naturally control the bad behaviors of the softmax. And as I said before, this is originally an innovation from the vision and sort of multi-modal model community. DeGani in 2023, this was, you know, paper on training very large vision transformers, and then chameleon and edifice from hugging face, sort of use these tricks for their like multi-modal training components. And then, you know, it got picked up by several others like M2, DCLM, OMO2, all basically uses this kind of techniques in order to stabilize their training. And I think I'm allowed to add one joke per lecture. And so this is the one I'm going to go with here.

**中文**: 你会对它进行Softmax操作，再乘以V，然后进行投影，最终得到全连接层输出和最终输出，对吧？因此，如果你忽略此处这个小模块，你会发现这看起来就和标准的多头注意力机制完全一样。那么，这里的区别究竟在哪里呢？一些研究者提出了这种被称为“QK归一化（QK norm）”的思路或方法：即在对查询（Q）和键（K）计算内积以进行Softmax操作之前，先将它们分别通过一个常规的归一化层。这是一种与以往截然不同的Softmax行为调控方式——此处你并非调控Softmax的归一化因子Z，而是通过控制Softmax的输入，使其数值范围自然受限，从而自然而然地抑制Softmax的不良行为。如前所述，这一创新最初源自计算机视觉及多模态模型研究领域：DeGani等人于2023年发表了一篇关于训练超大规模视觉Transformer的论文；随后Hugging Face推出的Chameleon和Edifice模型，也在其多模态训练组件中采用了此类技巧；此后，M2、DCLM、OMO2等多个模型也纷纷借鉴并应用了这一技术，以提升训练稳定性。另外，按惯例，每节课我被允许讲一个笑话——而这就是我今天要讲的那个。

## 段落 80

**英文**: I think one of the things that really has stood out in terms of stability interventions has been just how strikingly effective layer norms are. So we've seen, you know, going from layer norms just in the pre part of the block to both the beginning and the end of the non-residual component. And now we've also thrown it into the Q and the K component. At least in terms of improving stability, layer norms have been shockingly effective without affecting performance too much. The last trick that I'll know, I think this one has been sort of not quite as frequently used, which is the soft cap, the, the logeats that go into the softmax. So the other approach that you can take, so QK norm is in some sense a very heavy handed intervention because we're going to operate over the entire vector. But one thing you could do is after you take the inner products for self attention, you could pass them through kind of like a soft maximum operation. So you can pass them through this equation over here. So you have your logeats as your input divided by the soft cap, multiply by the soft cap. What does that do?.

**中文**: 我认为，在稳定性干预措施中，尤为突出的一点是层归一化（LayerNorm）效果之显著。例如，我们已观察到，层归一化的应用范围已从仅置于每个模块前部，扩展至非残差分支的起始与末端；如今，我们甚至进一步将其引入Q（查询）和K（键）分支中。至少就提升训练稳定性而言，层归一化成效惊人，且对模型性能影响甚微。最后我要介绍的技巧——软截断（soft cap），即对输入Softmax的logits施加缩放。另一种可行方法是：QK归一化在某种意义上属于一种较为激进的干预手段，因其需在整个向量维度上进行操作；而另一种更温和的做法则是在自注意力机制中计算完Q与K的内积后，再将所得结果经由一种类似“软最大值”（soft maximum）的操作进行处理。具体而言，可将logits输入代入此处所示公式：以logits为输入，先除以软截断值，再乘以该软截断值。那么，这究竟起到什么作用？

## 段落 81

**英文**: Well, if your logeats start exceeding the soft cap by a lot, the 10H is going to clip them off to one. And so you're going to have a maximum value of soft cap over here. So this is going to control in some sense soft clipping of the logeats. And gamma 2, and I think, almost 2 also do this. It hasn't been, I think, quite as popular otherwise. And I think the other sort of evidence against this, the Nvidia folks that I mentioned earlier, did actually quite a few different sort of stability improving interventions. And what they find is, you know, you have your baseline model over here. This is the perplexity of the baseline model 11. 19. Soft capping makes it worse.

**中文**: 嗯，如果您的对数概率值大幅超过软上限，10H 就会将其截断为 1，因此此处的软上限即为最大值。这在某种意义上实现了对对数概率值的软截断控制。伽马 2（γ2）以及我认为几乎为 2 的参数也具有类似作用，但据我所知，它们并未因此而广受欢迎。此外，我认为反对这一做法的另一类证据来自前文提到的英伟达研究人员；他们实际上尝试了多种旨在提升稳定性的干预措施。结果发现：您在此处的基线模型困惑度为 11.19；而采用软截断反而使其性能变差。

## 段落 82

**英文**: QK norm actually makes it better because you can use more aggressive learning rates is going to push the optimizer further. Cool. Okay. So that's the end of sort of the stability improving intervention stuff. Does anyone have any questions? I think that's been kind of the new development over the last year. Yes. So for the QKV norm, like, understand that you're betraying you have the layer not being applied. And in first time, is the layer not still being kept? Yes. So the question was, at first time, do you still use the norm? And the answer is yes. Because the layer norm has kind of learned parameters.

**中文**: QK归一化实际上效果更好，因为你可以采用更激进的学习率，从而推动优化器走得更远。不错。好的，以上便是提升稳定性的干预措施部分。大家有什么问题吗？我认为这算是过去一年中的新进展。是的。那么关于QKV归一化，您理解的是：此时并未应用层归一化；而首次训练时，该层归一化是否仍然保留？是的。因此，问题在于：首次训练时是否仍使用归一化？答案是肯定的，因为层归一化本身包含已学习的参数。

## 段落 83

**英文**: Like the whole, you know, action of the layer norm is it takes an activation, normalizes it to unit, and then scales them to some size. If you take that out, that's a huge change to the model. It will have no idea what to do with those unnormalized activations. Okay. Cool. All right. So I have this last bit last few slides that I want to end with. If we go over then we can always push this into the MOE lecture. But I think we also have a lot of content next time because I have to cover deep seat V3. So the last thing I want to cover is variations on the attention heads.

**中文**: 就像整体而言，层归一化（LayerNorm）的作用是：接收一个激活值，将其归一化为单位向量，然后再按某个尺度进行缩放。如果去掉这一操作，将对模型造成巨大影响——模型将完全无法处理这些未经归一化的激活值。好的，很好。那么，我还有最后几张幻灯片想作为本讲的收尾。如果我们时间不够，这部分内容可以移至专家混合（MoE）专题讲座中讲解。不过，我觉得下节课内容也相当丰富，因为我还需要讲解DeepSeek V3。因此，本讲最后要介绍的是注意力头（attention heads）的各种变体。

## 段落 84

**英文**: So attention heads, I think, haven't had as much work done to them. But there have been a few, I think, important changes that you need to know about in order to understand the models that are being trained. So the one thing I'll talk about, the first thing I'll talk about is GQA and MQA. And these aren't really critical to kind of the training time behavior of the models. But they're very important in understanding the inference cost and inference behavior of the models. And because this is an important architecture change, I'll mention them here in addition to probably being mentioned by Percy in some of the inference lectures. The other thing that the kind of new development I'll mention is how the most recent models like llama four, if you've heard of it, supports supposedly 10 million tokens of context. How does it do that? Well, it does so by sort of messing with the attention pattern in very structured ways. And so I'll talk about that as well. So GQA and MQA, if you looked at some of the larger models, like the big llama models or others, you'll have heard or seen this term GQA or MQA.

**中文**: 因此，注意力头（attention heads）方面的研究工作相对较少。但据我所知，已有几项重要改进，你必须了解这些改进，才能理解当前正在训练的模型。首先我要介绍的是GQA（分组查询注意力）和MQA（多查询注意力）。这两者对模型的训练过程影响不大，但对于理解模型的推理成本和推理行为却至关重要。由于这是重要的架构变更，我在此处予以说明，此外佩西（Percy）可能也会在部分推理课程中提及。另一项我将介绍的最新进展是：像最近发布的Llama 4（如果你有所耳闻）这类模型，据称支持高达1000万token的上下文长度。它是如何实现这一点的呢？其方法是通过以高度结构化的方式调整注意力模式。我也会对此加以说明。关于GQA和MQA，如果你关注过一些较大的模型（例如大型Llama模型或其他类似模型），应该已经听说过或见过GQA或MQA这一术语。

## 段落 85

**英文**: And I'll talk through what that sort of means. So to set the stage, let's think about the compute that you need to do attention. So this is, once again, 224N slides here. You're going to take your XQ, your query, and your XK, and then you're going to form your big sort of quadratic attention matrix. And you can sort of walk through each of these matrix multiplies. And you can convince yourself that the total number of arithmetic operations is going to be B times N times D squared. So that's going to be B is the batch dimension, N is the sequence length, and D squared is going to be the hidden dimension squared. And you can ask about the total memory accesses, and this is going to be B times N times D, and this is going to be, for example, accessing just this matrix here, this XQ is going to be that size. And then the softmax is going to be B times H times N squared. And you can kind of convince yourself of that by just thinking about the size of the softmax matrix, which is going to be batch times number of heads times all of the different softmax activations that you have.

**中文**: 接下来我将解释其具体含义。首先，我们来分析执行注意力机制所需的计算量。此处再次以224N张幻灯片为例：你需要输入查询向量XQ和键向量XK，然后构建一个大型的二次型注意力矩阵。你可以逐项分析这些矩阵乘法运算，并可自行验证其总算术运算量为B×N×D²。其中，B代表批处理维度，N代表序列长度，D²则为隐藏层维度的平方。至于总内存访问量，则为B×N×D；例如，仅访问此处的XQ矩阵即需如此规模的内存访问。而Softmax操作的计算量则为B×H×N²；你只需考虑Softmax矩阵的尺寸（即批处理大小×注意力头数×所有Softmax激活值的数量），即可自行验证该结论。

## 段落 86

**英文**: So that's N squared of them, right? And you've got a projection, and you've got D squared projection operations of the very end over here. And so we can take the ratio of total memory accesses and arithmetic operations. And this is going to be something that will be very important in a couple lectures. This idea called arithmetic intensity. So we want our arithmetic intensity to be high. What that means is we want to be doing a lot of compute for every single memory access that we do. And this is going to be because memory accesses are very expensive on a GPU, relatively speaking, and compute is relatively cheap. And so in this batch computation that I'm showing you here, the arithmetic intensity, if you take the ratio of those two things, is going to be 1 over k plus 1 over bn inverse. And so this is going to mean that we can kind of keep our GPUs running. Because if we have sort of large number of heads and we have large back size and large sequence length, those are all going to be sort of good large numbers.

**中文**: 因此，这样的操作共有 N² 次，对吧？接着你执行一次投影运算，最后这里又执行了 D² 次投影操作。于是，我们可以计算总内存访问次数与算术运算次数的比值。这个比值在接下来几讲中将变得极为重要，它被称为“计算强度”（arithmetic intensity）。我们希望计算强度尽可能高，也就是说，希望每次内存访问都能支撑起大量计算。这是因为，相对而言，GPU 上的内存访问开销很高，而计算开销则相对低廉。而在本例所示的批处理计算中，若取上述两者的比值，其计算强度为 1/k + 1/(bn)⁻¹。这意味着我们能让 GPU 始终保持高效运行：因为当注意力头数较多、批量大小较大、序列长度较长时，这些参数都会成为较大的数值，从而有利于提升计算强度。

## 段落 87

**英文**: Of course, this is what happens at training time. So the issue is that in front time, we do not have these big chunky matrices to multiply together. And so that's going to really change the nature of the behavior of our algorithms. So when we're generating text, remember that we have to generate a token, and then the transformer has to read that token, and then it has to process it. And now we can get the next token distribution. And then we do the things auto-aggressively, one token at a time. And by doing this, we can't paralyze this generation process. We need to go step by step for every single new token. And when we do this, we're going to need to incrementally compute attention, an idea that people call the kvcache. And so what do you do? This is a lovely animation of a kvcache that's been explained.

**中文**: 当然，这是在训练阶段发生的情况。因此问题在于，在推理阶段，我们并没有这些大型的稠密矩阵来进行相乘运算。而这将从根本上改变我们算法的行为特征。因此，在生成文本时，请记住：我们必须先生成一个词元（token），然后Transformer需要读取该词元并对其进行处理，之后才能得到下一个词元的概率分布；接着我们以自回归方式逐个生成词元。通过这种方式，我们无法并行化这一生成过程——必须针对每一个新生成的词元逐步执行。而在此过程中，我们需要增量式地计算注意力机制，这一技术被人们称为“KV缓存”（kvcache）。那么具体该怎么做呢？下面是一段对KV缓存机制的精彩动画演示与讲解。

## 段落 88

**英文**: So if you can sort of look at this figure, what you're doing is you've got a query token, right?. A query token here is you've generated a new token, you're conditioning on it, and now you want to ask what sort of information should I look up in the past, past that query token, right? And your query tokens are shifting from one through n, because you're generating new tokens one at a time. You're building up this sort of key cache over here, where basically I'm building up all of the past tokens keys, right? And the past tokens keys don't change, because they only depend on things in the past. And so I'm incrementally as I generate tokens, building up all of these past keys, and each time I can compute one new element of q dot k, right? So the big attention matrix is going to be this lower triangular matrix. I'm computing one row at a time, and that row is exactly what's necessary to generate the next token. So this kvcache idea, if you've not seen this before, is this idea of saying I'm going to generate the keys and the v's incrementally as I go, as I generate each token, and I'm only going to compute qk that's absolutely necessary to do my operations. And so once again, you can go through and do sort of the various arithmetic components of how many flops do we do? What's the total number of memory accesses? And if you think about the kvcache, right, I'm only multiplying the absolute necessary keys and values, right? Since I'm saving all of the intermediate computations, I'm not wasting any sort of matrix or vector vector multiplies. So the total number of arithmetic operations remains exactly the same, b and v squared. But the memory access patterns are now different. Why is that? Because when I do this kvcaching thing, I'm going to have to move various kinds of parameters in and out of memory repeatedly.

**中文**: 因此，若您观察此图，您所进行的操作是：首先获得一个查询标记（query token），对吗？此处的查询标记是指您新生成的一个标记，您将基于它进行条件化处理，然后询问：我应从该查询标记之前的过往信息中检索哪些内容？您的查询标记依次为第1个至第n个，因为您正逐个生成新标记。与此同时，您在此处逐步构建一个“键缓存”（key cache），即累积所有过往标记所对应的键（keys）。由于这些过往标记的键仅依赖于其之前的内容，因此它们保持不变。于是，随着每个新标记的生成，我逐步累积所有过往的键；每次仅需计算一个新的q·k元素即可。因此，整个注意力矩阵将呈现为一个下三角矩阵：我每次仅计算其中一行，而该行恰好正是生成下一个标记所必需的部分。这种“KV缓存”（kvcache）思想——若您此前未曾接触过——即指：在逐个生成标记的过程中，同步增量式地生成键（keys）和值（values），且仅计算执行当前操作所绝对必需的q·k乘积。由此，您可再次逐一分析各项算术运算量（如浮点运算次数）、总内存访问次数等。考虑到KV缓存机制，我仅执行绝对必需的键与值的乘法运算；由于所有中间计算结果均被保存，因而不会浪费任何矩阵或向量-向量乘法操作。因此，算术运算总量严格保持为b×v²。但此时内存访问模式已发生变化。为何如此？因为在采用KV缓存时，我需反复将各类参数调入和调出内存。

## 段落 89

**英文**: Whenever I multiply with a key sort of k matrix, I'm going to have to put that into memory, right, in the multiply by k. And then I need to put that away, and I need to compute some activations. And so I'm repeatedly loading in different matrices, and that's going to give me a much higher total memory access of b squared d plus nd squared. And so when you take this ratio, now the arithmetic intensity is not so good. You're going to get n over d plus 1 over b inverse. And so if we sort of reason through this, okay, so if I want arithmetic intensity to be high, I want this thing inside to be very small. So I need really large batches, and I need n over d to be small. What does that mean? I need really short sequence lengths or really big model dimensions. And this n over d is really unfavorable, because I don't want a bigger model, and I don't want a shorter sequence length. And so this is the core, in some sense, inference cost trade off that people face.

**中文**: 每当我用一个大小为 $k$ 的键矩阵进行乘法运算时，就必须将该矩阵加载到内存中，对吧？即在“乘以 $k$”这一步骤中。接着我需要将其暂存起来，并计算某些激活值。因此，我需要反复加载不同的矩阵，从而导致总内存访问量高达 $b^2 d + n d^2$。这样一来，当计算该比值时，算术强度便不再理想，结果为 $\frac{n}{d} + \frac{1}{b}$ 的倒数（即 $\frac{1}{\frac{n}{d} + \frac{1}{b}}$）。我们稍作分析即可发现：若希望算术强度较高，则需使该分母项尽可能小。这意味着我们需要极大的批处理规模（batch size），同时要求 $\frac{n}{d}$ 尽可能小。这又意味着什么呢？即需要极短的序列长度，或极大的模型维度。而 $\frac{n}{d}$ 这一项实际上非常不利，因为我既不希望模型更大，也不希望序列长度更短。因此，这在某种意义上正是人们在推理阶段所面临的核心成本权衡问题。

## 段落 90

**英文**: You have this very bad memory access pattern, where you have this one term, n over d, that's kind of really killing you in terms of the throughput of your system. And so this motivates this thing called mqa, and the key idea here, hopefully you kind of see from this figure back here, that really the part that's really bad is the keys and the values. They have this kv-cash thing being built up, and there's memory moving in and out. So what you do is you can have multiple heads for the query, multiple query heads, but only one dimension or one head for the keys and values. This immensely simplifies things. Once you do this, now you're moving much less information for the keys and the v's. And so, you know, k and v is shared, but query has many heads, and so you still have multi head attention or multiple queries, but only single case and v. So that's what's called multi query attention. And now, when you do the same kind of arithmetic, we have fewer memory accesses, because we've shared the case and the v's, and the arithmetic intensity is much, much better behaved. And so we can increase things like, you know, we have, we've decreased the first term by a factor of n, so longer sequence lengths are now viable.

**中文**: 你存在这种非常糟糕的内存访问模式，其中有一个项“n除以d”严重拖累了系统的吞吐量。因此，这催生了一种名为MQA（多查询注意力）的技术。其核心思想——希望你能从后方这张图中直观看出——真正造成性能瓶颈的是键（key）和值（value）：它们需要不断构建KV缓存，并频繁进行内存读写。为此，你可以为查询（query）设置多个头（即多查询头），但键和值却仅使用单一维度或单个头。这一做法极大地简化了计算过程。实施之后，键与值所需传输的数据量大幅减少；换言之，键和值被共享，而查询仍保留多个头，从而在维持多头注意力（或多查询）机制的同时，仅需单组键与值。这便是所谓的“多查询注意力”。此时，若进行同样的运算分析，由于键与值已被共享，内存访问次数显著降低，算术强度（arithmetic intensity）也得到极大改善。因此，我们可以提升相关性能指标：例如，第一项已减小至原来的1/n，使得更长的序列长度如今成为可行。

## 段落 91

**英文**: And the second term is now divided by the number of heads. So this term is also not so terrible. So all the different terms are controlled now. And mqa can give you much better behaviors. GQa or group query attention basically changes this slightly. Instead of having, you know, single query, or sorry, multiple query and single key, you can reduce the number of keys by some multiple. And so this will let you trade off between kind of the inference time behaviors and the expressiveness of the model, because maybe going from multi head all the way to multi query is a little bit too aggressive. So some works show that gqa doesn't hurt, but multi head attention hurts. I'm not going to get into that. I'm just going to close off of this, this very last thing, which I think is a really interesting development in the last few months.

**中文**: 第二项现在除以头数，因此这一项也不那么糟糕了。目前所有不同的项都得到了有效控制。而MQA（多查询注意力）能带来更优的性能表现。GQA（分组查询注意力）或称组查询注意力，其基本思路是对上述机制稍作调整：不再采用单一查询（或更正为：多个查询与单个键），而是将键的数量按某个倍数进行缩减。这使得我们能够在推理时间性能与模型表达能力之间进行权衡，因为从多头注意力直接过渡到多查询注意力可能过于激进。一些研究结果表明，GQA不会损害模型性能，而多头注意力反而会带来负面影响。我在此不深入探讨该问题。最后，我想简要提一下最近几个月出现的一个非常有趣的新进展。

## 段落 92

**英文**: So back in 2019, OpenAI had this kind of cool paper basically arguing how to build longer attention models. And they were basically arguing, well, one way to do that is to come up with sort of sparse attention patterns. So instead of paying attention to all the sequence, I'm going to pay attention to, let's say, a local window at each sort of chunk. And then I can have sort of other sort of attention patterns that are like diagonals that help propagate information across. So you can build sparse or structured attention that trades off, you know, various kinds of expressiveness versus runtime. Gpk3 uses exactly these kinds of tricks when they originally released it to get larger attention windows. Sliding window attention is another variant of this idea where, you know, at each layer, you only pay attention to a small region around your current position. And this also is going to control the total amount of sort of resources that you need. The total amount of resources you need in order to do longer context. So your effective receptive field is now the local one times kind of the layers.

**中文**: 因此，早在2019年，OpenAI就发表了一篇颇具启发性的论文，主要探讨如何构建更长注意力范围的模型。该论文的核心观点是：实现这一目标的一种方法是设计某种稀疏注意力模式。也就是说，模型无需关注整个序列，而只需在每个分块内关注一个局部窗口；此外，还可引入其他类型的注意力模式（例如对角线式注意力），以促进信息在不同区域间的传播。由此可构建出稀疏或结构化的注意力机制，在表达能力与运行开销之间进行权衡。GPT-3在最初发布时便采用了这类技巧，以支持更长的注意力窗口。滑动窗口注意力则是该思路的另一种变体：即在每一层中，模型仅关注当前位置周围的一个小范围区域。这种方法同样有助于控制处理更长上下文所需的总体资源开销。此时，模型的有效感受野即为局部窗口大小乘以网络层数。

## 段落 93

**英文**: The final trick, so those were kind of the older ideas, but the way that this has kind of been modern instantiation is some of the recent papers like Lawmma4 and Gema and cohere command a have now come up with this very clever trick of basically having transformer blocks where in this case, you have a set of four transformer blocks. The very bottom one uses full self attention with no position embedding. So there's no rope, no nothing. It doesn't know about position at all, but it's full self attention. And it only happens once every four blocks. And then the three blocks above it, you sliding window attention with rope. And so this is actually a really clever trick to both control the system's aspect of things because the full attention only happens every now and then. And also the length extrapolation aspect because rope only deals with local context windows and anything that's really, really long range has no position embedding at all. So it could extrapolate very, very aggressively, right? Because you don't have to do this position extrapolation that you do with something like rope. So that's a really cool development that we've seen in the last couple months.

**中文**: 最后一种技巧——以上这些都属于较早的思路，而当前这一思路的最新实现方式则体现在近期的一些论文中，例如Llama-4、Gemma以及Cohere Command系列模型。它们提出了一种极为巧妙的方法：在Transformer架构中采用一组四个Transformer块。其中最底层的块使用完整的自注意力机制，且不引入任何位置编码（即既无RoPE，也无其他形式的位置嵌入），因此完全不具备位置感知能力；但该完整自注意力仅每四个块才执行一次。其上方的三个块则采用带RoPE的滑动窗口注意力机制。这种设计实际上非常精巧：一方面，由于完整注意力仅间歇性触发，因而能有效控制模型的计算开销与系统资源消耗；另一方面，它也显著改善了长度外推能力，因为RoPE仅作用于局部上下文窗口，而真正长距离的依赖关系则完全不依赖任何位置编码，从而可实现极为激进的长度外推——毕竟无需像RoPE那样进行位置编码的外推。这是过去几个月中我们所见证的一项极为出色的进展。

## 段落 94

**英文**: So, all right, I think we're coming up on time. Feel free to ask the questions about architecture hyper parameters. I'll be happy to answer questions after.

**中文**: 好的，时间差不多了。大家可随时就架构超参数提出问题，之后我很乐意为大家解答。

---

*共 94 个段落，933 句话*
