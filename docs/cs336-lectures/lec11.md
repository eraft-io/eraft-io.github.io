#  Lecture 11： Scaling laws 2

生成时间: 2026-03-08 22:38:38

---

## 段落 1

**英文**: Okay, so let's get started. Today's the second and last of the scaling laws lectures. And today it's going to be a little bit more of a case study in details oriented lecture. I'm going to cover two separate kinds of things. The first one is I'm going to go through a couple of papers where people have done careful scaling law studies as part of their model building. And I'm going to use that as a way to convey to you how modern, large language model builders use scaling laws as part of their design process. So motivation from last time and today is what's the best practice for scaling a large model. We want to have large language models with nice hyper parameters and good architecture choices. And I've already told you about chinchilla and using scaling laws to validate some of this. But I think you should have rightfully skeptical questions about scaling laws.

**中文**: 好的，我们开始吧。今天是关于缩放定律的第二讲，也是最后一讲。今天的课程将更偏向于细节导向的案例研究。我将涵盖两类内容：第一类，我将介绍几篇论文，这些论文的作者在构建模型的过程中进行了严谨的缩放定律研究；我将借此向大家说明，现代大语言模型的构建者如何在设计过程中运用缩放定律。上一讲及本讲的出发点都是：如何对大型模型进行最优缩放？我们希望构建出超参数设置合理、架构选择优良的大语言模型。此前我已介绍过“奇琴伊察”（Chinchilla）研究，以及如何利用缩放定律来验证部分结论。但你们对缩放定律持合理的质疑态度，这完全正确。

## 段落 2

**英文**: Like it's curve fitting on a log log plot. Is it really as good as, you know, I said it was last lecture. So this chinchilla's approach to scaling laws actually work. You're finding this out in your assignments. You know, if you fit an ISO flop, is that really telling you about the right token trade-offs? Can you use this stuff to really set optimal learning rates?. And should we be picking sort of particular architecture as a prime risations to scale nicely? So the last paper or the newest paper we talked about with like lots of detailed scaling studies in last lecture was the deep mind chinchilla paper. And after that, chat GPT happened and kind of the competitive landscape of large language model building really changed. And people just stopped publishing anything about data and scaling and all these things, right? It was sort of very secretive. I've talked to people at some of the frontier labs before and asked them, oh, you know, like what are you guys doing for scaling? And they're like, no, you will not tell you anything about what we do for scaling. And so we have to sort of rely on other sources for how scaling happens in practice.

**中文**: 就像在对数-对数坐标图上进行曲线拟合一样。它真的有我上节课所说的那么好么？事实上，这种“金丝熊”（Chinchilla）方法的缩放定律确实有效，你们在作业中会发现这一点。例如，如果你拟合一个等FLOP曲线，它真的能告诉你关于词元权衡的正确信息吗？你能利用这些方法真正设定最优学习率吗？我们是否应选择某种特定架构作为首选，使其能够良好地扩展？上节课我们讨论的最后一篇、也是最新的一篇论文，即DeepMind的“金丝熊”论文，包含了大量详尽的缩放研究。此后，ChatGPT问世，大型语言模型构建领域的竞争格局发生了巨大变化，人们几乎完全停止了有关数据和缩放等方面的研究成果发布——整个过程变得高度保密。我此前曾与一些前沿实验室的研究人员交流，问他们：“你们在模型缩放方面具体做了些什么？”他们的回答往往是：“不，我们不会向你透露任何有关我们缩放策略的信息。”因此，我们不得不转而依赖其他渠道来了解实践中缩放的实际运作方式。

## 段落 3

**英文**: And there have been several, you know, competently executed large scale models that have done scaling. And so, you know, last year in this lecture, I covered 3D-BIS GPT, DeepSeek LLM, and MiniCPM. And as a nice side note, you know, last year I had to really strongly justify why I was covering these like Chinese models, so to speak. But this year, thankfully, hopefully you're all already excited to hear about DeepSeek rather than me trying to convince you that this is the right thing to listen to. In the year since then, I've looked at a lot of the models that have come out. And actually the hall in terms of new scaling law insights and papers is actually much sparser. I'll briefly mention some results from Lama 3, which came out at the later end of last year, Hunyuan Large, which is a MOE model from China. And then Minimax 01, which is a linear time sort of hybrid attention model from a long context model that came out this year. And all three of those have some scaling studies, but really nothing quite as extensive as DeepSeek or MiniCPM, which have really been the gold standard, I think, for modern scaling law studies. So that's one part of what I want to talk about today.

**中文**: 此外，已出现若干个执行得相当出色的大型模型，它们均开展了规模扩展研究。因此，各位可能还记得，去年我在本讲座中介绍了3D-BIS GPT、DeepSeek大语言模型以及MiniCPM。顺便提一句，去年我还不得不极力论证为何要介绍这些——姑且称之为“中国模型”的项目。但今年，令人欣慰的是，大家想必已迫不及待想了解DeepSeek，而无需我再费力说服各位：这确实是值得聆听的前沿成果。自那以来的一年中，我考察了大量新发布的模型。然而，就新缩放律洞见及相关论文而言，这一领域实际上反而显得冷清许多。我将简要提及三款模型的研究成果：去年年底发布的Llama 3、来自中国的混合专家（MoE）模型Hunyuan Large，以及今年推出的Minimax 01——一种面向长上下文建模的线性时间混合注意力模型。这三款模型虽均开展了缩放研究，但其深度与广度远不及DeepSeek或MiniCPM；后两者在我看来，堪称当代缩放律研究的黄金标准。以上便是我今天要探讨的第一部分内容。

## 段落 4

**英文**: I want to make sure you guys have an understanding of what scaling looks like in a real semi-production model. And the other thing I want to talk about, which is an important deep dive, I think, is the muP method that I mentioned last time. So muP is this approach, just as a recap of last lecture, is when we train these models, as we make them bigger, we need to change certain hyper parameters. On the left-hand side of this plot here, you see that as you make models wider, in this case, like an MLP, you make them wider, the optimum learning rate shifts downwards. So you need smaller learning rates for these bigger models. And that's a really big problem, potentially, because then you need the hyper parameter to your learning rates at the very large scale, right? And that's going to be very computationally expensive. It's going to be a huge problem. If on the other hand, if we could sort of parameterize our model differently, so that the learning rate that's optimal just stayed the same forever across all the scales, that's great. So we would really simplify our search process. We would like all of our hyper parameters and really choices in general to remain stable across scales.

**中文**: 我想确保大家对实际半生产模型中的缩放（scaling）效果有一个清晰的认识。此外，我还想深入探讨另一个重要话题——即上次提到的muP方法。作为对上一讲的回顾，muP是一种训练方法：当我们扩大模型规模时，需要相应调整某些超参数。在本图左侧可见，随着模型变宽（此处以多层感知机MLP为例），最优学习率会向下偏移，即更大规模的模型需要更小的学习率。这可能带来严重问题——因为这意味着我们必须在极大尺度下反复调优学习率这一超参数，计算开销极高，将成为巨大挑战。反之，倘若我们能以某种方式重新参数化模型，使得最优学习率在所有尺度下始终保持不变，那将非常理想。这样一来，我们的超参数搜索过程将大大简化；我们也希望所有超参数乃至整体设计选择，在不同规模下均保持稳定。

## 段落 5

**英文**: And muP is a very interesting class of approaches. And it teaches us some pretty interesting ways of thinking about the problem. So I'm going to actually go through some of the details in the math. In the years, last time I thought this, there were a couple of very nice tutorials on muP that came out, so I'm going to follow those, because they have math that's pretty easy to follow. And then I'll talk about some work that has come out doing sort of third party validation and evaluation of muP style methods. So, okay. The focus of the first part of this lecture, which is the case study, is going to be on three models. I talked about three additional more modern models, but actually the details in those are much more sparse. And I think the lessons you learn are primarily from these three papers here. So that's going to be my focus for the first part of this lecture.

**中文**: muP 是一类非常有趣的方法，它为我们思考该问题提供了若干颇具启发性的思路。因此，我将实际讲解其中一些数学细节。近年来，尤其是上次我思考这个问题时，出现了几篇关于 muP 的优秀教程，我将参照这些教程，因为其中的数学推导相当清晰易懂。随后，我将介绍一些近期开展的、针对 muP 类方法的第三方验证与评估工作。  
好的，本次讲座第一部分（即案例研究）的重点将放在三个模型上。我之前提到了另外三种更新颖的模型，但这些模型的具体细节要稀疏得多；我认为，从中获得的核心启示主要来自以下三篇论文。因此，这将是本次讲座第一部分的重点所在。

## 段落 6

**英文**: So I'm going to talk about cerebrist, GPT, mini-CPM, and deep-seek. And each one of these will have actually a pretty different mix of scaling strategies, and it will also have different things to teach us about how to get scaling right. So we'll get started. So cerebrist, GPT, is the first of the models and sort of scaling things that I want to talk about. It's a large family of models. It's trained, 0. 1 to 13 billion parameter models, trained with the chinchilla recipe. So roughly the same number of token to parameter accounts as is optimal. And they have an interesting core finding. The cerebrist folks actually are pretty interested in a lot of these scaling and parameterization studies.

**中文**: 因此，我将讨论Cerebrist、GPT、Mini-CPM和DeepSeek这四类模型。每一种模型实际上都采用了截然不同的缩放策略组合，同时也为我们提供了关于如何正确实现模型缩放的不同启示。下面我们正式开始。首先介绍的是Cerebrist和GPT——这是我将要探讨的第一批模型及相应的缩放方法。Cerebrist是一系列大型模型，参数规模从1亿到130亿不等，均采用Chinchilla训练范式进行训练，即训练所用的token数量与参数量之比大致处于理论最优水平。该研究团队得出了一个颇具启发性的核心结论：Cerebrist团队本身对诸多缩放规律与参数化研究抱有浓厚兴趣。

## 段落 7

**英文**: And they have a really interesting core finding, which is they scale up this muP thing that I mentioned before, and they find that it makes scaling a lot more stable and a lot more pleasant to deal with. And just to kind of show you the punchline, right, you've got test loss on the pile, and you've got sort of the scaling curves here of like the cerebrist GPT in blue. This is with standard parameterization. You've got muP in orange. This is the model that they also train. We're using the maximum update parameterization, and they show that it scales more nicely if not better than things like Pythia or GPTJ. So that's nice. And the thing that I want to emphasize here is that this is kind of one of the few, if not first, public validations of muP. We know that all of, or most of the labs that are doing LM scaling, pay close attention to how they parameterize their networks, their initializations as a function of the scale of the model, as well as things like per layer learning rates. Are things that people pay close attention to to make scaling much more stable? And so things like muP are pretty important in this space.

**中文**: 他们得出了一个非常有趣的核心发现：即他们对之前提到的muP方法进行了规模化扩展，结果发现该方法显著提升了模型缩放过程的稳定性与可操作性。为直观呈现其核心结论，图中横轴为The Pile数据集上的测试损失，纵轴为缩放曲线：蓝色曲线代表采用标准参数化方式训练的Cerebras GPT模型；橙色曲线则代表采用最大更新参数化（muP）方式训练的模型。结果显示，muP的缩放效果至少不逊于Pythia或GPT-J等模型，甚至更优。这一点颇为可喜。而此处我想特别强调的是：这或许是目前公开领域中为数不多、甚至可能是首个对muP方法的实证验证。我们清楚地知道，所有（或绝大多数）从事大语言模型缩放研究的实验室，均极为关注网络参数化策略——包括模型初始化方式如何随模型规模变化，以及各层学习率等超参数的设置——这些因素正是研究人员为提升缩放稳定性而重点把控的关键环节。因此，muP这类方法在该领域具有极其重要的意义。

## 段落 8

**英文**: While I'm a for, for example, the paper for that isn't out, and I don't know if they will be out, but they talk about a technique they call metapy, which is a variant of this as well. So what they show is that when they train models using sort of standard parameterization, they find that they have sort of big oscillations kind of around the predicted scaling point. So that's the, this dash line, you know, they have kind of oscillations. Do you have the fact that, for example, they have to adjust the learning rate as a function of scale. And so it's hard for them to kind of really get the predicted performance sort of exactly right, which is this dash line using their scaling recipe. On the other hand, what they find is, you know, if you have sort of the three-risk GPT, sorry, muP scaling, then you got this orange line, which is much, much closer to the scaling law fit for this muP version. And so their claim here, at least, is that using this alternative parameterization allows them to get much more predictable scaling, much more nice hyper parameter tuning. We're going to see this in more detail. I'll return to this slide again once I've sort of gone through the mathematical derivation of muP. But in case you're ever interested in implementing this thing, some of the cerebrist GPT folks in general, the kind of artifacts that the cerebrist research folks are putting out is very, very helpful for muP, because they have this big table in the appendix that really just tells you exactly the difference between the standard initialization and parameterization or SP.

**中文**: 虽然我支持该方法，但相关论文尚未发表，我也不确定其是否会正式发布。不过，他们提到了一种名为“MetaPy”的技术，这也是该方法的一种变体。他们的实验结果表明：当采用标准参数化方式训练模型时，模型性能在预测的缩放点附近会出现显著振荡（即图中这条虚线附近出现振荡）。例如，他们不得不根据模型规模动态调整学习率，因此很难严格复现缩放定律所预测的性能（即图中这条虚线所示的性能），即使采用他们提出的缩放方案也是如此。另一方面，他们发现：若采用三风险GPT（应为“muP”）缩放方式，则可得到图中这条橙色曲线，其与muP版本的缩放律拟合结果高度吻合。因此，他们至少声称：这种替代性参数化方式能带来更可预测的缩放行为，也使超参数调优更为简便。我们后续将对此展开更详细的分析。待我完成muP的数学推导后，会再次回到本页幻灯片。另外，若您有意实现该方法，需注意Cerebras GPT团队（通常指Cerebras研究院的研究人员）发布的各类技术资料对muP实践极具参考价值——其附录中提供了一张详尽的对照表，清晰列出了标准初始化与参数化（SP）和muP之间的具体差异。

## 段落 9

**英文**: And the maximum update version or muP, and you'll see that, you know, I'll just give you kind of the one-liner version, basically every non-embedding parameter is initialized with one over the width, and then the learning rates per layer are scaled down by one over kind of the width, right?. So the interesting difference from standard parameterization, even if you're already doing sort of one over width scaling on the initialization, is actually there's per layer learning rates that are different. And I'm going to get to that later. I'm going to do a full derivation of this result, but you can kind of see here this kind of like nice quick reference, and also if you want to implement this thing, this gives you very easy ways of implementing muP. Another interesting thing that we also see in some of the other scaling strategies is that you combine these strategies like muP, which makes hyper parameter selection stable, with very, very aggressive scaling. So what they do here is they scale down their experiments all the way down to 40 million parameters. They do extensive, extensive hyper parameter search on this proxy model. And then they scale things back up using sort of muP to try to keep hyper parameters as stable as possible. And so this is sort of what they see in their small scale hyper parameter search, each one of these dots is a model run, and there's sort of a hyper parameter associated with each one of these, and then they pick the minimum across these runs, giving them essentially their sort of hyper parameter grid. This is a very clean approach, hyper parameter selection.

**中文**: 最大更新版本（即muP），您会发现——我先简要概括一下：基本上，所有非嵌入参数均以“1/宽度”进行初始化，且各层的学习率则按“1/宽度”比例进行缩放。因此，与标准参数化方法相比，一个有趣的不同之处在于：即使您已在初始化阶段采用了“1/宽度”的缩放方式，muP仍进一步为每一层设置了不同的学习率。这一点我稍后会详细展开。我将完整推导这一结果，但此处已提供了一个简洁明了的速查参考；若您希望实现muP，该方案也提供了极为简便的实现路径。另一项我们在其他一些缩放策略中观察到的有趣现象是：将muP这类可提升超参数选择稳定性的策略，与极其激进的缩放方法相结合。具体而言，研究人员将实验模型全面缩减至仅含4000万参数，并在此代理模型上开展极为广泛、深入的超参数搜索；随后，再借助muP原理将模型规模重新放大，从而尽可能维持超参数的稳定性。下图展示了他们在小规模超参数搜索中所观察到的结果：每个点代表一次模型运行，每个点均对应一组超参数；他们从中选取所有运行结果中的最优值，由此构建出最终的超参数网格。这是一种极为清晰、高效的超参数选择方法。

## 段落 10

**英文**: It's unclear whether this level of aggressive scaling down is really what you want to do if you want to train these really, really large models. But this is kind of a one strategy that we see also in many CPM and deep sea, like training much smaller surrogate models, and then trying to figure out how to stably scale them back up, and that's going to be kind of a theme that we see throughout. And yeah, if folks have questions, please stop me. I think maybe I'll stop here for a moment in case anyone has questions for the three brisk GPT piece. Although maybe you'll be clear once I talk about the muP derivation later in this lecture. There's another paper I want to talk about, mini CPM, or another artifact, I guess. And for whatever reason, I think mini CPM hasn't been talked about quite as much, especially in sort of like Western academic circles. But at least for me, this was one of the first sort of releases or papers I saw coming out of like a Chinese research group where they had done some really cool in depth, you know, scaling and other kinds of research. It really felt like, you know, stuff coming out of the frontier, right? And to give you an overview of what they do, their goals here is they want to train, you know, relatively small language models, but use a lot of computer train really good small language models. That's their ostensible goal.

**中文**: 目前尚不清楚，若想训练这些真正、真正庞大的模型，这种激进的大幅缩减规模策略是否真的是你想要采取的方法。但这种策略在许多CPM（中文预训练模型）和DeepSea等工作中也可见一斑，即先训练规模小得多的代理模型，再设法稳定地将其扩展回目标规模——这将成为我们后续讨论的一个重要主题。  
好了，如果大家有任何问题，请随时打断我。我想在此稍作停顿，以便各位就前述三篇关于“Brisk GPT”的内容提问。不过，或许待我稍后在本讲座中讲解μP（微参数化）推导时，相关疑问便会自然澄清。  
我还想介绍另一篇论文——Mini-CPM，或称另一项研究成果。不知为何，Mini-CPM似乎尚未得到广泛关注，尤其在西方学术界。但至少对我而言，这是我最早接触到的由中国研究团队发布的成果之一，其中包含大量深入而精彩的缩放规律及其他相关研究，其水准确实令人感觉仿佛来自前沿阵地。  
为便于大家了解，他们工作的总体目标是：训练相对较小的语言模型，同时充分利用大量算力，以打造性能卓越的小型语言模型——这便是他们明确宣称的目标。

## 段落 11

**英文**: And in doing so, they do a lot of careful scaling computations. They also once again use muP to stabilize and simplify scaling when they sort of end up scaling these models, not in size, but in terms of the amount of data. And to try to convince you that, you know, this is a paper worth following, right? You know, at the time that they were trained, this was a remarkably good 1. 2 to 2. 4 B models. It beats out, you know, most of the 2B models that were out there. And it matched many of the modern 7B models, at least modern as of 2024 standards. I mean, now of course you've got even better 7B models, the arms race is fierce. But this should give you a sense that at least, you know, given the amount of compute and technology available, you know, back in mid 2024, this was actually really at the frontier and they did something right to get models of this quality. And so much like Siri, Brist, GPT, they do essentially, they have to have, you know, some strategy to get scaling right, right? So stepping back, right? You're going to do a really big model run.

**中文**: 在此过程中，他们进行了大量精细的缩放计算。此外，他们再次采用muP方法，在对模型进行缩放时（并非扩大模型规模，而是增加训练数据量）实现稳定化与简化。为向您说明这篇论文确实值得关注：在这些模型完成训练之时，其性能表现极为出色——参数量仅为12亿至24亿的模型，已超越当时市面上绝大多数20亿参数模型，并可媲美许多2024年标准下的现代70亿参数模型（当然，如今已有更优的70亿参数模型问世，模型性能竞赛异常激烈）。这至少表明，考虑到2024年年中可用的算力与技术条件，该研究实际上已处于前沿水平，其方法确有独到之处，方能训练出如此高质量的模型。因此，与Siri、Brist、GPT等系统类似，它们本质上都必须具备某种确保正确缩放的策略。综上所述，您将要开展一次规模庞大的模型训练。

## 段落 12

**英文**: What do you have to do? You have to pick hyper parameters. You have to make sure those hyper parameters scale nicely and then you scale up your model, right? So we can do the same thing as the 3BRIS GPT folks. We can try to pick hyper parameters at a small scale, hope that they sort of stay stable and then scale everything up. And the way to do that would be to use something like muP. And this has exactly the same kinds of strategy at play here. So for embedding, you don't really do anything. You just scale it by a constant. Whenever you have some sort of like residual connection like a MLP, you scale it by a square root of the number of layers. You initialize it by sort of fan and one over the base width. And then the learning rates are also scaled by the width of the model.

**中文**: 你需要做些什么？你需要选择超参数，确保这些超参数能良好地随规模缩放，然后扩大模型规模，对吧？因此，我们可以采用与3BRIS GPT团队相同的做法：先在小规模上选取超参数，期望它们大致保持稳定，再将整个模型放大。实现这一目标的方法之一是采用类似muP（微调参数化）的技术。此处所采用的策略完全相同：对于嵌入层，实际上无需特殊处理，仅需按一个常数进行缩放；对于包含残差连接的模块（如MLP），则按层数的平方根进行缩放；其初始化方式采用“fan-in”策略，并按基础宽度的倒数进行缩放；此外，学习率也需按模型宽度进行缩放。

## 段落 13

**英文**: We see basically the same strategy or the same kinds of scaling factors appear as the 3BRIS GPT case, right? And they also end up with very similar parameters as CVS GPT, the same kinds of scale embeddings, similar learning rates off by a factor of 2 or so. But generally you end up in similar places as these kinds of hyper parameters. And then what you do is once you have this, you're sort of relying on your optimum learning rates to remain stable. So you're just going to keep those roughly fixed. And we know that the aspect ratio is a pretty important thing. So we just fix that after figuring out what the right one is. And then you scale up the overall model size going all the way from 9 or 30m all the way to half or 1 billion parameter models. And so what they have is like a roughly 5x or maybe a little bit more compute savings going from the smallest models that they've got to the largest sort of pilot run models that they have. And now you can use this and then you can sort of figure out whether you have sort of optimal batch sizes as a function of scale. So you want to figure out be crit, which is the critical batch size.

**中文**: 我们基本上看到与3BRIS GPT案例中相同的策略或同类缩放因子，对吧？而且它们最终也得到与CVS GPT非常相似的超参数：同类的尺度嵌入（scale embeddings）、学习率也相近，仅相差约2倍左右。总体而言，这类超参数最终都会收敛到相似的取值范围。接着，一旦确定了这些设置，你便依赖最优学习率保持稳定，因此只需大致固定这些学习率即可。同时我们知道，长宽比（aspect ratio）是一个相当重要的因素，因此在确定了合适的长宽比后，就将其固定下来。随后，再将整个模型规模逐步扩大，从900万或3000万参数模型，一直扩展到五亿甚至十亿参数模型。结果是，相较于他们所采用的最小模型，其最大规模的试点运行模型可实现约5倍、甚至略高的计算量节省。利用这一规律，你便可进一步推导出随模型规模变化的最优批量大小（batch size），即确定所谓的“临界批量大小”（critical batch size，记为 \( B_{\text{crit}} \)）。

## 段落 14

**英文**: If you remember correctly, this is the critical batch sizes roughly the diminishing returns point. So as models get bigger, their losses get lower, as their loss gets lower, you can make use of bigger and bigger batch sizes. So the critical batch sizes roughly telling you for the given model size and scale that I'm operating at, what is what is appropriate global batch size for me to be training these models with. And so much like the cap on paper, they follow a similar kind of recipe, you know, the plots look different from the cap on paper, but the underlying strategy is kind of the same. What they're trying to figure out is what is the critical batch size for training or the optimum batch size in this case of for training different models and they're trying to find relatively predictable scaling relationships between the batch size and for example the data size or the loss size. And vertical columns here sort of represent a single training curve and then sort of the the quadratics are sort of being fitted to try to identify the minimum. So the red line here is the is the minimum across all these points as we go upwards and this is trying to tell us the optimum batch size for a particular choice of model size and data set size. And then at this point, you know, you can follow the same logic as the cap on paper for identifying the batch sizes. Basically, you reproduce the same kind of plot. If you remember the cap on paper and the critical back size discussion from two lectures ago, if not, you can kind of pull up the lecture slides.

**中文**: 如果你记得没错，这就是关键批量大小，大致对应于收益递减的拐点。因此，随着模型规模增大，其损失会降低；而损失越低，你就能使用越来越大的批量大小。因此，关键批量大小大致告诉你：在当前所采用的模型规模与尺度下，训练这些模型时合适的全局批量大小是多少。这与论文中提出的“CAP”（计算-精度权衡）上限类似，它们遵循相似的方法论——虽然图表外观与论文中的CAP图有所不同，但底层策略基本一致。它们试图确定不同模型训练过程中的关键批量大小（即本例中的最优批量大小），并寻找批量大小与数据集规模或损失值等指标之间相对可预测的缩放关系。此处的垂直列大致代表一条单独的训练曲线，而拟合的二次函数则用于识别最小值点。图中红线表示所有这些点中向上延伸时的最小值，旨在告诉我们：针对特定的模型规模和数据集规模，最优批量大小是多少。此后，你便可沿用论文中CAP方法的相同逻辑来确定批量大小——本质上是复现同样类型的图表。如果你还记得两节课前关于论文中CAP及关键批量大小的讨论，那就很好；如果记不清了，可以翻阅当时的课件。

## 段落 15

**英文**: You'll remember that basically the thing that's highly predictable is the loss that you're trying to train to the terminal loss and the batch size of like the critical batch size point. And so we see that once again much like in Kaplan, you see a log log linear relationship here between the target loss or the terminal loss and the batch size that you want. And so from this, you know, you can kind of figure out what batch size you're going to get, right? Because if you have a particular target scale, you can use scaling loss to figure out what is the loss that I expect to get. Once you know the loss that you expect to get, you can use that to back out what batch size you can kind of operate out, right? So there's a fairly clean trend, polynomial increase the batch size as lost decreases. Now batch sizes do sort of shift around as a function of target loss and thus compute. So we have to fit a scaling law for that guy. But we already did mu P. And so in theory, right, if the approach works at all, what we should now get is we should get that the optimum learning rate here is stable. So on this plot, we're seeing essentially different model sizes from sort of small models in the light colors to their biggest models in the dark colors. And you see them sort of varying different learning rates.

**中文**: 你会记得，基本上高度可预测的是你试图训练至的终态损失（terminal loss）以及所谓的“临界批量大小”（critical batch size）点。因此，我们再次看到，与Kaplan的研究类似，目标损失（或终态损失）与所需批量大小之间呈现出对数-对数线性关系。由此，你便能大致推算出将采用的批量大小：如果你设定了特定的目标规模，就可以利用损失缩放规律（scaling law）来估算预期达到的损失值；而一旦知道了预期损失值，便可反推出可实际采用的批量大小。因此，整体趋势相当清晰：随着损失减小，批量大小呈多项式增长。不过，批量大小确实会随目标损失及计算量的变化而有所浮动，因此我们需要为此拟合一条缩放律。但我们此前已完成了μP的拟合。因此理论上，只要该方法本身有效，我们现在就应观察到：此处最优学习率保持稳定。在本图中，横轴代表不同模型规模，浅色曲线对应较小模型，深色曲线对应最大模型；你可以看到它们在不同学习率下呈现相应变化。

## 段落 16

**英文**: The big models, they're only running for a little bit for compute reasons. So what you see is a fairly clear trend. And you know, once again, very consistent with some of the earlier results that we've seen in Kaplan at all, where you have a relatively wide basin and then sort of sharp increases as your model becomes very unstable, right? But the important thing here is that the minimum remains fixed across relatively large orders of magnitude, right? From your small model to the big model, the minimum, or at least tied with the minimum, is at the exact same point roughly 10 to the negative 2 learning rate. And so this is a nice sort of piece of evidence or some validation that properly scaling your model initialization and properly scaling your per layer learning rates allow you to avoid tuning learning rates over and over, or even fitting scaling laws on learning rates in order to try to predict what the optimal learning rate is. Okay, and then the final thing is, you know, you might want to figure out essentially model size to data trade off. If you're training small models, you're going to be probably overtraining your model. So at least you want to justify yourself while you're training on so many tokens. And so you might want to replicate something like the chinchilla analysis. So the mini CPM people had a really cool or nice innovation. Others have done similar things, but I think they were the first to really popularize this in the LM setting, especially in the context of chinchilla style scaling is kind of the following.

**中文**: 大型模型仅能短暂运行，这是出于计算资源的限制。因此，你所观察到的是一个相当清晰的趋势。而且，这再次与我们此前在Kaplan等人研究中看到的一些早期结果高度一致：即在模型规模相对适中时，存在一个较宽的性能“盆地”，而当模型变得极不稳定时，性能则会急剧下降。但此处的关键在于，该最优学习率在跨越多个数量级的模型规模范围内基本保持不变——从较小模型到大型模型，取得最低损失（或至少并列最低损失）的学习率始终稳定在约10⁻²附近。因此，这是一个有力的证据或验证：只要恰当地缩放模型初始化参数，并合理地调整各层的学习率，就能避免反复调优学习率，甚至无需拟合学习率的缩放定律来预测最优学习率。好的，最后一点是：你可能希望探究模型规模与训练数据量之间的权衡关系。若训练小型模型，很可能出现过拟合；因此，至少需要为使用如此大量token进行训练的做法提供合理依据。为此，你或许希望复现类似Chinchilla的分析。Mini-CPM团队提出了一项非常出色（或精巧）的创新；其他人也曾开展类似工作，但我认为他们是在大语言模型（LM）领域、尤其是在Chinchilla式缩放背景下，最早真正推广这一方法的团队。其核心思路大致如下：

## 段落 17

**英文**: So let's say I want to fit a chinchilla scaling law. When I do that, what do I need to do? Well, I need to vary the number of tokens and I need to vary model sizes, right? And so when I do that, I'm going to fix a model size and I'm going to train a model for longer and longer. It would be nice if I could sort of early stop and take the checkpoints of this model and have that be sort of the difference or changes to the data set size, right? Because earlier checkpoint, C less data, it would be nice if I could use a single run to collect all this sort of data scaling things. Unfortunately, what I'm showing here is that kind of the cosine learning rates for different data target are different, right? So if you have a very small amount of data, you have a cosine that goes up very quickly or a sort that goes up, the warm up is always the same, but a very fast cooldown, right? You train for a little bit and then you come down very quickly. If you have a lot of data, then you're going to very slowly come down to the end. And so your learning rates between a small data training run and a big data training will be different, right? This is a very, very key point, right? Lots of people get tripped up by this. You cannot use a single run of a cosine learning rate model and try to get early checkpoints and reason about data scaling behavior based on that, right? This is this bites people all the time. And so in order to avoid this, what you would normally need to do is you need to train a model from start to every single endpoint, right? So you have to train it to every single target. And so this kind of takes you to n squared runs, right? Like some of the runs are small, but you have to basically run lots of runs, each one with a target termination point rather than using a single run and collecting checkpoints. It feels kind of senseless that we have to do this.

**中文**: 那么，假设我想拟合一个“南美栗鼠”（chinchilla）缩放定律。此时我该怎么做呢？我需要调整训练所用的词元（token）数量，也需要调整模型规模，对吧？因此，我会先固定模型规模，然后让模型持续训练越来越长的时间。如果能在训练早期就提前终止，并保存各个检查点（checkpoint），再将这些检查点视作对应不同数据集规模的模型，那就再好不过了——因为较早的检查点对应较少的数据量。理想情况下，单次训练就能收集到所有关于数据规模扩展所需的数据。但遗憾的是，此处展示的结果表明：针对不同目标数据量所采用的余弦学习率调度曲线是各不相同的。例如，当数据量极小时，余弦学习率会迅速上升（或者说快速升温），虽然预热阶段（warm-up）时长恒定，但降温阶段却极为迅速——即训练一小段时间后，学习率便急速下降；而当数据量极大时，学习率则会在训练末期极其缓慢地下降。因此，小数据量训练与大数据量训练所采用的学习率曲线截然不同。这一点至关重要！许多人恰恰在此处犯错。你绝不能仅靠一次采用余弦学习率调度的训练过程，通过提取其早期检查点，就推断出数据规模扩展的行为规律——这种做法屡屡导致错误结论。为避免此类问题，常规做法是：对每个目标终点都从头开始单独训练一个模型，即必须为每一个目标数据量分别完成一次完整训练。这就导致训练次数呈平方级增长——尽管其中部分训练耗时较短，但你仍不得不执行大量独立训练，每次均需设定特定的终止点，而无法仅靠单次训练并采集多个检查点来替代。这种做法看似毫无必要，令人费解。

## 段落 18

**英文**: So the mini CPM folks popularize this idea of a WSD or warm up stable decay learning rate. And so this plot on the left really shows you, you know, what's going on here. Normally, what we would train with is something that looks like this cosine learning rate shown in yellow here, right? It goes up. There's a warm up period, usually very short to get to your full learning rate. And then there's a cosine that goes all the way down to kind of your termination point. And maybe you stay at your minimum learning rate. This is all, of course, optional. You might terminate here as well. You might go all the way to zero, right? And so cosine learning rate looks like this. And the issue here, of course, is that if I have a different target, the cosine is going to be totally different.

**中文**: 因此，小型CPM团队推广了WSD（预热-稳定-衰减）学习率这一理念。左侧的图表清晰地展示了其工作原理。通常，我们采用的学习率调度方式如图中黄色曲线所示——即余弦学习率：首先经历一个预热阶段（通常很短），使学习率迅速上升至设定的最大值；随后按余弦函数规律持续下降，直至训练终止点；最后可能维持在最低学习率水平。当然，所有这些步骤都是可选的：你也可以在此处直接终止训练，甚至将学习率降至零。而余弦学习率的形态即如此。此处的问题显而易见：若目标不同，该余弦曲线将完全改变。

## 段落 19

**英文**: So everything past the warm up can't be reused. Now, if you look at this new WSD, which is basically trapezoid learning rate, what it has is three phases. It's got a warm up phase. That's the same as a cosine. It's got a stable phase that's flat. And then it's got a decay phase that rapidly cools down the model down to its minimum learning rate. And of course, you can have variations of this. You can go up, down, and then stay stable at your minimum. You know, you can do any of these variations. But I think in general, the simplest form to think about is warm up stable decay terminate.

**中文**: 因此，所有热身阶段之后的部分都无法复用。现在，若观察这种新的WSD（本质上是梯形学习率），它包含三个阶段：首先是热身阶段，这与余弦学习率相同；其次是平稳阶段，学习率保持恒定；最后是衰减阶段，学习率迅速下降至最低值。当然，该方案还可有多种变体，例如先上升、再下降，最后在最低学习率处保持稳定——诸如此类的变体均可实现。但总体而言，最简单、最直观的形式便是“热身—平稳—衰减—终止”。

## 段落 20

**英文**: Why is this nice? This is nice because you can reuse the stable part. So the thing that you do is if you want to do chinchilla in almost one run, what you do is you sort of warm up. You have a stable run all the way to the end. And then you cool down. And then if you want to figure out, oh, how would my model have been if I use less data, you rewind the checkpoints. And then you do another cool down. Right. And now you've got an exact warm up stable decay learning rate shape without having done the training from the beginning. So this is a very nice thing. The fact that the stable part essentially is flat allows you to do chinchilla style scaling or data scaling in a single training run or for mostly the cost of single training on.

**中文**: 为什么这很好？这很好，是因为你可以复用稳定阶段。具体做法是：若想几乎一次性完成“奇尼拉”（Chinchilla）式训练，你首先进行预热，然后让学习率全程保持稳定直至结束，最后再进行冷却。接着，如果你想探究“若使用更少数据，我的模型性能会如何”，只需回退到之前的检查点，再执行一次冷却即可。如此一来，你便无需从头开始训练，就能精确复现预热—稳定—衰减的学习率曲线。这非常便利。而稳定阶段本质上是平坦的这一特性，使得你仅需单次训练（或近乎单次训练的成本），即可实现类似“奇尼拉”的模型缩放或数据量缩放。

## 段落 21

**英文**: And a lot of people now do this. Okay. So they work very well. Many CPM, I think popularize this. And, you know, I think a lot of people have since then adopted it. And we see a lot of WSD style schedules in many, many places. They you see curves that look kind of like this. You've got a cosine learning rate schedule. You'll see essentially relatively predictable smooth decay towards your terminal loss like this yellow line here. If you train with WSD, you'll see much, much funnier learning curves that look like, you know, the curves that I have here above them, the darker lines.

**中文**: 如今，许多人已采用这种方法。好的，因此该方法效果非常出色。我认为，许多CPM（可能是“大规模预训练模型”的缩写）推动了这一方法的普及。而且，据我所知，此后已有大量研究人员采用了它。我们在许多地方都看到了大量采用WSD（可能是“权重空间解耦”或其他特定技术）风格的学习率调度方案。你会看到类似这样的学习曲线：例如余弦学习率调度，其损失值会呈现出相对可预测、平滑地向最终损失值衰减的趋势，如图中这条黄色曲线所示；而若采用WSD进行训练，则会观察到有趣得多的学习曲线——即我上方所展示的那些更深色的曲线。

## 段落 22

**英文**: Right. So you've got your warm up phase, which doesn't really show up in this training curve. It's so short. Then you got your stable phase where it sort of goes down normally. And then as soon as you hit your decay phase, like the cool down part, your loss really rapidly drops off until you hit your sort of either zero or minimum learning point, at which point you've gotten your terminal loss. Right. So these losses may look very disturbing to you, but they are actually pretty normal when you're training with training with these kinds of sort of rapid cool down learning curves. And maybe the point to make here is every single token count. You see that the warm up stable decay curve, you know, the minimum point beats or matches the cosine learning rate. That's not always the case.

**中文**: 没错。因此，您首先有一个预热阶段，该阶段在这一训练曲线上几乎无法体现，因为它持续时间极短；接着是稳定阶段，此时损失值大致呈正常下降趋势；随后一旦进入衰减阶段（即“冷却”阶段），损失值便会迅速下降，直至达到零或最小学习点，此时即获得最终损失值。因此，这些损失值可能看起来令您十分不安，但若采用此类快速冷却式学习率曲线进行训练，这种现象实际上相当正常。此处需要强调的一点是：针对每个词元数量，您都会发现预热—稳定—衰减曲线的最低点优于或至少不逊于余弦学习率曲线——但这并非总是成立。

## 段落 23

**英文**: There can sometimes be cases where cosine works better WSD works better. But in general, I think a lot of the, I think things that people say here is that the two learning rates are roughly comparable, but WSD has the additional nice advantage that you don't have to worry about your termination point. You can repeatedly cool down to get checkpoints of different data counts. Cool. Okay. And then of course, there's other things that have appeared for trying to estimate chinchilla curves. Some folks on a collaboration of like you, dub formerly you, dub and Apple folks had this paper on estimating sort of the chinchilla penalty. That is when you keep adding more and more data, you know, how much worse is your loss. Then if you had, you know, scaled according to chinchilla, so you have your sort of teal line here, which is sort of m equals 20, you know, 20 tokens to parameters. And you can sort of think about, okay, what happens if I trained with 320 tokens to parameters? Well, then you have a separate parallel scaling line.

**中文**: 有时会出现余弦退火效果优于WSD，或WSD效果优于余弦退火的情况。但总体而言，我认为——这里许多人提到的观点是：这两种学习率调度方法的效果大致相当；而WSD还有一个额外的显著优势，即你无需担心终止点的选择——你可以反复降温，从而获得不同数据量对应的检查点。很好。好的。当然，目前还出现了其他用于估计“奇尼拉曲线”（Chinchilla curve）的方法。例如，你与前Dub团队及苹果公司研究人员合作发表了一篇论文，旨在估算所谓的“奇尼拉惩罚”（Chinchilla penalty），即持续增加训练数据量时，损失函数会恶化多少。若按奇尼拉法则进行缩放，图中这条青绿色直线即代表参数量与token数之比为20:1（即每个参数对应20个token）。那么，我们便可思考：若将训练比例设为320:1（即每个参数对应320个token），则会得到一条独立的、与之平行的缩放曲线。

## 段落 24

**英文**: And then you have another line, which is the circles, which is what if I trained with 640 or the darker one is there. And so the thing that they show is actually instead of doing the WSD style thing, another thing you could do. Is you could try to figure out, okay, how much does my model degrade as a function of sort of higher tokens to parameter ratios? Well, that turns out also to have a fairly predictable shape. And you can sort of extrapolate that based on sort of degradation at small training runs. I don't think I've seen bar scale training runs using this idea, but it's kind of an additional cool thing to know that essentially you could do chinchilla in almost one training run by sort of extrapolating the excess token penalty at a small scale as well. So, okay, going back to mini CPM, now we have the tools that we need. We have the WSD learning rate, which allows us to essentially do one training run and that one training run allows us to, you know, have both variations. Sorry, not that one training run allows us to vary data as we go along. And then we have multiple training runs for different model sizes. That gives us all that we need to do chinchilla analysis.

**中文**: 接着是另一条线，即圆形标记的曲线，代表使用640训练或更暗色（即更深）的情况。他们所展示的关键点在于：与其采用WSD式方法，还可采取另一种思路——尝试探究模型性能随“令牌数与参数量之比”升高而下降的程度。结果表明，这种性能退化趋势也呈现出相当可预测的形态，并且可根据小规模训练运行时的退化情况加以外推。据我所知，目前尚未有人利用这一思路开展大规模训练实验，但值得注意的是，原则上我们仅需一次小规模训练，即可通过外推“超额令牌惩罚”来近乎一次性地完成Chinchilla式分析，这无疑是一项额外有趣的新发现。  
好，回到Mini-CPM，现在我们已具备所需工具：一是WSD学习率，它使我们能仅通过单次训练运行，便在训练过程中灵活调整数据量；二是针对不同模型尺寸开展的多次训练运行。这两者结合，便足以支撑完整的Chinchilla分析。

## 段落 25

**英文**: And they use method one and method three. If you remember what those are, method one is you overlay all the learning curves and you take the lower envelope and the lower envelope of all the training curves is supposed to be roughly a power law. And then method three is you basically jointly fit this equation to you have here. You hypothesize this two variable scaling law and you kind of fit it to all the data that you have and kind of curve fitting style fashion. And then that allows you to solve for the optimum token to data ratio through that fit. So they do both of that. They do see for chinchilla method one fairly clear, although not perfectly linear trends that allow them to essentially go from compute to token ratios. And their primary approach that they use to justify a lot of their design decisions is the method three. It's the curve fitting. So the kind of contours that you see here is the curve that they fit.

**中文**: 他们同时采用了方法一和方法三。如果你还记得这两种方法的具体内容，方法一是将所有学习曲线叠加在一起，取其下包络线，而所有训练曲线的下包络线大致应符合幂律关系；方法三则是对上述方程进行联合拟合——你先假设存在一个双变量缩放律，再以曲线拟合的方式将其整体拟合到全部可用数据上。通过该拟合结果，便可求解出最优的“词元—数据”比例。他们两种方法均进行了尝试：对于Chinchilla模型，方法一展现出相当清晰（尽管并非完全线性）的趋势，使其能够基本实现从算力到词元—数据比例的推导；而他们用以支撑诸多设计决策的主要方法则是方法三，即曲线拟合。因此，此处所见的等高线即为其拟合所得的曲线。

## 段落 26

**英文**: The dots that they have here is the small scale runs that they did to fit the chinchilla parameters. And just to, you know, sort of justify what they do. They find very, very high token to parameter ratios. Like so high that I feel like this is an outlier that doesn't really agree very closely with most of the other literature. They argue that llama style architecture should all have a higher ratio because of, you know, improved data quality and improved model efficiency. But they're token to parameter ratio estimates are really, really high. 192 tokens per parameter, which I don't think I've seen anyone else derive. I think other people have done replications of chinchilla. I don't think anyone's ever really done or argued for 192 tokens to parameter. Regardless, you know, we have seen that recent models like llama three have significantly higher data to model ratios.

**中文**: 他们在此处标注的点，代表为拟合金丝雀（Chinchilla）参数而开展的小规模实验。其目的无非是为其研究方法提供某种合理性支撑。结果他们得出的“每参数对应词元数”（token-to-parameter ratio）极高，高到在我看来属于明显异常值，与现有绝大多数文献的结果相去甚远。他们辩称，由于数据质量提升和模型效率优化，所有类似Llama架构的模型都应具有更高的该比值。然而，他们所估算的每参数对应词元数实在过高——高达192，据我所知，尚无其他研究者得出过这一数值。尽管已有其他研究者对金丝雀研究进行过复现，但似乎从未有人真正推导出、更未主张过192这一每参数对应词元数。不过，无论怎样，我们确实已观察到，诸如Llama 3等近期模型的数据量与模型规模之比已显著提高。

## 段落 27

**英文**: We also don't really see diminishing returns like these models aren't way worse than the equivalent chinchilla scaled like llama to models. This kind of suggests that with careful optimization and careful tuning, we should be able to go far beyond like the 20 times model size rule of thumb. So if there's one thing you take away from this set of sort of, you know, these last two slides, maybe not necessarily that you should trust whatever scaling law fits that mini CPM bid. But rather that sort of the chinchilla analysis isn't really, you know, a strong constraint, right? Like 20 times model size is just the starting point. You should be, you know, feeling free to significantly increase that token to parameter ratio. Finally, the the curve fits that they get are generally pretty good looking. So this is the scaling lockers for essentially data and model size scaling and perplexities on code in English. They do have some really weird outliers that I don't really understand why they get these. But they're sort of fitted scaling laws are generally pretty good as they increase the amount of data on their relatively small model. So this is one example of a large scale training run scaling recipe.

**中文**: 我们实际上也并未观察到明显的收益递减现象，即这些模型的表现并未远逊于按Chinchilla法则缩放至与Llama相当规模的模型。这在一定程度上表明，通过精心优化和细致调优，我们完全有望大幅突破“模型参数量提升20倍”这一经验法则的限制。因此，若从最后这两张幻灯片中只记住一点，或许并非一定要盲目相信拟合Mini-CPM数据所得的任何缩放律；而更应认识到，Chinchilla分析本身并非一项严格约束——换言之，“20倍模型参数量”仅是一个起点，你完全可以大幅提高该“训练token数与参数量之比”。最后，他们所拟合的曲线整体效果普遍相当理想。此处展示的是针对代码（英文）任务的、关于数据量与模型规模缩放关系及其对应困惑度（perplexity）的缩放律。尽管其中存在若干异常离群点，其成因尚不明确，但总体而言，当他们在相对较小的模型上持续增加训练数据量时，所拟合出的缩放律仍表现良好。这便是一个大规模训练缩放方案的实例。

## 段落 28

**英文**: So I'll stop here. Things like WSD are probably new to you. So, you know, if you have any questions, please feel free to ask or any of the other bits including like the chinchilla replication and mu P and so on. Sure. You're going to be a generation of mu P in terms of visualizing the way of randomness. So the question was the main change in mu P was initialization. So there's two things that will happen when you derive and you implement mu P. One of them will be the initialization will change and the other thing will be that the learning rate changes. Or the learning rate changes per layer, right? And that is probably a more exotic object than many of you are used to. The initialization actually is not that different.

**中文**: 因此，我将在此停住。像WSD这样的概念对你们来说可能比较陌生。所以，如果你们有任何问题，请随时提问，包括关于Chinchilla复现、μP等内容。好的。你们将成为以可视化随机性方式来理解μP的一代人。刚才的问题是：μP的主要变化在于初始化。因此，当你推导并实现μP时，会出现两方面变化：一是初始化方式会改变；二是学习率会发生变化——或者说，各层的学习率会分别变化，对吧？而后者可能比你们许多人所熟悉的对象更为新奇。实际上，初始化方式的差异并不大。

## 段落 29

**英文**: If you're already using like a standard like like Kymings style initialization that's already one over the fan in one over the square root of the fan in, which is going to be already the right thing. Whereas the learning rate normally like unless you're doing something really exotic, you're using like a global constant learning rate, right? Everywhere. So that's going to be a big difference from what you're normally training with. So you can think of that as like the practical difference for a lot of the mu P implementations. Yes. The learning rate was constant because the current was very close to the cosine decay. Right? Right? Yes. So you're talking about this curve and you're saying like, oh, when when we're in the stable phase of WSD, like when we're up here, the curve remains pretty close. And like why is that? Well, it's kind of close, but also not really, right? Like if you look at this, this last curve over here, you know, there's a big gap before we enter the decay phase between cosine and WSD. And I think this is like one of the pretty interesting, you know, mysteries about deep learning optimizers.

**中文**: 如果你已经在使用类似Kaiming风格的初始化方法，即采用$1/\sqrt{\text{fan\_in}}$的缩放因子，那么这本身就已经是正确的做法了。而学习率通常——除非你采用某种非常特殊的策略——往往是一个全局恒定的学习率，对所有参数都一视同仁，对吧？因此，这与你平时训练时所用的方式存在很大差异。你可以将这一点视为许多muP（微参数化）实现中一个重要的实际区别。是的，学习率保持恒定，因为当前曲线与余弦衰减曲线非常接近，对吧？对。所以你提到的这条曲线，意思是：当我们在WSD（权重空间动态）的稳定阶段——也就是曲线处于较高位置的这一段时——该曲线仍能较好地贴近余弦衰减曲线。但为什么会这样呢？嗯，表面上看确实很接近，但实际上又并非完全一致，对吧？比如观察最右侧的这条曲线，你会发现，在进入衰减阶段之前，余弦衰减曲线与WSD曲线之间存在明显差距。我认为，这正是深度学习优化器领域中一个相当有趣、尚待破解的谜题。

## 段落 30

**英文**: Clearly you need a stable phase to kind of get you far from your initialization, but also the cooldown phase is what gets you most of your gains in your losses, right? If you don't cool down, this is a gigantic loss in your losses. So the cooldown is actually really critical. And you know, a lot of the gains from cosine versus here, like this relative gap, this is all from cooldown, right? And so a lot of the optimizer learning rate design is about this balance between like, how do I keep learning rates high to travel far from my initialization, but still have, you know, good decay on my learning rate to be able to un-neil my loss down to a very low value. So the other paper I want to talk about is deep seek. This is the original deep seek LLM paper from 2024. And in many ways, if you read the original deep seek LLM paper, you'll kind of, you can know that these are like very serious science people, you know, because they do a lot of very careful scaling oblations and they're really trying to get it right when they scale up. And so that's kind of an attitude that's shared amongst sort of the players that get scaling right. So, you know, they have seven and 67 B parameter models, you know, at the time, very high performance relative to, you know, LLM, which is really the primary competitor at the time. You know, at the time, I guess, you know, LLM at two and mistral were kind of the big players deep seek comes in and they're able to match the performance, not quite the flashy impact of deep seek V3 coming in and sort of matching open AIs GBD40, but, you know, for our first time, sort of attempt, this is a pretty remarkable result. So let's kind of dig in and try to understand, all right, like what did deep seek do that allowed them to go from essentially zero to, you know, at least for open source state of the art at the time, right.

**中文**: 显然，你需要一个稳定的训练阶段，以便让你远离初始状态；但同时，退火阶段（cooldown phase）才是你在损失函数上获得大部分收益的关键，对吧？如果不进行退火，你的损失将大幅上升。因此，退火阶段实际上至关重要。而余弦学习率调度相较于其他方法所展现出的诸多优势——比如这种相对性能差距——全部源于退火阶段，对吧？所以，优化器学习率的设计在很大程度上正是围绕这一权衡展开：如何保持较高的学习率以充分远离初始点，同时又确保学习率能合理衰减，从而将损失持续降低至极低水平。

接下来我想介绍的另一篇论文是 DeepSeek。这是 DeepSeek 大语言模型的原始论文，发表于 2024 年。从很多方面来看，如果你通读这篇原始论文，就会发现作者团队是一群极为严谨的科研人员，因为他们做了大量细致入微的缩放消融实验（scaling ablations），并竭尽全力确保模型在扩大规模时依然准确可靠。这种态度也正是那些真正掌握模型缩放技术的研究者所共有的。当时，他们推出了参数量分别为 70 亿和 670 亿的两个模型，在性能上远超同期主流大语言模型（LLM），而后者当时正是主要竞争对手。彼时，LLaMA-2 和 Mistral 是业界公认的两大主力模型；DeepSeek 横空出世后，成功实现了与之相当的性能表现——虽未像 DeepSeek-V3 那样惊艳亮相、直接对标 OpenAI 的 GPT-4o，但对于其首次尝试而言，已是一项极为卓越的成果。那么，我们不妨深入探究一下：DeepSeek 到底采取了哪些关键举措，才使其得以从近乎零基础起步，一跃成为当时开源领域最先进水平的代表？

## 段落 31

**英文**: So, and I think deep seek more than, you know, most other players, maybe the only comparable one being mini CPM is very, very open about a lot of the experiments they did and the approach they use to choose a lot of these hyper parameters. So immediately we see one difference between deep seek V1 and mini CPM and also Siri brist GPT, which is that they don't use any new P and they're going to directly try to estimate both the optimal back size and the optimum optimal learning race. So it's like a really direct method you might call it and requires kind of a strong belief in scaling laws. So what they do is they, you know, take two relatively small models and they have, they run a grid over different batch sizes, they run a grid over different learning rates and they get losses across this grid. They do the same thing at a larger scale and you know, you can get kind of the optimum batch size and learning rate, right. And so they're saying, all right, well, this is a pretty wide basin. So we don't maybe have to be, you know, too scared about messing this up. And so then what they do is they know that all right, this the choice of learning rate and batch size are both relatively forgiving, but we do want to get the order of magnitude of these things correct, right. So how do we get the order of magnitude of these things correct. Well, you know, what we're going to do is we're going to train a bunch of models with different amounts of, you know, non embedding flops and we're going to change essentially across a grid, the parameters that I had before, both the batch size and the learning rate.

**中文**: 因此，我认为深度求索（DeepSeek）在诸多方面比大多数其他参与者更为开放——或许唯一可与之相提并论的只有Mini-CPM——他们非常坦率地公开了大量实验细节以及用于选择众多超参数的方法。因此，我们立刻就能看出DeepSeek V1、Mini-CPM以及Siri-Brist GPT之间的一个显著差异：它们均未采用新型P（new P），而是直接尝试同时估计最优批量大小（batch size）和最优学习率（learning rate）。这种方法可谓极为直接，且需对缩放定律（scaling laws）抱有相当强的信心。具体而言，他们先选取两个相对较小的模型，在不同批量大小和不同学习率的组合网格上分别进行训练，并记录对应损失值；随后，再以更大规模重复这一过程。由此即可大致确定最优的批量大小与学习率。他们指出：“这一最优区域范围相当宽广，因此我们或许无需过度担忧参数设置稍有偏差。” 接着，他们进一步认识到：学习率与批量大小的选择虽相对宽容，但二者数量级的设定仍须基本准确。那么，如何确保其数量级正确呢？他们的做法是：训练一批具有不同非嵌入计算量（non-embedding FLOPs）的模型，并在前述网格中系统性地调整批量大小与学习率这两个参数。

## 段落 32

**英文**: And by varying these, you know, we're going to have the optimum batch size and the optimum learning rate, sorry, across these different scales, right. So you can imagine basically making these grids across many different flop scales and basically marking down a star for each one. Perhaps unsurprisingly, because it's the scaling law lectures, these things seem to kind of follow a scaling law line, at least for the batch size things seem more clear. And you can kind of fit a line to here and you can, you know, extrapolate out to the big models that you're going to train what your optimum batch sizes should kind of look like, right. They do the same thing with learning rate and they sort of fit this line and they say, oh, these are the two learning rates we're going to use. It might be because the points are being plotted on top of each other, but I find this line to be particularly not pretty like somewhat suspicious looking. I mean, I could have probably fit a horizontal line and that would have also looked okay. This one, I don't know, even as a scaling line fusiest. I'm not quite sure I would, you know, bet my life on this one to pick the learning rate, but they did and that's how they get the learning rate. Now, they also kind of follow best practices at the time.

**中文**: 通过调整这些参数，我们就能在不同计算量规模下分别获得最优的批量大小和最优的学习率，抱歉，我说的是“不同计算量规模下”，对吧？因此，你可以想象在多种不同的浮点运算量（FLOP）规模上绘制这样的网格，并在每个网格点上标出一个星号。或许并不令人意外——毕竟这是关于缩放定律的讲座——这些参数似乎大致遵循某种缩放定律曲线，尤其是批量大小的变化趋势看起来更为清晰。你可以在该曲线上拟合一条直线，并据此外推到你即将训练的大模型，从而大致判断其最优批量大小应为何值，对吧？他们对学习率也做了同样的处理：拟合出这样一条直线，并宣称：“哦，我们将采用这两个学习率。”不过，这可能是因为多个数据点恰好重叠在了一起；而在我看来，这条拟合线尤其不够美观，甚至显得有些可疑。我的意思是，我大概也能画一条水平线，结果看起来同样合理。至于这条线，说实话，哪怕作为一名坚信缩放定律的研究者，我也未必会笃信它——更别说拿它来决定学习率了；但他们的做法就是如此，最终便依此确定了学习率。此外，他们当时也遵循了当时公认的最佳实践。

## 段落 33

**英文**: They do a chinchilla style analysis and they use once again a WSD style learning rate where, you know, they're trying to essentially minimize the amount of repeated work that they do. They do kind of something a little bit weird or a little bit more non-standard where what they're doing is, you know, they do warm up, they do stable, and then they do two sets of decay steps decaying down to zero. So it's like two, you know, decay phases consisting of kind of like 10% plus 10%. And they sort of analyze different choices of that decay phase. And they kind of doesn't seem to matter very much, but generally speaking it, you know, it's about 20% of the total compute budget is going to be spent on that cool down phase. And so they also show once again that it matches cosine learning rates, but once again, the advantage here is that we can do chinchilla style analysis for very cheap. In contrast to the learning rate, sorry, learning rate fits. You know, chinchilla style analysis just fits really, really cleanly. I think this is a broad lesson. When you look at lots of people's scaling laws, I think the stuff on hyper parameters always looks a little noisy and tenuous.

**中文**: 他们采用“绒鼠式”（Chinchilla-style）分析方法，并再次使用一种类似词义消歧（WSD）风格的学习率调度策略——其核心目标是尽可能减少重复计算量。他们的做法略显特殊或不够常规：先进行预热（warm up），再进入稳定阶段（stable），随后分两个阶段逐步衰减学习率至零。这两个衰减阶段各自约占总训练步数的10%，合计约20%。他们还系统分析了不同衰减阶段长度的选择，结果表明该选择影响甚微；但总体而言，这一“冷却”（cool down）阶段大约消耗总计算预算的20%。此外，他们再次验证了该策略与余弦学习率调度效果相当；而此处的关键优势在于，“绒鼠式”分析成本极低。相比之下，学习率拟合（learning rate fits）则复杂得多。“绒鼠式”分析的适配性极佳、极为简洁。我认为这是一个普适性启示：当我们审视大量研究者的缩放定律（scaling laws）时，超参数相关部分往往显得噪声较大、结论较不稳固。

## 段落 34

**英文**: But the ISO Flops analysis from all the players, like always like very, very nice. And so this is, you know, replication of the chinchilla result. You see, you know, different compute scales. We see different quadratics. We draw a line through the bottom of the quadratics. We get, you know, exactly the kinds of sort of optimum, sorry, optimum Flops retoken and optimum token size as a function of training Flops. Right. So this gives us a very straightforward way of analyzing the token size to model size trade offs. And this allows them sort of do everything from scratch. Right.

**中文**: 但所有参与者提供的ISO Flops分析结果，一如既往地非常出色。因此，这实际上是对“金丝雀”（Chinchilla）研究结果的复现。如您所见，我们考察了不同的计算量规模，观察到了不同的二次曲线关系；我们通过这些二次曲线的底部拟合出一条直线，从而精确得出最优Flops重分词数（optimum Flops retoken）和最优词元尺寸（optimum token size）随训练Flops变化的规律。这为我们提供了一种极为直接的方法来分析词元尺寸与模型尺寸之间的权衡关系，并使研究人员得以从零开始全面开展相关工作。

## 段落 35

**英文**: Of course, I think, you know, the side commentary. I think it's really nice that they're kind of redoing a lot of this. So there's certainly cargo-culted chinchilla and just pick 20 tokens per parameter. But they said, no, like let's actually go and do the scaling law analysis. And like let's actually make sure that, you know, the token sizes are relatively appropriate for us. Okay. And then they have, you know, a fitted scaling law at the very end. This is in some ways not surprising because this is after they fix their scaling strategy. They do predictable scaling. They try to predict what happens on the 7B and the 67B models.

**中文**: 当然，我想，您知道，指的是旁白部分。我觉得他们重新梳理大量内容这一点确实很好。此前确实存在盲目模仿“伶鼬”（Chinchilla）模型的做法，机械地采用每参数对应20个词元（token）的策略。但他们明确表示：“不，我们要真正开展缩放定律（scaling law）分析，确保所选词元数量对我们而言相对合适。”最后，他们得出了一个拟合的缩放定律。从某种意义上说，这并不令人意外，因为这是在他们确定缩放策略之后得出的结果；他们实现了可预测的缩放，并尝试预测7B和67B模型上的表现。

## 段落 36

**英文**: You know, it's unsurprising in many ways, but very nice that they're able to extrapolate out from about 10 to the 20, the 10 to the 24 and actually nail the prediction on the basis of the scaling law. Right. So it's a very nice thing to be able to see that we can actually get predictive measures of model capabilities before we actually train them. So that's kind of the deep seek part. And you would have questions for kind of the deep seek strategy and kind of what they did and any of the other pieces. I think most of this, I think WSD was probably the newest thing that I've sort of mentioned today. The other thing that deep seek does is directly fitting a scaling law onto the optimum learning rate and batch sizes rather than using something like new P. Yes. Do they have a global learning? Yeah. So it's a big tuning that global.

**中文**: 你知道，从很多方面来看，这并不令人意外，但非常棒的是，他们能够基于缩放定律，将预测范围从约10²⁰外推至10²⁴，并且精准地实现预测。没错。因此，能够在实际训练模型之前就获得对其能力的可预测性度量，这确实非常有价值。这部分就属于深度求索（DeepSeek）的研究内容。你可能还想了解深度求索的战略思路、具体做法，以及其他相关细节。我认为，今天我所提及的内容中，“WSD”可能是最新颖的一项。此外，深度求索的另一项工作是直接针对最优学习率和批量大小拟合缩放定律，而非采用类似NewP之类的方法。是的，他们采用的是全局学习率。没错，这是一种大规模的全局调优。

## 段落 37

**英文**: Cool. Okay. Yeah. Once we know the problem. I think you're hard to do. Yeah. So the question was like do people redo this kind of analysis for you from your models? To be honest, I'm not actually sure. And I am beginning to think that a lot of people maybe don't exactly replicate some of this because we see that in the newer paper is just increasingly less scaling details. Like even from deep seek, for example, like deep seek V2 and then V3, we see a lot of emphasis on the new parts of each paper. Like so for deep seek V2, we see a lot of emphasis on like MLA and like the architectural improvements and the deep seek V3, we see a lot of the systems components being emphasized like the low bit training.

**中文**: 很好。好的。是的。一旦我们明确了问题，我觉得这确实很难做到。是的。所以问题是：人们是否会基于你们的模型重新进行这类分析？坦白说，我其实并不确定。而且我开始觉得，很多人可能并未完全复现其中的某些内容，因为我们发现，新发表的论文中关于扩展性（scaling）的细节正变得越来越少。例如，以深度求索（DeepSeek）系列为例，从DeepSeek V2到V3，我们看到每篇论文都愈发强调各自的新颖部分：在DeepSeek V2中，大量篇幅聚焦于多头潜在注意力（MLA）及架构层面的改进；而在DeepSeek V3中，则更多强调系统组件，例如低比特训练技术。

## 段落 38

**英文**: But we don't see for example in either of those any additional new scaling law studies. And so I think my guess is that there's not much new there. Like maybe they're replicating and just to make sure it works, but nothing new to report. And I think that that will kind of be captured in the next couple slides where I'm going to talk about scaling laws and papers and models from the last year or so. So I did a little brief survey, but actually there's nothing that is at the level of detail of either mini CPM or deep seek. Like those are really still I think the most detailed open studies into scaling that we have in 2025. Okay, so you know, llama three was probably one of the bigger model releases in the past years since I last thought this class. And they do have some pretty interesting scaling bits. For one, you know, just the question right now of like do people actually replicate these analyses once they've run them once? Well, kind of yes. Lama three, you know, redo the ice of flop style scaling.

**中文**: 但例如，在这两项研究中，我们并未看到任何额外的新型缩放定律研究。因此，我猜测其中并无太多新内容，或许只是在进行复现，以确保其有效性，但并无新的成果可报告。我认为接下来的几张幻灯片将涵盖这一情况，我将在其中讨论过去一年左右关于缩放定律、相关论文及模型的研究进展。我虽做了简要调研，但实际上，并无任何研究在细节深度上达到Mini-CPM或DeepSeek的水平；在我看来，截至2025年，这两项研究仍是目前最详尽的开源缩放定律研究。好的，众所周知，Llama 3可能是自上次讲授本课程以来这几年中最重要的模型发布之一，其缩放分析部分也颇具启发性。首先，当前一个关键问题是：人们在完成一次分析后，是否真的会复现这些分析？答案大致是肯定的——Llama 3团队便重新进行了类似FLOP风格的缩放分析。

## 段落 39

**英文**: Chinchilla scaling laws and they find, you know, roughly that the optimum ratio, if I if I got the calculation right is about 39 to one, right. And I do think this is interesting because you know, Chinchilla got the 20 to one parameter ratio. I think many of us have trained models at the Chinchilla ratio in our research and so on. You know, it's quite clear that the 20 isn't really that stable like other people that have fitting it have been getting generally slightly higher ratios than before. And that might point to things like improved algorithmic efficiency in sort of architectures that that learn better from data. It might mean something else like improved data quality. All those are kind of moving parts. So it's hard to know like what's leading to these slightly different ratios. The results seem fairly clear. The fits are relatively good and they do get a 40 to one ratio.

**中文**: “金吉拉”（Chinchilla）缩放定律指出，最优参数量与训练数据量之比约为39:1（若我的计算无误）。这一点颇有趣，因为“金吉拉”最初得出的是20:1的参数量与数据量之比。我认为，许多研究者在实际工作中已按这一20:1的比例训练模型。但显而易见的是，20:1这一比例并不十分稳定——其他研究人员在拟合时普遍得到略高于20:1的比值。这或许反映出模型架构在算法效率上的提升，使其能更高效地从数据中学习；也可能意味着数据质量有所提高。这些因素均属动态变量，因此很难确切判断究竟是何种因素导致了比值的细微差异。不过实验结果相当明确，拟合效果总体良好，最终得出的比值约为40:1。

## 段落 40

**英文**: The other thing which is close to the data scaling stuff that I mentioned, the early parts of my first scaling lecture. One of the interesting things that the Lama three folks do is they try to essentially correlate. Compute into NLLs like log loss and then correlate those NLLs back into downstream accuracies, right. And so the thinking that they're trying to do here is they would like to not really scale against log likelihood. That's not really a thing they truly care about, right. They care about improving, I don't know, benchmark numbers on MMLU or Lombata or whatever other benchmarks that they've decided to hill climb on. And so if that's the case, then what they're going to need is to have a conversion factor going from these NLLs per character, these perplexities or equivalent to perplexities, and then map them into accuracies. And so they've done some studies in Lama three essentially trying to relate these two, fitting sigmoids, showing that if you fit essentially these small models and you fit some Lama two models and you fit a sigmoid on the whole thing, then accurately predict the performance of Lama three 405B on the basis of those fits. It's interesting. I think they say that they use these kinds of ideas for data selection, but I think there's not that much details there.

**中文**: 另一点与我之前提到的数据缩放内容密切相关，即我第一堂缩放课程的前期部分。Llama 3 团队所做的一个有趣尝试是：他们试图将计算量（Compute）与负对数似然（NLL，即对数损失）建立关联，再将这些 NLL 值与下游任务准确率相关联。他们的思路是：并不真正以对数似然为优化目标——因为这并非他们真正关心的指标；他们真正关注的是提升某些基准测试的分数，例如 MMLU、Lombata 或其他他们选定用于梯度上升优化的基准。因此，在这种情况下，他们需要一个转换因子，将每字符的 NLL 值（或等效的困惑度值）映射为准确率。为此，Llama 3 团队开展了一些研究，尝试建立这两者之间的关联：通过拟合 Sigmoid 函数发现，若先在小型模型及部分 Llama 2 模型上拟合 Sigmoid 曲线，便能据此准确预测 Llama 3-405B 的性能。这很有意思。据称他们将此类方法用于数据筛选，但相关细节披露得并不多。

## 段落 41

**英文**: And it's unclear whether this is like a really core object when Lama three was being trained or whether this was kind of a side scaling thing that was like just of interest to the authors. Another recent work that has come out sort of yet another Chinese all that's nicely executed is a Hunyuan one. Hopefully I didn't really butcher the pronunciation there. They are training MOE's. And so because they're training MOE's, they want to kind of redo the chinchilla style analysis. They fit once again, they do isopflops analysis, they fit quadratics, they figure out the minimums, and then they're able to get a different token to parameter ratio. So they get a 96 to one data to active parameter ratio. These ratios are obviously going to be quite different because the training MOE's. There's lots of differences about the architectures. We don't really expect the same thing as chinchilla, right? And so we do actually see in various papers, essentially replications of chinchilla happen again and again, because a lot of these people are very interested in understanding like how far can I push the token to parameter size ratio.

**中文**: 目前尚不清楚，这一现象是在Llama-3训练过程中涉及的一个真正核心对象，还是仅属于作者出于兴趣而开展的一种辅助性缩放研究。另一项近期发布的、执行得颇为出色的中国研究成果是“混元”（Hunyuan）系列模型——希望我的发音没有严重失准。该团队正在训练混合专家（MoE）模型。正因如此，他们希望重新开展类似Chinchilla风格的分析：再次进行拟合，再次开展等计算量（iso-FLOPs）分析，再次拟合二次函数，再次确定最优值，最终得出一种不同的“数据量—活跃参数量”比例，即达到96:1的数据量与活跃参数量之比。由于训练的是MoE模型，且各类架构存在诸多差异，这些比例显然会显著不同；我们本就不应期待其结果与Chinchilla完全一致，对吧？因此，我们确实在多篇论文中反复看到Chinchilla式分析的复现，因为许多研究者都极为关注：数据量与参数量之比究竟能推进到何种程度？

## 段落 42

**英文**: We would like to stay on the higher end of that, right? Like have more data than parameters because then people will actually use our models or our models will be cheap to serve. So for all those reasons, people have been replicating chinchilla. I think this is one of the best replicated results in scaling in many ways. The actual 20 to one parameter ratio isn't the thing that consistently replicates. But the fact that you can do isopflops and fit the minimum and get these very predictable trade offs in flops to optimal, optimal, optimum parameters is quite clean and consistent in the replications. Okay, the last one, which is honestly a little bit more of an exotic scaling law over the last year, is Minimax 1, which came out pretty recently. So Minimax 1 is a kind of linear time or long context language model, really spy another sort of tiny startup. And their interest is, well, what we're going to do is we're going to take softmax attention, which is quadratic. And they have this thing called lightning attention, which is a kind of linear attention or linear, yeah, linear attention layer, which is linear time. And then, you know, they have a hybrid version of this model.

**中文**: 我们希望保持在该范围的高端，对吧？也就是说，让数据量多于参数量，这样用户才会真正使用我们的模型，或者我们的模型才能以较低成本部署。因此，出于所有这些原因，人们一直在复现“金丝雀”（Chinchilla）研究。我认为，就缩放规律而言，这或许是迄今为止被复现得最成功的结果之一。实际上，20:1 的数据-参数比并非始终能稳定复现；但通过等计算量（isoflops）分析拟合最优曲线，并由此得出计算量（FLOPs）与最优参数量之间极为可预测的权衡关系，这一结论在多次复现中表现得非常清晰且一致。  
最后一点——坦白说，这是过去一年中一种略显另类的缩放定律——即近期发布的“极小极大1号”（Minimax 1）。它是一种线性时间或长上下文语言模型，实则出自一家规模极小的初创公司。他们的研究兴趣在于：我们将采用计算复杂度为平方级的 Softmax 注意力机制，并提出一种名为“闪电注意力”（Lightning Attention）的技术，这是一种线性时间复杂度的注意力机制（即线性注意力层）；此外，他们还构建了该模型的一种混合版本。

## 段落 43

**英文**: And they want to figure out like, or how much cost am I paying in terms of the performance of the model going from softmax to linear to hybrid attention. So they do things like, they basically replicate method one for chinchilla, where they're looking at the lower envelope of the lost curves as they train. They look at essentially the implied optimal model size and the implied optimal token count as they go. And roughly the conclusion that they draw from this is that, you know, the lightning and the hybrid models, you know, roughly perform the same as the softmax attention. And thus, they can, they're, you know, okay to train long context models on the basis of these architectures. We've seen these kinds of plots occur very often in research papers. Like if you look at the, the mamba paper or the mamba tube paper or any, or the delta net paper or any of these other kinds of linear time complexity RNM papers, you'll see plots that look a lot like this where they say, oh, the full attention scaling and my linear attention scaling are basically the same as a function of compute. But this is, you know, I would say like a kind of a rare case of this same plot being produced almost at scale from a major sort of artifact release. Okay, so putting all that together, right, I know that was like a bunch of mini case studies that I went through fairly quickly. But I want to sort of step back and recap it a little bit, right? We've seen several common ingredients being used in these scaling recipes.

**中文**: 他们希望弄清楚：从Softmax注意力切换到线性注意力，再切换到混合注意力时，模型性能下降所对应的代价究竟有多大。因此，他们采取了类似的方法，例如在Chinchilla模型上复现方法一——即在训练过程中考察损失曲线的下包络线，并据此推断出隐含的最优模型规模与最优词元数量。他们由此得出的大致结论是：Lightning模型与混合注意力模型的性能大致与Softmax注意力相当；因此，基于这些架构来训练长上下文模型是可行的。我们在诸多研究论文中频繁见到此类图表：例如查阅Mamba论文、Mamba Tube论文、Delta Net论文，或任何其他具有线性时间复杂度的RNN类论文，都会看到非常相似的图表，其核心论点往往是：“全注意力机制的计算扩展性”与“我的线性注意力机制的计算扩展性”作为计算量的函数，二者基本一致。但需要指出的是，这种图表由一次大规模重要成果发布近乎完整地呈现出来，实属罕见。好了，综上所述——刚才我快速梳理了若干微型案例研究——现在我想稍作退步，对前述内容略作回顾与总结：我们已观察到，这些缩放策略中反复使用了若干共同的核心要素。

## 段落 44

**英文**: We've seen three recipes, mini CPM, and then the few new paper since. So three best GPT and mini CPM both use mu P as a way to make hyper parameters more stable across scale. And you know, they essentially mini CPM especially has a nice WSD schedule, which is a thing they popularize to be able to do chincholastile scaling. A series of this doesn't bother to replicate chinchol. Deepseek does a little bit different thing. They assume that most hyper parameters just don't change with scale, but they do a full scaling analysis on batch size and learning rate. And then they use the scaling losses away to figure out optimum scaling. You know, I've already noted that some of the scaling looks a little bit more suspicious than others, but really this is a way to at least get the order of magnitude hopefully right. They use isoflops analysis, you know, they replicate chinchol once again to figure out the model sizing and to make sure they're kind of in the right order of magnitude. Of the more recent releases, you know, llama three and hunywando isoflops analysis only llama three does a little bit more, but that's basically it.

**中文**: 我们已看到三种方法：Mini CPM，以及此后几篇新论文。其中，三种最优GPT模型和Mini CPM均采用μP（mu Parameterization）方法，以提升超参数在不同规模下的稳定性。尤其值得一提的是，Mini CPM引入了一种出色的WSD（Warmup-Scale-Decay）学习率调度策略，该策略由其推广开来，从而实现类似Chinchilla的缩放训练。而这一系列工作并未刻意复现Chinchilla。DeepSeek则采取了略有不同的做法：他们假设大多数超参数并不随模型规模变化，但对批量大小（batch size）和学习率（learning rate）进行了完整的缩放分析，并利用缩放损失（scaling loss）来确定最优缩放方案。此前我已指出，部分缩放规律看起来略显可疑，但至少这种方法有望确保数量级大致正确。他们还采用了等FLOPs（isoflops）分析法——再次复现Chinchilla，以确定模型尺寸，并确保其处于合理的数量级范围内。在近期发布的模型中，Llama 3与Hunyuan均采用了等FLOPs分析；其中Llama 3稍作拓展，但总体而言仅此而已。

## 段落 45

**英文**: And then minimax does the more interesting thing of basically justifying architecture choices through the lens of a scaling law. But we see generally speaking that there's a few different things that get replicated like chinchola and learning rate and batch size are really the things that people are really deeply concerned about when they're scaling models up. And they sort of do things like fix aspect ratio and just scale the total model size up. And that's generally the way that people handle a lot of the moving pieces of scaling up. Okay, any questions about the case studies pieces actually I'm going to stay here and just make sure I've covered any questions that people might have. Okay, cool. Okay, so the second and kind of last part of this lecture is going to be understanding new people hopefully through the case studies you've seen that essentially getting the learning rate right is one of the core concerns that people have. And also the vacs. But in general, I think we want to have scale and variant hyper parameters. And it is the case that, you know, our choice of initialization and our choice of per layer learning rates are essentially arbitrary right like there's no reason why we have to initialize them one way and not the other.

**中文**: 随后，Minimax 通过缩放定律的视角，更有趣地论证了架构选择的合理性。但总体而言，我们发现有几项要素被反复沿用，例如“Chinchilla”原则、学习率和批量大小——这些正是人们在扩大模型规模时尤为关注的核心问题。人们通常会固定长宽比（即模型各维度间的比例），仅单纯扩大模型的总规模，这基本上就是处理模型扩展中诸多可变因素的通用做法。  
好的，关于案例研究部分，大家有什么问题吗？实际上，我会继续留在此处，确保已解答各位可能提出的任何疑问。  
好的，很好。  
那么，本次讲座的第二部分，也是最后一部分，将聚焦于理解新范式。希望借助前述案例研究，大家已认识到：正确设定学习率是人们最核心的关注点之一；此外还有验证集（vacs）相关问题。但总体而言，我们认为超参数应具备尺度不变性（scale-invariant）。事实上，我们对初始化方式的选择、对各层学习率的设定，本质上都是任意的——换言之，并无充分理由要求必须采用某种特定初始化方式或某类特定分层学习率设定，而非其他方式。

## 段落 46

**英文**: And so if we could manipulate those sort of three variables to get scale and variance in our learning rates, that would just be really wonderful right like that would make our lives way easier and it would make you know small scale experiments much more possible. So you know also talk you know first through the the math of this like how it's derived what's the justification what are the core conceptual objects behind trying to make models scale predictably. And then I want to talk about a pretty pretty nice pre print by an independent researcher on basically just a bunch of oblations on mu p like what makes it break what what is it robust to does it work on a real you know transformer language model these kinds of questions are explored pretty well in this pre print that will talk about at the very end here. Okay, what is mu p anyway I feel like maybe I've jumped the gun for the last two lectures I've mentioned what this is without really giving you the core conceptual object that is based off of. On the other hand, I think I'm justified in doing this because I think most literature doesn't explain mu p that clearly either there's just like yeah just scale the initialization by one over the width and scale the per layer learning rate by one over the width that's mu p. But I think the ideas behind new p are pretty interesting and worth discussing because I think they speak to some core object that that occur in deep learning in general. So I'm going to be basing my slides off this pre print or paper if you're interested in kind of reading about mu p I would point you to this one I think this and another blog post called a practitioner guides to mu p I think are the two kind of readable. Descriptions of what the sort of this paradigm is okay so I'm going to base myself off this the math is for whatever reason not exactly the same across these different presentations I'll you know clarify that I'm basing the math off this one. So mu p is based off of this the following relatively simple ideas right so there's two things that we think should happen when we're training a neural network right so. You know when we scale a neural network we're going to make the in this case let's just say only the width the width of the network bigger right where I'm going to fix the layer size or the depth and I'm going to make the width bigger as we go.

**中文**: 因此，如果我们能调控上述三个变量，从而在学习率中实现规模缩放与方差控制，那将非常理想——这将极大简化我们的工作，并使小规模实验变得切实可行。接下来，我将首先从数学角度讲解这一方法：它是如何推导出来的？其理论依据是什么？支撑模型可预测缩放的核心概念对象又是什么？随后，我想介绍一篇由独立研究者发布的优质预印本论文，该文主要围绕μP（微参数化）展开了一系列深入探讨：例如，μP在何种情况下会失效？它对哪些因素具有鲁棒性？它能否在真实的Transformer语言模型上有效运行？诸如此类的问题，这篇预印本都进行了相当充分的探索，我们将在最后专门讨论它。

好的，那么μP究竟是什么？我感觉过去两讲中，我可能有些操之过急——在尚未清晰阐明其核心概念基础之前，就已多次提及这一术语。另一方面，我认为这种做法也情有可原，因为目前大多数文献对μP的解释同样不够清晰：往往只是简单地说，“将参数初始化按宽度的倒数缩放，同时将每层学习率也按宽度的倒数缩放，这就是μP”。但我认为，μP背后的思想其实颇为有趣，值得深入探讨，因为它触及了深度学习中普遍存在的某些核心现象。

因此，我的幻灯片内容将主要基于这篇预印本（或正式论文）。若您希望进一步了解μP，我推荐您阅读这篇论文；此外，另一篇题为《μP实践者指南》的博客文章，我认为这两者是目前关于这一范式最易读、最清晰的两份资料。我将主要依据这篇论文展开讲解；需要说明的是，不同资料中所呈现的数学表述略有差异，我将明确指出：此处所有数学推导均以该论文为准。

μP基于以下两个相对简洁的核心思想：当我们训练神经网络时，有两点是我们期望发生的。具体而言，当我们对网络进行缩放时——此处我们暂且仅考虑“宽度”（width）的缩放，即固定网络层数（深度）不变，仅增大每一层的神经元数量（宽度）——随着宽度不断增大，我们期望网络行为呈现出某种特定的变化规律。

## 段落 47

**英文**: Now if I do that as I make the width bigger I want the activations initialization to remain you know big theta of one right I want it to remain roughly constant you know bounded above and below by universal constant you know roughly constant as I make the width bigger it shouldn't blow up it shouldn't vanish. Seems like a pretty natural thing to want right you know on your activations to get too big this is per coordinate. Now the second assertion I want is that you know I'm an initialized model and I'm going to take a single gradient step and when I take that single gradient step I want to make sure that the change in activation should also be big theta of one right so both of these seems like very natural conditions right because if you violate these it's going to mean that you know as I make the models bigger either the initial activations will blow up or or vanish or after one gradient step my activations will either blow up or vanish right those are both bad bad conditions right. And as a note right I'm talking about individual activations like coordinates and so if you're thinking about norms right of of an entire vector of activations right that should look like you know big theta of square root of NL right because each one of these are going to be roughly independent so the norm is going to look like the square root of the width the number of elements in my with coordinate. So I can derive you know mu p from those two conditions so the first condition which is that I want my activation to remain stable imposes sort of constraints on the initialization right so so I'm going to walk you through a very very simple example right so I'm going to consider a deep linear network so this is h of L so this is the activations out layer little L and that's going to be a function of the. weight matrix at layer L and the activations from the previous layer no nonlinearities no fancy stuff right like just you know it's all square just forget all this complexities if you want complexities you can go read the pre print they'll explain in slightly handway returns why those things don't map now the initialization I'm going to pick a Gaussian initialization right so it's going to be zero centered it's going to be a rectangular size of the sizes that depend on the sizes of my activations and then I'm going to have one hyper parameter which is the noise scale of this matrix at this layer sorry there should be a little L on this sigma so now what can we say that I'm going to say that I'm going to say that I'm going to say that I'm going to have a small problem, so now what can we say well I want to understand the size of h of L you know at initialization so how can we do that well one thing we can do is we can consider sort of the limiting behavior of this system right I'm going to take the basically little n of L and little n of L minus one to infinity if I do that this W is going to concentrate it's a random Gaussian matrix if you remember your your random matrix theory actually that's not a prerequisite for the course but you know if you know some basic random matrix theory you know that the the operator norm of a get Gaussian matrix is going to roughly concentrate to this object right it's going to be sigma which is the noise scale times you know the square root of both of the coordinates at the moment and importantly you know you can write down roughly that this equivalence is true right so the activations that layer L the norm of that is going to be approximately equal to the operator norm of W L times the activation norm of h of L minus one right and this is roughly assuming that W of L is independent of h of L minus one which is true at the initialization so I think you can basically make that a right arrow if you'd like now I'm going to say I'm going to pick a particular choice of sigma which is going to be square root NL over square root NL minus one times this object you can simply think of it as this right hand side thing this is the exact form this is kind of the more static form that you can think of this as but really it's just one over the square root of the fan in of your layer times the minimum of one and sort of the aspect ratio of your your model in case your fan in is much larger than your fan out then then this sort of kicks in. So let's say that I pick this sigma what happens like roughly one over the square root of my fan in so now what happens I can plug this back into this formula the matrix concentration limit and also this approximation here and I can sort of inductively prove that every layer is going to have the right sort of activation size so let's just go through all the layers and assume that up until layer L minus one I have this property right so that's the inductive assumption at layer L minus one I have that my activation norm is square root of NL minus one is the. L minus one. So that's just an assumption now if this is true then I just plug all these in right so I plug in square root of NL minus one into this component into WL operator norm I plug in the limit and then for sigma I plug in this expression over here you see that this inverse cancels this and then you're going to get exactly that H of L the L to normal H of L is equal to square root of NL so this is the thing that we wanted because before remember we said we want to make sure that the activations remains big theta of one which means that the norm should be square root of NL so that's exactly what we get plus some lower order terms right so you know this is a fairly clear step by step argument that shows you what the right thing to do is for initializations I want to pick one over the square root of the fan in plus a small correction factor in order to make sure that my activations do not blow up at initialization right I'll pass here for a moment in case someone has questions I feel like this is actually maybe the first like real math that we've done in the class so maybe it's a bit of a context which for people I did not warn you that I was going to talk about a bit of math. Okay is this all relatively clear for people one over square root of fan in yes okay I'm going to assume that everyone's on board with one over square root of fan.

**中文**: 现在，如果我在增大网络宽度的同时进行上述操作，我希望激活值的初始化仍保持为Θ(1)量级——即大致恒定，上下界均由普适常数界定；也就是说，当宽度增大时，激活值既不应爆炸性增长，也不应趋近于零。这看起来是一个非常自然的要求，对吧？毕竟，就每个坐标分量而言，若激活值过大，显然会带来问题。  
其次，我提出的第二个要求是：在模型初始化后，当我执行单步梯度更新时，该步所引起的激活值变化量也应保持为Θ(1)量级。这两个条件都显得极为自然，因为一旦违反其中任一条件，就意味着：随着模型规模扩大，初始激活值将发生爆炸或消失；或者仅经一次梯度更新后，激活值同样会爆炸或消失——而这两种情形显然都是不可接受的。需要说明的是，此处讨论的是单个激活值（即各坐标分量）；若考虑整个激活向量的范数，则其量级应为Θ(√(Nₗ))，因为各分量大致独立，故范数近似等于宽度（即该层激活值维度Nₗ）的平方根。  

由此两个条件，我可推导出参数μₚ。第一个条件——即要求激活值在初始化阶段保持稳定——本质上对初始化方式施加了约束。下面我将通过一个极其简单的例子加以说明：考虑一个深度线性网络，记第ℓ层输出激活为h⁽ˡ⁾，其由第ℓ层权重矩阵W⁽ˡ⁾与前一层激活h⁽ˡ⁻¹⁾共同决定；此处无任何非线性变换，亦无其他复杂结构——纯粹是线性映射。若你关注复杂情形，可参阅预印本，其中以略带启发性的方式解释了为何这些复杂因素在此并不影响核心结论。  

我采用高斯初始化：均值为零，矩阵尺寸由前后层激活维度决定，并引入一个超参数——即该层权重矩阵的噪声尺度σ⁽ˡ⁾（注意此处σ应带下标ℓ）。  

接下来我们分析初始化状态下h⁽ˡ⁾的量级。一种可行思路是考察该系统的渐近行为：令第ℓ层及第ℓ−1层的神经元数量n⁽ˡ⁾与n⁽ˡ⁻¹⁾均趋于无穷大。此时，随机高斯矩阵W⁽ˡ⁾将呈现集中现象——熟悉随机矩阵理论者应知，其算子范数大致集中于σ⁽ˡ⁾乘以前后维度平方根之积；更重要的是，这一近似关系大致成立：即第ℓ层激活向量的范数近似等于W⁽ˡ⁾的算子范数乘以前一层激活向量h⁽ˡ⁻¹⁾的范数。该近似基于W⁽ˡ⁾与h⁽ˡ⁻¹⁾在初始化时相互独立的假设，因此可严格写作等号（或单向箭头）。  

现选取特定σ⁽ˡ⁾：即σ⁽ˡ⁾ = √(n⁽ˡ⁾) / √(n⁽ˡ⁻¹⁾)。你可直观理解为右侧表达式——这是精确形式；而更通用的静态形式则为：1/√(fan-in)，再乘以min{1, fan-in/fan-out的某种比例因子}；换言之，若某层fan-in远大于fan-out，则该比例因子开始起作用。  

假设我们按此方式选取σ⁽ˡ⁾（即约等于1/√(fan-in)），将其代入前述矩阵集中极限及近似公式，即可通过数学归纳法证明：每一层的激活值范数均能维持在理想量级。具体而言，假设至第ℓ−1层时该性质成立（即归纳假设）：h⁽ˡ⁻¹⁾的范数为√(n⁽ˡ⁻¹⁾)。若此假设成立，则将√(n⁽ˡ⁻¹⁾)代入W⁽ˡ⁾的算子范数表达式，同时代入前述σ⁽ˡ⁾表达式，可见分母中的√(n⁽ˡ⁻¹⁾)与分子中的√(n⁽ˡ⁻¹⁾)恰好抵消，最终得到h⁽ˡ⁾的范数严格等于√(n⁽ˡ⁾)。这正是我们所期望的结果——如前所述，为使激活值保持Θ(1)量级，其范数需为√(n⁽ˡ⁾)，而我们恰好获得该结果（含低阶修正项）。因此，这是一个清晰、严谨的逐步推导过程，揭示了正确初始化策略：应取1/√(fan-in)并辅以微小修正项，从而确保初始化阶段激活值不致爆炸。  

此处稍作停顿，以便大家提问。我意识到这可能是本课程中首次引入较严格的数学推导，或许部分同学尚未适应——此前我并未预告将涉及此类内容。  
好，大家对此是否基本理解？即1/√(fan-in)？好的。我默认各位已掌握该要点。

## 段落 48

**英文**: Okay so now we're going to derive the second part of mu P right so the first part of mu P was about initializations the second part of mu P is going to be about learning rates right and so how are you going to think about learning rates well to think about learning rates I'm going to look at the second condition the second condition a two which says when I take one gradient step past initialization one needs to happen is that my activation sorry my update size needs to remain constant it can't blow up it can't vanish. Okay so what does that mean so if I have an update of delta WL on the weights at layer L what where does that come from well that comes from let's say what I'm doing SGD that's going to come from this expression it's going to be a learning rate times L which is my loss the gradient of L which is my loss and then the activations transpose in the case that my batch sizes one this is a rank one object right this is a rank one update to delta of L right and because it's rank one object. So if I have an object one you know there's a nice easy expression the change of WL times the activation of the previous layer is equal to the norm of the change in WL and the operator norm of this thing times the L to norm of H of L minus one right now you know combine this with the fact that the change in activation at layer L is kind of this expression you can convince yourself that this is true you can write this out by by sort of figuring out what the actual final activation is at layer L after the update. And that in canceling out WL H of L which is a shared term across left and right then you'll get this expression you'll get that you know what is the update in H of L this is the object that we want to keep roughly square root of N of L right the norm of this object so let's look through each of these terms and look at what the magnitude of this is the first term here WL delta H of L minus one this you know we can assume is going to be controlled from the inductive assumption because this is exactly. The sort of the delta H of L that we have plus the condition a one argument condition a one basically says. So condition a one is going to say that delta of H of L minus one is going to be screw N L and then WL is going to maintain that norm. The more complicated parts is going to be these two arguments the second and third terms that we have here delta WL H of L minus one and delta WL delta H L minus one sorry that's quite the mouthful and they all have the same order of magnitude actually and the only thing that we need to really figure out is this expression here what is the product of the previous layers norm times the operator norm of delta WL right because we don't really know how big the update is going to be in the weight matrix W if we knew that all very straightforward stuff. Okay and so the remaining argument is actually relatively straightforward even though this is actually like a complicated jumble of things the intuition is actually very clear. The intuition for this is says okay what do I really need to figure out the one thing I really need to figure out is this expression here how much does the weight at layer L change right if I can figure that out then I can sort of derive all of the relevant quantities and solve for the learning rate that's at a high level that's our strategy. And so how can we possibly figure out after one gradient step how much delta WL moves right that's really the key question well there's an additional sort of sneaky assumption that then shows up here and the assumption is something like this if our learning is well behaved then after a single gradient step then the change in the loss delta of L this quantity has to also be big theta of one right and why is that well because we don't want to be able to see the same thing.

**中文**: 好的，现在我们将推导μP的第二部分。μP的第一部分涉及初始化，而第二部分则涉及学习率。那么，我们该如何思考学习率呢？为理解学习率，我将考察第二个条件——条件A2。该条件指出：在初始化之后执行一次梯度更新时，必须满足一个要求，即我的激活值（抱歉，此处应为“更新量”）大小需保持恒定——既不能爆炸性增长，也不能趋于消失。

那么，这具体意味着什么？假设第L层权重的更新量为δWL，它从何而来？以随机梯度下降（SGD）为例，该更新量来源于如下表达式：学习率η乘以损失函数L关于权重的梯度∇L，再乘以前一层激活值H(L−1)的转置（当批量大小为1时，该更新为秩一矩阵，即对δWL实施的是秩一更新）。

由于这是一个秩一对象，因此存在一个简洁明了的表达式：WL与δH(L−1)的乘积的范数，等于δWL的范数乘以该秩一矩阵的算子范数，再乘以H(L−1)的ℓ²范数。结合第L层激活值变化量δH(L)近似满足该表达式的事实，你可以自行验证其正确性——只需通过推导权重更新后第L层的实际最终激活值，并消去左右两侧共有的项WL·H(L−1)，即可得到该表达式。由此可知，δH(L)的更新量正是我们希望大致维持为√N(L)的量（即该向量的范数）。

接下来，我们逐项分析该表达式中各项的量级。第一项WL·δH(L−1)可通过归纳假设加以控制，因为这恰好对应于我们已知的δH(L)形式，且条件A1指出：δH(L−1)的量级为Θ(√N(L))，而WL则保持该范数不变。

更复杂的部分在于后两项：δWL·H(L−1)与δWL·δH(L−1)（抱歉，表述略显冗长）。实际上，这两项具有相同的量级；我们真正需要确定的，是前一层激活值范数与δWL算子范数之乘积——因为我们尚不清楚权重矩阵W的更新量δWL究竟有多大；若此量已知，则其余问题均迎刃而解。

尽管上述推导看似纷繁复杂，但剩余论证本身其实相对直接，其核心直觉却异常清晰：关键在于明确一点——我们真正需要求解的，正是此处所示表达式：第L层权重究竟变化了多少？一旦厘清此点，便可顺势推导出所有相关量，并最终解出所需的学习率。这便是我们整体策略的高层思路。

那么，如何才能确定单次梯度更新后δWL的具体变化量？这正是核心问题所在。此时，一个额外的、略带隐蔽性的假设便浮现出来：若学习过程表现良好，则单次梯度更新所引起的损失变化量δL，其量级必为Θ(1)。为何如此？因为否则我们将无法观测到任何有效变化。

## 段落 49

**英文**: Because we don't want the size of our losses the update the decrease in our losses to kind of blow up or go to zero as the width goes to infinity right we want essentially our improvement in losses to remain roughly the same order of magnitude no matter how big our models get that's a stronger assumption than what we've had before but assuming that's true then essentially we can say okay the change in the loss is kind of like multiplying the gradient with the change in the weights this left side is O of one we know how big this. Delta of L should look like so now we know how big this delta WL looks like now we can solve for the gradient size and once we have that we can plug that in here we know delta WL we know the gradient of L we know the size of H of L from condition a one and now we can solve for the learning rate and that's exactly what you got at the bottom here and the you know if you work through the arithmetic the final result that you get here is that the learning rate for SGD is equal to the fan out of the fan out over the fan in right so lots of steps involved and lots of like substitution and slightly sketchy big O notation being substituted into the equations here but once we do that we're going to end up with a very simple formula note that this is true for SGD and those of you that have kind of been paying attention and like staring at this equation are probably you know internally complaining you're like you have you have misled us because you know in a transformer well what's NL over NL minus one for like a MLP that actually is like a the four right because you've got a factor for between DFF and and D model right and so this thing doesn't really change just a constant in most models right unless you're aspect ratios are like dramatically changing through your network the reason why mu P is different from standard prime characterization is because this derivation is for SGD where the prime izations look very similar between mu P and SP if you do the exact same derivation for Adam you're going to find that actually you're going to get slightly different things which is that it's going to be one over the fan in rather than the fan out over the fan in. Okay so here's the recap I have sort of dragged you through hopefully willingly the derivation of the basic what people call the spectral conditions that define mu P but now I will give you the kind of one slide high level take away of that result right so. you know when we want to do something like new P if we are following the guidelines from before directly what we will end up with is the following blue box initialization you know you set yourself to one over the square root of fan in times a correction factor that's you know one if your fan is is smaller than your fan out but you know square root of the ratio otherwise and then this is a simple initialization for the scale of your Gaussian. For your learning rate if you're doing SGD then you set it to fan out or fan in but if you're doing Adam that's going to be slightly different it's going to be one over the fan in. Now in case you already sort of know the standard like kind of initialization and so on off top your head you know you can mentally compare what this looks like to the standard parameterization so in a standard parameterization if you're doing it right you should probably be already setting your goussion to initialize to one over the square root of fan in so that's good that's already perfectly set. But your learning rates are probably being set globally to a constant this is fine for SGD not so fine for Adam where the really big difference between sp and mu p comes in. So that brings us right back to kind of the serious gpp paper now we have all the context we need to understand all the operations they do. If you look once again at the column over here of mu p the embedding layer special it doesn't really do essentially like any scaling because embeddings are one hot so their norms don't scale linearly with the number of vocab elements but ignoring that basically you see that all the layers get scaled down by one over the width you know that's the initialization. And then the learning rate rules are scaled by one over the width as well right so this is once again the learning rate rule for Adam right so if you're using Adam that's exactly the right thing to do and that's also exactly what they do in syrus gpp syrus gpp so hopefully that's clear and hopefully this gives you a sense of you know both the interestingness of manipulating per layer learning rates to get more predictable scaling and also maybe an appreciation of this idea of trying to control.

**中文**: 因为我们不希望损失值的大小在模型宽度趋于无穷大时，其更新量（即损失的减小量）急剧放大或趋近于零；本质上，我们希望无论模型规模变得多大，损失值的改善幅度大致保持在同一数量级。这是一个比之前更强的假设；但若该假设成立，则我们基本上可以认为：损失的变化量近似等于梯度与权重变化量的乘积。左侧为 O(1) 量级，我们已知 ΔL 的量级应为何种形式，因此现在也能确定 ΔWₗ 的量级。接着，我们便可解出梯度的量级；一旦获得该量级，即可代入此处：我们已知 ΔWₗ、损失 L 的梯度、以及由条件 A1 所确定的 Hₗ 的量级，从而解出学习率——这正是底部所得结果。若你仔细推演其中算术过程，最终可得：SGD 的学习率等于“扇出数除以扇入数”。整个推导涉及大量步骤，包含诸多代换，且此处所用的大 O 记号略显粗略、直接代入方程中。但完成这些代换后，我们最终将得到一个极为简洁的公式。需注意，该公式仅适用于 SGD。而那些一直专注观察并凝视此公式的同学，内心可能已在质疑：“你误导了我们！”——因为在 Transformer 中，“Nₗ/Nₗ₋₁” 对于 MLP 实际上约等于 4，这是因为 DFF 与 D_model 之间存在一个固定比例因子；因此，在大多数模型中，该比值基本恒定，除非网络中各层的宽高比发生剧烈变化。μP 区别于标准参数化（SP）的根本原因在于：本推导针对 SGD；而在 SGD 下，μP 与 SP 的参数化形式非常相似。若对 Adam 采用完全相同的推导过程，则会发现结果略有不同：此时学习率应为“1 除以扇入数”，而非“扇出数除以扇入数”。

下面进行简要回顾：我已尽可能清晰地（也希望各位是欣然地）带领大家完成了所谓“谱条件”（spectral conditions）这一基础推导——该条件定义了 μP。现在，我将用一页幻灯片高度概括该结论的核心要点。

因此，当我们希望实现 μP 时，若严格遵循此前的指导原则，最终将得到如下蓝色框内所示的初始化方案：即高斯分布的标准差设为“1/√(扇入数)”再乘以一个校正因子——当扇入数小于扇出数时，该校正因子为 1；否则为“√(扇出数/扇入数)”。这是对高斯分布尺度的一种简单初始化方式。对于学习率：若采用 SGD，则设为“扇出数/扇入数”；若采用 Adam，则略有不同，应设为“1/扇入数”。

倘若你已熟记标准初始化方法等相关知识，便可凭直觉将上述方案与标准参数化（SP）作一比较。在标准参数化中，若操作正确，你很可能已将高斯初始化的标准差设为“1/√(扇入数)”——这一点已然完美。但你的学习率却很可能被全局设为一个常数：这对 SGD 尚可接受，对 Adam 却并不理想；而 SP 与 μP 的真正差异，恰恰就体现在此处。

由此，我们便自然回归到严肃的 GPP 论文。如今，我们已具备全部必要背景知识，足以理解其中所有操作。再次观察此处 μP 列：嵌入层（embedding layer）较为特殊，实际上几乎不进行任何缩放——因为嵌入向量为独热（one-hot）编码，其范数并不随词表规模线性增长。忽略该细节后，你基本可见：所有层的权重初始化均按“1/宽度”比例缩小；同时，学习率规则也按相同比例“1/宽度”进行缩放——这正是前述 Adam 的学习率规则。因此，若使用 Adam，这正是理应采取的做法；而 Syrus GPP 论文中的做法也完全一致。希望这一点已足够清晰，也希望它能让你既体会到逐层调节学习率以实现更可预测缩放的精妙之处，也对这种力图实施精准控制的理念有所感悟。

## 段落 50

**英文**: Activations and updates as a function of model with right like you know I'll pause for a moment there and just mention right that's like a very successful idea from physics right lots of physicists think about ideas like renormalization as I take limits of certain things I want things to remain stable I don't want them to not blow up or go to zero this is an exact application of that idea that's that's kind of an interesting use of that okay. Any questions about I don't know mupe derivation or syrus gpp or any other things yes. Yeah so so that is part of the subtlety the question was you know what's the architecture assumptions well I mean technically there's a even stronger assumption here oh there's even stronger assumption here which is that I'm assuming things are deep linear network right I'm just multiplying matrices repeatedly you know this is the kind of filliest network that you can have. Basically there are arguments for why adding nonlinearity's are fine there arguments for how you would take the same arguments and apply them to the attention layer there arguments for why much more complex things are needed for a gated linear unit so each one of those architecture pieces needs a careful analysis in order to have a you know corresponding object yes. So it's a like an end sub-multiply and L minus one is the input so for example if you have a MLP you're going to have a matrix multiply that takes you from D model dimension to four times D model like the DFF dimension right so that would give you a NL over NL minus one of four for example so all the different matrix shapes are giving you the NL and NL minus one. The fan in the fan out of a matrix yeah exactly yeah so the input and output dimensions are determining all these objects okay. Yeah, I was using those terms exchangeably but I should have been a little bit more clear. Oh yes. So the question was like since deep sea uses a global learning rate does that mean they don't have an order one update so you know all this argument is asymptotic right it's basically saying as I scale my with out to infinity things will kind of be big or small and I mean if you if you look at the mu P plot for example you do you do kind of see this right you see that the learning rates have to shift as the model gets larger in order to compensate for the fact that the updates are getting bigger and bigger right what's empirically you know been seen as if you do nail the learning rate you don't need mu P right it's not like mu P is necessary for you to train a good model it's really just an attempt to try to keep this shift as small as possible so you can use the same learning rate you know throughout scaling and if you go back to deep seek you know if you remember the scaling wall that I was you know being a slight change in the way that I was doing it. So you know being a slight peter for you'll see that you know they they too have learning rates that that go down as a function of scale in order to try to compensate for the fact that the bigger models are going to have bigger updates right and so you know to respond more directly to the question yes in the case of deep seek right as we scale the model up you know our activation updates will get bigger so we have to strengthen global learning rate where we should go global learning rate to compensate for that.

**中文**: 激活值与更新量作为模型的函数关系——正如您所知，我在此稍作停顿，特别指出：这实际上源自物理学的一个极为成功的思想。许多物理学家会思考诸如“重整化”（renormalization）这类概念：当对某些量取极限时，我们希望系统保持稳定，既不发散爆炸，也不坍缩为零。此处正是该思想的精确应用，这也是一种颇为有趣的运用方式。  
关于μP推导、Syrus GPP或其他相关内容，大家有什么问题吗？是的。  
是的，这正体现了其中的微妙之处。刚才的问题是：架构层面有哪些假设？从技术上讲，这里其实存在一个更强的假设——甚至更强的假设——即我们假定网络是深度线性网络：仅反复进行矩阵乘法运算。这本质上就是所能构想的最简化的网络形式。  
当然，已有论证表明引入非线性是可行的；也有论证说明如何将同类分析思路推广至注意力层；还有论证指出，对于门控线性单元（GLU）等更复杂的结构，则需更为精细的处理。因此，每一种架构组件都需要经过审慎分析，才能得到与其严格对应的理论对象。  
是的，这类似于“Nₗ”与“Nₗ₋₁”的比值，其中Nₗ₋₁代表输入维度。例如，在多层感知机（MLP）中，存在一个矩阵乘法操作，将维度从d_model映射至4×d_model（即前馈网络FFN的隐藏层维度），此时Nₗ/Nₗ₋₁即为4。因此，各类矩阵的形状共同决定了所有Nₗ与Nₗ₋₁的取值。  
矩阵的“入度”（fan-in）与“出度”（fan-out）——是的，完全正确。也就是说，输入与输出维度共同决定了所有这些量。  
是的，我之前将这些术语混用了，本应表述得更清晰一些。  
哦，是的。  
刚才的问题是：既然DeepSeek采用全局学习率，是否意味着其更新量并非O(1)量级？需要明确的是，全部相关论证本质上都是渐近性的：它本质上是在说，当我们将模型规模无限放大时，各项量级将相应地变大或变小。例如，若观察μP的学习率曲线图，您确实会发现：为补偿更新量随模型增大而持续增大的事实，学习率必须随之调整。经验上已观察到：只要恰当地设定学习率，便无需μP；μP并非训练优质模型的必要条件，它实质上只是一种尝试，旨在尽可能抑制这种随规模变化而产生的学习率偏移，从而允许在整个模型缩放过程中沿用同一学习率。若您回顾DeepSeek的相关工作，想必还记得我此前提到的“缩放瓶颈”（scaling wall）——当时我对实现方式做了细微调整。  
是的，您会注意到，他们自身也采用了随模型规模增大而逐步调低学习率的策略，以补偿更大模型所导致的更新量增大这一事实。因此，更直接地回应您的问题：在DeepSeek的情形下，随着模型规模扩大，我们的激活值更新量确实会增大，因此必须相应调低全局学习率，以对此加以补偿。

## 段落 51

**英文**: Okay nice questions. Okay so that was kind of the conceptual somewhat mathematical components of mu P now I want to talk about the empirical aspects of mu P and so I'm going to talk through a pre print or I think this one maybe I think this is a very important part of the scale of the model is a large scale operation of new transfer and I like this because it's got a bunch of oblations and I think I'm a sucker for oblations so I'll present any paper that has large scale in the course and so they do essentially with mu P as we've described it just look at the right hand side which is the more relevant piece you know they're scaling down the variances they're scaling down the learning rates by the with the global with the models and and they're primarily keeping the depth fixed which is a little bit of an unusual scaling regime because usually you'd scale depth them with together but they really want to do a controlled experiment where they're only looking out with variations and they want to see if mu P precisely nails scaling in this way. There's also a little bit of a kind of weird subtlety that all of the mu P papers seem to do which is that if you remember your 224 and lecture you know you remember that there's like a scaling on the attention activations you scaled you know you do your inner product and you scale it down by one over the square root of D you know and I told you kind of this was a magic constant that was like the right thing to do you know mu P and other papers use one over D scaling. Instead of a one over square root D for various arguments related to activation and update size stability and so that's another thing that I think was worth pointing out because you might sort of not initially think of that as being something that's related to mu P. Architecture is mostly similar to the standard you know transformer stuff and as I already mentioned before they only consider with scaling right so they take a standard transformer trained auto regressively on you know pre training text and they want to basically make the model wider and wider and wider on the MLPs and sort of the model residual stream dimensions they're going to make that bigger and bigger and bigger and bigger and what they want is for the optimum learning rate to remain the same as they scaled the with up and if it remains stable then that's the big one. Big victory for mu P right so the game is hopefully cleared everybody you just want to scale with I want my learning rate that's optimal to stay the same. So question number one is you know does it work well the answer is yes so we have different with 128 512 2048 we have different learning rates across the columns and you know the sort of idealized strategy here is we run a sweep of learning rate to the small scale I pick the small scale. And I scale that up and hopefully that base learning rate remains optimal and yeah it seems like you know learning rates transfer very reliably across model sizes if we're doing this like you know some a precise with scaling. And so then I think you start asking questions of all right very similar to the previous question that was just asked of like okay when does mu P break right so you can ask that question in theory but you can also ask that question in practice right so I'm just going to try all sorts of modern variations to architectures that people do and then I'm going to ask you know does this hyper parameter transfer thing continue to hold under these variations or not and you know the papers quite nice because they just go through a lot of different stuff they'll vary the activations they'll vary the batch sizes the initializations the RMS norm gains though even use like a really exotic optimizers like sort of sign gradient style stuff and then they'll also vary the regularizers so which one of these prevents learning rate transfer. So the first one you know which I think is probably relevant if you were kind of looking at that deep linear network and saying oh no one just multiply his matrices together there's like nonlinearities in between right so the mu P work when we change nonlinearities around well.

**中文**: 好的，问题很棒。刚才我们大致探讨了μP在概念层面（带一定数学性质）的组成部分，接下来我想谈谈μP的经验性（实证）方面。我将介绍一篇预印本论文——我认为这篇论文尤为关键，因为它展示了该模型在大规模迁移任务上的实际表现。我喜欢这篇论文，原因之一是它包含大量消融实验；而我本人恰好特别偏爱消融分析，因此只要课程中涉及大规模实验的论文，我都会予以呈现。

他们本质上采用的是我们此前已描述过的μP方法：重点关注右侧（即更具相关性的部分）。具体而言，他们按全局尺度（即随模型规模变化）同步缩小参数方差与学习率；同时基本保持网络深度不变——这属于一种略显特殊的缩放范式，因为通常我们会同步缩放宽度与深度。但此处他们刻意设计为受控实验，仅考察宽度变化带来的影响，从而检验μP是否能精准刻画此类缩放规律。

此外，所有μP相关论文似乎都存在一个略显微妙且略带反常的细节：回顾你们在224课程或相关讲座中的内容，应记得注意力激活值存在一种缩放方式——即在计算内积后，需除以√D（D为隐藏层维度），我曾向你们强调过，这一“魔法常数”正是正确做法。然而，μP及其他论文却采用1/D而非1/√D的缩放方式，其依据在于保障激活值与参数更新量的稳定性等多方面考量。这一点也值得特别指出，因为初看之下，你可能并不会立刻意识到它与μP存在关联。

模型架构整体上与标准Transformer相近。如前所述，他们仅考察宽度缩放：即以标准Transformer为基础，在预训练文本上进行自回归训练，然后持续扩大MLP层及残差流通道的维度。其核心目标是：当模型宽度不断增大时，最优学习率能保持恒定；若该目标得以实现，则将成为μP的一大重要胜利。换言之，整个实验的核心目标非常清晰——大家务必明确：只需缩放宽度，同时确保最优学习率维持不变。

那么第一个问题是：该方法是否效果良好？答案是肯定的。图中展示了不同宽度（128、512、2048）下的实验结果，各列对应不同学习率。理想策略是：先在小规模模型上进行学习率扫描，选定最优值，再将其直接按比例放大至更大规模模型；若该基础学习率在放大后仍保持最优，则说明成功。实验结果表明：只要采用精确的宽度缩放方式，学习率便能在不同规模模型间高度可靠地迁移。

接下来，自然会引出与之前类似的问题：“μP究竟在何种条件下会失效？”这一问题既可从理论上探讨，也可通过实践验证。因此，我将尝试各类当前主流的架构变体，进而检验：在这些变体下，超参数（特别是学习率）的可迁移性是否依然成立？值得称道的是，该论文进行了大量细致的实验：他们分别改变激活函数、批量大小、初始化方式、RMSNorm增益系数，甚至尝试了极为前沿的优化器（例如基于符号梯度的方法），同时还调整了各类正则化策略。最终目标是：找出上述哪些因素会破坏学习率的可迁移性。

第一个问题——也是我认为最相关的：若回顾深度线性网络，你会注意到，其中并非简单地将矩阵相乘，而是矩阵之间嵌入了非线性变换。那么，当我们在μP框架中更换非线性激活函数时，其有效性是否依然成立？

## 段落 52

**英文**: But essentially it probably also says that it's made about if you're good at guessing if you're working faster than you actually notice right here. seen deep seek basically fits scaling lost to batch sizes to try to get what the optimum batch size was. Once again, we see that as we scale up batch sizes by 4 up or down, optimum learning rates remain stable. What about initializations? There are some initializations that people vary. For example, some people set the query matrix to 0 so that all the different items get uniform attention. Maybe that's more stable. Some people, sort of the unembedding layer at the very top, they'll scale differently based on either you standard parameterization or muP. Maybe that matters a lot. Turns out neither of those do. The center column, the optimum learning rate, remains optimal in all of these cases.

**中文**: 但本质上，这可能也表明：它主要关乎你是否擅长猜测——即你实际工作速度是否快于你当前所能察觉的速度。深度搜索（Deep Seek）基本遵循了批量大小的缩放规律，以尝试找出最优批量大小。我们再次发现，当批量大小上下调整4倍时，最优学习率依然保持稳定。那么初始化方式呢？人们确实会采用不同的初始化方法。例如，有人将查询矩阵初始化为0，从而使所有不同项目获得均匀的注意力权重，这或许更稳定；还有人会对顶层的解嵌入层（unembedding layer）采用不同的缩放方式，具体取决于标准参数化方法或μP（micro-parameterization）方法，这或许影响很大。但结果表明，这两种做法实际上都无关紧要。中间一列所示的最优学习率，在所有这些情况下均保持最优。

## 段落 53

**英文**: What is it not robust to? Well, it's not going to work for every single case. For example, if you add sort of learnable gains, that turns out to break muP. So you need to remove the biases. But if you remove them, the muP works. If you add them back in, don't necessarily work. Similarly, you can try more exotic optimizers. Lyon is an optimizer that takes the sign of the gradient updates, which to me feel a little bit crazy. But I think this was searched. This was found through like evolutionary search or something like this to find the fastest optimizer. If you use this kind of a more crazy optimizer, it really breaks down.

**中文**: 它对哪些情况不具备鲁棒性？事实上，它并非适用于所有情形。例如，若引入某种可学习的增益（learnable gains），就会导致muP失效。因此，必须移除偏置项；一旦移除，muP便能正常工作；但若再将偏置项加回，则未必仍能奏效。类似地，你也可以尝试更复杂的优化器。例如，“Lyon”优化器采用梯度更新的符号进行更新，这在我看来略显激进。但我认为该优化器是通过进化搜索之类的方法筛选出来的，旨在寻找收敛速度最快的优化器。然而，若使用这类更为激进的优化器，muP性能将显著下降。

## 段落 54

**英文**: And I think this is what you expect. muP is designed to adapt to a very particular optimizer, like Adam W, to control the update sizes. So if you're using a totally different optimizer, I don't know why you'd expect the learning rates to transfer. So maybe expected that this thing fails. And then sort of finally, what is it? Also not robust due, turns out if you really have much stronger weight decay, muP actually starts to fail. And so this is one of the few significant muP failures that are in there. A lot of the other ones are kind of just like, oh, we may be expected that, or that's not standard to do. Weight decay is something that you actually do. OK. So muP seems generally useful.

**中文**: 我认为这正是您所预期的。muP 的设计初衷是适配某类特定优化器（例如 AdamW），以控制参数更新的幅度。因此，若您使用的是完全不同的优化器，我实在无法理解为何您会期望学习率能够直接迁移。所以，或许本就预料到这种方法会失效。最后，还有另一个问题：muP 的鲁棒性也不足——事实证明，若使用权重衰减强度显著增大，muP 实际上就会开始失效。因此，这是 muP 现有为数不多的重大失效案例之一。其余多数失效情形，往往只是“我们本就预料到会如此”，或“这种做法本身并不标准”。而权重衰减却是实际训练中普遍采用的操作。好的，综上所述，muP 似乎总体上仍具实用性。

## 段落 55

**英文**: Like if you take standard parameterization,. and kind of going back to the baseline, you might ask, all right, what if I just do standard baseline stuff, you can't use the same learning rate. The same learning rate results in significantly worse losses at 2048. Your model just blows up, gives you basically degenerate losses. You would have been very sad scaling up at the same learning rate. And we see also that the learning rate needs to scale down predictably as a function of the width. On the other hand, even if you scale up all the way to a 10B parameter model, you see that the base loss remains the same. So they do one large scale experiment, and they see that the learning rate remains sort of ideal at the 2 to the negative 6 level, which. is a kind of cool validation, right? So they do the whole study at a medium to small scale. They do one big sort of hero run, and then the learning rate remains optimal.

**中文**: 例如，若采用标准参数化方式并回归基准设置，你可能会问：“如果我仅执行标准的基准操作，会怎样？”此时，你无法沿用相同的学习率。在参数量为2048时，使用相同学习率会导致损失显著恶化，模型甚至直接崩溃，产生近乎退化的损失值；若以相同学习率进行规模扩展，结果将令人极度失望。我们还发现，学习率需随模型宽度的增加而可预测地降低。另一方面，即使将模型规模一路扩展至100亿参数，其基础损失仍保持不变。因此，他们开展了一次大规模实验，结果发现学习率在2的负6次方（即1/64）左右始终维持在较理想水平——这无疑是一项颇为有趣的验证。换言之，他们先在中等及较小规模上完成整项研究，再进行一次大规模“关键验证实验”，最终证实该学习率在此类扩展下依然最优。

## 段落 56

**英文**: So the empirical results on that look somewhat promising. The fact that Meta used it for Lawmaphore is also quite nice. But as far as I know, it's not a consensus that people use muP. OK, so putting it all together, how do you scale in the wild? I have never trained a 70B model at super-tintial sizes. And so we're going to have to rely a lot on case studies. And we saw several examples of scaling in the wild. We saw people setting things like model hyper parameters, especially learning rate and batch sizes, using scaling laws. We saw people using things like muP,. who are assumed stability to try to avoid search over these spaces. And then also, the use of things like alternative learning schedules like WSD can decrease the amount of compute that you need in order to fit a lot of these scaling laws.

**中文**: 因此，这方面的实证结果看起来颇为乐观。Meta 将其用于 Lawmaphore 项目，这一点也相当不错。但据我所知，目前业界尚未就采用 muP 达成共识。综上所述，在实际应用中应如何进行模型扩展呢？我本人从未在超大规模下训练过 70B 参数的模型，因此我们不得不大量依赖案例研究。我们已看到多个在真实场景中进行模型扩展的实例：有人依据扩展定律设定模型超参数（尤其是学习率和批量大小）；有人采用 muP 等方法，借助其假设的稳定性来避免在这些超参数空间中进行繁琐搜索；此外，采用 WSD 等替代性学习调度策略，亦可减少拟合诸多扩展定律所需的计算量。

## 段落 57

**英文**: So that's all I got.

**中文**: 所以，我就知道这么多。

---

*共 57 个段落，561 句话*
