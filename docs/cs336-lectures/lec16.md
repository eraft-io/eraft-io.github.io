# Lecture 16： Alignment - RL 1

生成时间: 2026-03-08 23:27:52

---

## 段落 1

**英文**: Okay, we'll get started. This is the second of the post-training lectures, and we're going to finish off the lecture from Tuesday first, whatever remaining content we have. And then we'll talk about reinforcement learning from verifiable rewards, kind of the new exciting thing that's been happening over maybe the last six months or so. And really powering all of these reasoning models that you kind of see dominating landscape. So before I get into the verifiable rewards part, the exciting math, RL content, I'm going to go back and I'm going to finish off RLHF. This is a very, very quick recap of the last few slides from Tuesday's lecture. We want to do reinforcement learning from human feedback, which is the setting where we observe pairwise preference data, which of the two responses are better. We'd like to get a language model policy that sort of maximizes some sort of underlying reward for this preference data. And if you remember, DPO is this algorithm that allows us to optimize this reinforcement learning objective. And this objective is hard because our language model, the policy, so to speak, is on the.

**中文**: 好的，我们开始吧。这是培训后的第二场讲座，我们将首先完成上周二讲座的剩余内容，然后再探讨基于可验证奖励的强化学习——这大概是过去六个月左右兴起的一项令人兴奋的新技术，并真正推动了当前占据主导地位的各类推理模型的发展。因此，在进入可验证奖励这一激动人心的数学与强化学习内容之前，我将先回溯并完整讲解RLHF（基于人类反馈的强化学习）。这部分是对上周二讲座最后几页幻灯片的极简回顾：我们的目标是基于人类反馈开展强化学习，即在观察到成对偏好数据（即两个回复中哪一个更好）的前提下，训练一个语言模型策略，使其能够最大化某种隐含于该偏好数据之上的奖励。如各位所知，DPO（直接偏好优化）正是一种用于优化该强化学习目标的算法；而该目标之所以难以优化，是因为我们的语言模型（即策略本身）处于……

## 段落 2

**英文**: bottom of this expectation. You're sampling from your language model. You're not just maximizing the likelihood under the language model. And in the way we're going to do this, if you remember the derivation is that we're going to make the non-parametric assumption that our policy classes the set of all functions, we're going to rewrite the reward as a ratio of the policies and then plug that into the Bradley Terry objective. And so what we're going to do is we're going to find a policy such that sort of the implied. reward maximizes the probability of seeing sort of the preferences that we see. So now this is great because it's basically supervised learning on a kind of alternatively parameterized objective, which is nice. DPO updates have this following form. The reason why DPO has kind of took the world by storm for a bit a year ago or so is that it can be written in this very nice form where what you're going to do is you're going to take these gradient steps, which are going to be multiplied by beta. That's kind of your regularization.

**中文**: 这一期望的底层机制。你正在从语言模型中进行采样，而不仅仅是最大化语言模型下的似然。按照我们即将采用的方法（如你回忆推导过程所示），我们将做出非参数假设，即策略类为所有函数的集合；接着将奖励重写为策略之比，再代入Bradley-Terry目标函数。因此，我们的目标是找到一个策略，使得该策略所隐含的奖励能够最大化我们所观察到的偏好数据出现的概率。这非常理想，因为它本质上是在一种另类参数化的目标函数上进行监督学习，这一点十分有利。DPO更新具有如下形式：大约一年前DPO之所以一度风靡业界，其原因在于它可被简洁地表达为这种优美形式——你只需执行梯度更新步骤，并将其乘以系数β，而β即为正则化参数。

## 段落 3

**英文**: You're going to have higher weight when your reward estimates are wrong. So you're going to update more when your sort of implied rewards are not correct. And then you're going to increase the likelihood of the good example and you're going to decrease the likelihood of the bad example. As I was kind of saying, if you sort of remember what I was saying last lecture, really RL algorithms kind of boil down often to just upweigh the good stuff, downweigh the bad stuff. And the subtlety really isn't deciding what the good stuff is and how much to upweigh. So this is one particular choice of doing that. And we'll see other things happen. So DPO works pretty well. For a while, essentially all the open model releases use some variant of a DPO to do their post training. And really, it's just very easy to get working compared to PPO.

**中文**: 当你的奖励估计出现偏差时，相应样本的权重会更高。因此，当隐含奖励不准确时，你将进行更大幅度的更新。随后，你会提高优质示例的概率，同时降低劣质示例的概率。正如我之前提到的（如果你还记得上一讲的内容），实际上许多强化学习算法本质上都可归结为：提升优质样本的权重、降低劣质样本的权重。其中的关键难点并不在于如何界定“优质样本”，也不在于确定应提升多少权重。上述方法只是实现这一目标的一种具体选择，后续我们还会看到其他方法。DPO效果相当出色；在一段时间内，几乎所有开源模型的发布版本均采用某种DPO变体进行后训练。与PPO相比，DPO的实现要简单得多。

## 段落 4

**英文**: So for a while, it was really the dominant approach. In the time since then, there was a huge flood of papers that was like, you know, asterisk. PEO. Basically everyone wanted to come up with a DPO variant. There was dozens and dozens of papers. There's so many that I don't think it's worth it to go over the big ocean of such papers. I'll mention two. Not because necessarily, I think they're like the right ones, but because I think they're ones that have been used recently by people that are sort of really pushing the limits of this kind of DPO style post training. One is SimPO. And SimPO just makes a very simple modification or two simple modifications.

**中文**: 因此，一段时间内，这种方法确实占据了主导地位。自那以后，相关论文如潮水般涌现，几乎每篇都带有“*”标记，例如PEO。基本上每个人都想提出一种DPO变体，相关论文多达数十篇。数量如此之多，以至于我认为逐一梳理这浩如烟海的论文并无必要。我仅提及其中两篇：并非因为我认为它们一定是最佳方案，而是因为近期一些致力于突破此类DPO风格后训练极限的研究者已实际应用了它们。其一是SimPO，它仅做了非常简单的修改，或说两种简单修改。

## 段落 5

**英文**: The first one is to really normalize the update size by the length of the responses. We'll see kind of this theme appearing later. And the other thing they do is they just get rid of the reference. So now we've lost the DPO sort of mathematical argument that, you know, what we're doing is looking at ratios of policies. But well, this is more purely looking at something, looks like something where we upweigh, you. know, the good stuff and downweigh the bad stuff. Under that, a sort of underlying motivation, SimPO totally fine. There's other variants where you don't do necessarily this like reference policy removal, where you just sort of normalize by length, called length normalize DPO, that kind of people have done. And this, you know, these two forms, DPO with length normalization and SimPO were the things that were tried, you know, pretty extensively by AI2 folks when they did 2, 2, 3. And I think one thing that I'll point out, it's about a pause for a moment because I feel.

**中文**: 第一种方法是真正地根据回复长度对更新量进行归一化处理。我们稍后会看到这一思路反复出现。另一种做法则是直接去除参考策略（reference policy）。这样一来，我们就失去了DPO原有的数学论证基础——即我们原本是在比较不同策略的概率比值。不过，现在的方法更纯粹地聚焦于某种类似“提升优质内容权重、降低劣质内容权重”的机制。在这种底层动机下，SimPO完全可行。此外，还存在其他变体，例如并不一定移除参考策略，而仅对长度进行归一化的方案，即所谓“长度归一化DPO”，已有研究者采用。实际上，AI2团队在开展2022年和2023年的相关工作时，已相当广泛地尝试了这两种方法：带长度归一化的DPO以及SimPO。这里我想稍作停顿，强调一点，因为我有所感触。

## 段落 6

**英文**: like this is an important conceptual, not conceptual, just an important empirical point, which is that in RL, a lot of the findings are very, very contingent on the specific setting. Right? So depending on the environment and which you run it, depending on the base model that you have, depending on the post-training preferences that you're running on, you will find pretty different conclusions. And so one example of this, right? So, you know, a bunch of the AI2 folks have been doing a lot of really good post-training. empirical work. And they had one work where they were sort of comparing DPO and PPO and they had found that PPO was better than DPO because of maybe its on-poliness and they showed this, you know, this jump exactly is the DPO to PPO gap. And then in a later work in 2, 3, they find actually if you do your SFT in a sort of nicer, better way, actually, that just eats up all the gains of both PPO and DPO. So neither of these really get any gains. And the only thing that does better is maybe DPO with normalization, right?. Like pretty different conclusions. Of course, a lot is different between this left and right paper, but it's not that one is wrong and the other is wrong.

**中文**: 例如，这是一个重要的实证性观点（而非概念性观点）：在强化学习（RL）中，许多研究发现都高度依赖于具体实验设置。对吧？因此，结论会因所采用的环境、基础模型以及后训练阶段所使用的偏好数据而大相径庭。举个例子：AI2 的许多研究人员开展了大量高质量的后训练实证研究。其中一项工作比较了 DPO 与 PPO，结果发现 PPO 优于 DPO，原因可能在于其“在线策略性”（on-poliness），并明确展示了这一性能跃升——即 DPO 与 PPO 之间的差距。然而，在后续另一项研究（发表于两年或三年后）中，他们发现：若以更优方式执行监督微调（SFT），则该改进便完全抵消了 PPO 和 DPO 各自带来的全部增益——换言之，二者均不再体现任何优势；此时唯一表现更优的，或许仅是经归一化处理的 DPO。结论差异可谓显著。当然，这两篇论文在诸多方面均存在差异，但并非其中某一篇错误、另一篇正确。

## 段落 7

**英文**: It's really that you should be careful about reading too many generalized conclusions about any of this stuff on the basis of one paper. Right? So that's an important note even as I go and talk about, you know, PPO and I go and talk about a GRPO later in this. You shouldn't really take any single experimental result necessarily as gospel. Okay. So the last thing I want to end with for RLHF is sort of two important things. And one of them will really just motivate, I think, the entirety of today's lecture. So I think that's important. So the first thing is over-optimization. So in some sense, this is just over-fitting in fancier naming, but it is a term that I think is very important because what it's saying is essentially as you optimize your. policy more and more.

**中文**: 事实上，你应当谨慎对待仅凭一篇论文就得出的、关于上述任何内容的过于宽泛的结论，对吧？因此，即便我接下来会谈到PPO，以及后文还会谈到GRPO，这一提醒依然至关重要。你切不可将任何单一的实验结果都奉为圭臬。好的，关于RLHF，我想最后强调两点重要内容，其中第一点将切实引出今天整场讲座的核心主题，因此尤为重要。第一点是“过度优化”。从某种意义上说，这不过是换了一种更花哨说法的“过拟合”，但我认为这一术语极为重要，因为它本质上揭示的是：随着你对策略的优化程度不断提高……

## 段落 8

**英文**: So think about this x-axis as how much RLI did. Initially your reward kind of goes up and up and up, but eventually, you know, the reward models that you've fitted on human preferences are diverging from, you know, real human preferences. And the more you optimize, you eventually kind of diverge out, right? Like you're not doing any better. You're just, you know, optimizing, but not actually improving your rewards, right? So this over-optimization thing appears basically everywhere in RLHF. It is a very big problem. And so that is kind of a concern. And over-optimization is a phenomenon that really happens in many ways because of the noisiness of human preferences and the complexities of human preferences. So some of my students had a study where, you know, they basically did RLHF on human preferences. They did RLHF on noisy versions of, you know, AI feedback. And then they did RLHF on non-noisy versions of human feedback.

**中文**: 因此，请将该x轴理解为RLI的执行程度。起初，你的奖励会持续上升，但最终，你基于人类偏好拟合出的奖励模型会逐渐偏离真实的人类偏好。随着优化程度不断提高，这种偏离最终会愈发严重——此时你实际上并未取得任何进步，而只是在盲目优化，并未真正提升奖励值。这种过度优化现象几乎普遍存在于RLHF的各个环节，是一个非常严重的问题。因此，这确实令人担忧。而过度优化之所以在多种情况下真实发生，根本原因在于人类偏好的噪声性与复杂性。例如，我的一些学生曾开展过一项研究：他们分别基于人类偏好、含噪声的AI反馈以及无噪声的人类反馈，开展了RLHF实验。

## 段落 9

**英文**: And you see clear over-optimization phenomena both for humans and for noisy AI feedback,. but not really for sort of clean, noiseless AI feedback. And so really, you know, if you're going to be post-training, you should expect to see curves that look like things on the left. As you train your model to do better and better on your sort of proxy rewards that you've measured, you're not necessarily going to get better and better models in terms of sort of human preference ruin rates. OK? And then the other thing, which is an important side note, is that when you do RL, right,. remember that I said that we're no longer in probabilistic world, right? So when you're doing supervised fine tuning or when you're doing pre-training, it's quite clear that what you're doing is you're trying to do probabilistic modeling on some distribution, right? You're doing distribution matching for something, right? But RLHF is all, it's all just a policy. There's no underlying necessarily distribution and how this manifests is that often you'll get much less calibrated models, right?. A number of results across many different papers have shown that, you know, RLHF models often, especially at temperature one, you know, show much more overconfident behavior. So this one was from one of the anthropic papers. This one was from, I think, the GPT-4 release.

**中文**: 你既能看到人类反馈中的明显过优化现象，也能看到含噪声的AI反馈中的类似现象，但在相对“干净”、无噪声的AI反馈中则基本看不到这种现象。因此，实际上，如果你要进行后训练，就应当预期会看到左侧这类曲线：随着模型在你所测量的代理奖励（proxy rewards）上表现越来越好，其在人类偏好胜率（human preference win rates）方面的实际表现却未必持续提升。明白了吗？  
另一点重要补充是：当你采用强化学习（RL）方法时，请记住——我们已不再处于概率建模的世界中，对吧？在监督微调或预训练阶段，你显然是在对某个分布进行概率建模，即试图实现某种分布匹配；但基于人类反馈的强化学习（RLHF）本质上只是一种策略优化，其背后并不必然存在一个基础分布。这一差异的实际体现是：RLHF模型往往校准度更低。大量不同论文的研究结果均表明，RLHF模型（尤其在温度参数设为1时）通常表现出更强的过度自信倾向。例如，这一发现出自Anthropic公司的一篇论文；而另一项相关发现则见于GPT-4的发布报告。

## 段落 10

**英文**: This one was from a paper that one of my post-docs did. In all these cases, essentially, the RLHF models are much less calibrated. And maybe that's fine, right? Because calibration isn't part of the reward that you're putting in. So that's, you know, as design, but you should be very careful thinking of these models as sort of calibrated probabilistic models, which you might be sort of tempted to do if you're kind of coming into this from a generative modeling background. So okay. So up until now, we've been kind of thinking about RLHF and all of these things. Actually, I'll stop for a moment. If anyone has any final questions for RLHF, I'll answer them before I go on because we're going to totally switch topics to RL from verified rewards in a moment. So, we went to the human references graph. What's the order of the vaccine step that you're going to be doing? This one? Yeah.

**中文**: 这篇论文出自我的一位博士后研究人员。在所有这些案例中，RLHF模型的校准程度普遍较低。但也许这并无大碍，对吧？因为校准并未被纳入你所设定的奖励函数之中。因此，从设计角度看，这本就如此；但你必须格外谨慎，切勿将这些模型简单视作经过良好校准的概率模型——如果你是从生成式建模背景出发，或许会不自觉地产生这种倾向。好的。到目前为止，我们一直在讨论RLHF以及相关问题。实际上，我先暂停一下：如果大家对RLHF还有任何最后的问题，我现在就来解答，因为接下来我们将完全转向另一个主题——基于经验证奖励的强化学习（RL）。我们已进入“人类参考图”环节。你接下来要执行的疫苗接种步骤顺序是什么？是这一个吗？对。

## 段落 11

**英文**: So the x-axis is proxy reward. So here, we're fitting like a reward model. We're not doing DPO here, right? So let's say we're doing something like PPO where we fit a reward classifier. And the x-axis is saying, how well did my RL algorithm optimize that reward classifier? So this is success at RL is on the x-axis. My axis is this is kind of like true wind rates, either measured by AI feedback here or human feedback. So this is like real human votes on the y-axis here. And so this is saying you might think that succeeding at RL will make you succeed at the actual task that you're trying to optimize for. That's not the case. It will overfit at some point for at least human preferences. Yes? So, we did that in slow motion.

**中文**: 因此，横轴代表代理奖励。此处，我们正在拟合一个奖励模型，而非采用DPO，对吗？假设我们采用的是类似PPO的方法，即拟合一个奖励分类器。横轴表示：我的强化学习算法在多大程度上成功优化了该奖励分类器？换言之，横轴刻画的是强化学习的成功程度。纵轴则代表真实偏好（即“真实风评”），此处通过AI反馈或人工反馈来衡量，因此纵轴实际反映的是真实的人类投票结果。这说明：你或许会认为，强化学习越成功，就越能优化你真正想达成的任务目标；但事实并非如此——至少在人类偏好方面，强化学习最终会出现过拟合。是的？我们此前已逐步演示过这一过程。

## 段落 12

**英文**: Why is it really that problem? Like wind up, log A minus log B minus Gatsby, log A, log A. Or just like log A zero root. Are you just asking like why don't they like pull the fractions out in the log or? Is this a new way of computation thing that like when you came east to ratio, taking the log. is consumed to compute and that's why you want to be able to do that? No, no. I don't think that I think any ratio is actually bad in numerically. So I think this is really just like a save the space. You know, the ratios are more compact and there are some intuitive objects. Right? So. Because the moment you put a simple, it becomes a different part because you know that we're. at the same speed up.

**中文**: 这问题究竟出在哪里？比如，是“wind up”（收尾）、log A 减 log B 再减盖茨比（Gatsby），还是 log A、log A？抑或仅仅是 log A 的零次方根？您是在问，为什么他们不直接把对数中的分数提出来？还是说这是一种新的计算方式——比如，当您转向比值时，取对数是为了便于计算，因此才希望这样操作？不，不。我不这么认为；我认为任何比值在数值上其实都不太理想。所以，这本质上纯粹是为了节省空间。您知道，比值形式更紧凑，而且更具直观性，对吧？因此，一旦您引入一个简单因子，它就变成了另一部分，因为……我们知道，此时它们的加速效果是一致的。

## 段落 13

**英文**: That's right. Yeah, yeah. It's simply always just like a very different motivation for why you would do this thing. Right? There's no ratios. It's a very fundamentally different object. Yes. For your show, like the rewards we have had you think of them. It goes on really like how the pauses measure like when we run truce off like a reward score. This is usually the reward for the monotony of the mod that we've been doing. Right.

**中文**: 没错。是的，是的。你做这件事的动机始终截然不同，对吧？这里没有比例关系，它本质上就是一个完全不同的对象。是的，就您的节目而言，比如我们之前设定的那些奖励——您想想看，这些奖励实际上就像我们在运行“休战”程序时所测量的“奖励分数”一样。这通常是我们持续进行当前模组工作所带来的单调感所对应的奖励，对吧？

## 段落 14

**英文**: But it's kind of like a trained test gap. The x-axis here, you have a train set. You fit a reward model on it and you're measuring that fitted reward model. Right? And the y-axis is fresh samples from the true reward to Oracle. Right. And so they're measuring in expectation the same thing but they're not measuring in finite sample the same thing. It's the same. It's the same as like train test gaps in machine learning. Right. Same conceptual object.

**中文**: 但这类似于一种训练-测试差距。此处横轴表示训练集：你在该数据集上拟合一个奖励模型，并测量该拟合所得的奖励模型；纵轴则表示从真实奖励（即“Oracle”）中抽取的新样本。因此，二者在期望意义上测量的是同一对象，但在有限样本下测量的却并非完全相同。这与机器学习中的训练-测试差距本质上是一致的，属于同一概念性对象。

## 段落 15

**英文**: They are in expectation measuring the same thing for any finite sample that's not the case. Okay. Good. So I think you know all this question is about reinforcement learning from human feedback. I think brings up a good point which is that you know human feedback is very difficult to optimize. It's very difficult to scale. So you might wonder are there other kinds of more effective RL that we can bring to bear on post training. Right. And that's going to get us here. Right.

**中文**: 它们在有限样本下预期测量的是同一事物，但实际情况并非如此。好的。很好。因此，我认为大家都知道，这个问题涉及基于人类反馈的强化学习。这引出了一个很好的观点：人类反馈非常难以优化，也极难扩展。因此，你可能会思考：是否存在其他更有效的强化学习方法，可用于模型训练后的优化？没错，这正是我们接下来要探讨的内容。

## 段落 16

**英文**: So up until now we've been kind of talking about chat gbd 3. 5 and that era of models. And now I think we want to talk about 01 and the new set of kind of reasoning style models. Right. And the way we're going to get there is kind of this thinking of saying, okay, we have this very powerful tool now. We have this reinforcement learning tool that we can use for post training. And initially, you know, the instinct was to say, you know, there's one true objective and that objective is whether people like the bomb. Right. And so if we just optimize through the true objective we care about, that's all we need. Turns out to be very difficult, right.

**中文**: 因此，到目前为止，我们一直在讨论ChatGPT 3.5及那个时代的模型。而现在，我想我们应该转向讨论01以及新一代以推理为导向的模型了，对吧？我们将通过这样一种思路来切入：首先，我们如今已拥有一个极为强大的工具——一种可用于后训练的强化学习工具。起初，人们的直觉是认为存在唯一真正的目标，而这一目标就是用户是否喜欢该模型，对吧？因此，只要直接针对我们真正关心的这一目标进行优化，就已足够。但事实证明，这极其困难，对吧。

## 段落 17

**英文**: Human, human approval is easy to hack. It's hard to collect that scale. It's hard to RL at scale. For all those reasons you see, you know, over optimization and all these kind of issues. On the other hand, we could instead turn to where RL has really dominated, right. Things like AlphaGo or AlphaFold. And then, you know, looking at those domains you might say, you know, what we really need is kind of a domain between know the true reward. And for which we can evaluate such true rewards very quickly and efficiently and at scale. Right. And if we could do that, then we can bring to bear all of the successes of reinforcement learning from the past into language model.

**中文**: 人类的评判容易被“破解”，但要大规模收集人类反馈却十分困难，大规模开展强化学习（RL）同样面临巨大挑战。正因如此，我们才会看到过度优化等各类问题。另一方面，我们可以转而关注强化学习真正大放异彩的领域，例如AlphaGo或AlphaFold。观察这些领域，你或许会意识到：我们真正需要的，是一种能够明确知晓真实奖励（true reward）的场景，并且这种真实奖励必须能快速、高效、大规模地进行评估。倘若能做到这一点，我们便能将过去强化学习所取得的全部成功经验，应用于语言模型之中。

## 段落 18

**英文**: Right. So that's the overall mindset that we're adopting now. Right. We have the tools that we've built. Now let's apply them into a very kind of different way of using the same tools, right. Taking inspiration from successes in RL. Okay. And so today I'm going to talk in two parts. Right. So the first part is just going to be an algorithmic part.

**中文**: 没错。因此，这就是我们目前所采用的整体思维模式。没错。我们已经构建了相关工具。现在，我们要以一种截然不同的方式来运用这些相同的工具，对吧？从强化学习（RL）的成功案例中汲取灵感。好的。今天我将分两部分进行讲解。第一部分将纯粹聚焦于算法层面。

## 段落 19

**英文**: Right. So this part on the very top. I'm going to talk about different algorithms. First, I'm going to talk about PPO in more detail because you know, I kind of cut that from last lecture for length reasons. And then I'm going to transform PPO into GRPO, a simpler version in some sense. And then we'll talk through those objectives and various implementation details of GRPO to get us to multiple canonical variations of GRPO. And having done that, we will then walk through the three, I think, big reasoning model sort of training things that have been done. As case studies, since after part one, you have all the tools that you need to understand everything that is happening in part two. Right. And after you do part two, you understand at least how these three Chinese open LMs were made.

**中文**: 好的。那么，最上面的这一部分，我将介绍不同的算法。首先，我会更详细地讲解PPO，因为上一讲中出于时长考虑，我略去了这部分内容。接着，我将把PPO转化为GRPO——某种意义上更简化的版本。然后，我们将逐一分析GRPO的目标函数及其各种实现细节，从而引出GRPO的多种标准变体。完成这部分后，我们将以三个典型案例来梳理目前所开展的、我认为最重要的三大推理模型训练工作；因为在第一部分结束后，大家已掌握全部必要工具，足以完全理解第二部分所涉及的所有内容。而学完第二部分后，大家至少就能明白这三款中国开源大语言模型是如何构建出来的。

## 段落 20

**英文**: Okay. So now we are going to return to PPO. It is the thing that I didn't want to do, but we have to do in order for you to understand why GRPO exists. Right. So what is PPO? At least in my sort of mental model of what PPO is, we start from the simplest possible thing, and then we move slowly but surely downwards to PPO. So the simplest possible thing, I mentioned this in last lecture, this is the policy gradient. So on the left, I would like to optimize the expected reward under my policy P of theta. And we're going to optimize that through gradient descent. So I take the gradient of that object, and I get the right hand side of the equation, which is the policy gradient, the expectation. Under my current policy of the reward, and I'm going to take gradient steps that sort of either increase or decrease the probability according to the sign of the reward z.

**中文**: 好的，现在我们将回到PPO（近端策略优化）。这是我原本不想讲的内容，但为了让你理解GRPO为何存在，我们又不得不讲。那么，什么是PPO？至少在我的认知模型中，我们从最简单的基本概念出发，再逐步、稳妥地过渡到PPO。这个最简单的概念，我在上一讲中已提到过，即策略梯度（Policy Gradient）。在左侧，我们的目标是优化策略π_θ下所获得的期望奖励；我们将通过梯度下降法来实现这一优化。因此，我们对这一目标函数求梯度，从而得到等式右侧——即策略梯度，它是在当前策略下对“奖励乘以策略概率梯度”的期望值；随后，我们将依据奖励r的符号，沿梯度方向进行更新，以相应增大或减小对应动作被选中的概率。

## 段落 21

**英文**: Right. Hopefully straight forward. If this is not familiar to you, you can sort of brush up on how that policy gradient derivation happens. Now, there are two things that we might want to think about here that are kind of inefficient. Right. So the first thing is that this is what you might call purely on policy. Right. So the way that the reinforced gradients work, I have to sample from P theta, and then I will immediately take a step on those sampled examples. Right. And so every time that I want to take a gradient step, I have to compute a reward.

**中文**: 没错，希望这很直接。如果这部分内容对你来说比较陌生，你可以大致回顾一下策略梯度推导的过程。现在，这里有两个可能显得效率较低的问题。首先，这种方法属于纯粹的“在线策略”（on-policy）方法：强化梯度的计算方式要求我们从分布 $P_\theta$ 中采样，然后立即基于这些采样样本执行一次参数更新。因此，每次进行梯度更新时，都必须重新计算奖励。

## 段落 22

**英文**: Right. And I have to do a rollout. You will sort of understand this in your assignment five, but the expensive part of RL is the rollouts. Right. We have to actually run the language model and get samples, and we know that that's slow. Right. From Percy's inference lecture, we know that that's very complicated. It's very tricky. We would much rather sample less frequently. Right.

**中文**: 没错。而且我需要进行采样（rollout）。你们在第五次作业中会大致理解这一点，但强化学习（RL）中最耗时的部分正是这些采样过程。我们需要实际运行语言模型并获取样本，而我们知道这很慢。正如珀西（Percy）在推理课程中所讲的那样，这一过程非常复杂，也十分棘手。因此，我们更倾向于降低采样频率。

## 段落 23

**英文**: We do a rollout once. We sample once, and then we take multiple updates on those rollouts. So that motivates something called TRPO, right. Where instead of taking updates from P theta, I would like to allow my sort of updates to go stale. So what I'm going to do is I'm going to sample from P theta old, this sort of base distribution down here, but still get sort of valid policy gradients. Well, how can I do that? Well, I can do what's called an important sample in correction, and then I can keep, you know, my policies close to my old ones so that I don't get too far from my old one and get sort of crazy reward estimates. Right. Very simple idea. Just do a little bit of off to on policy correction. You get something called TRPO.

**中文**: 我们仅执行一次轨迹 rollout，仅采样一次，然后基于这些轨迹进行多次更新。这便引出了所谓的“信任区域策略优化”（TRPO）。其核心思想是：不再直接从当前策略 \(P_{\theta}\) 中采样并更新，而是允许更新所依据的策略略微“过时”。具体而言，我将从旧策略 \(P_{\theta_{\text{old}}}\)（即下方所示的基础分布）中采样，但仍能获得有效的策略梯度。那么，如何实现这一点呢？方法是采用重要性采样校正，并同时约束新策略与旧策略之间的差异，避免偏离过远，从而防止奖励估计出现剧烈波动。这是一个非常简单的思路：仅做少量的“离策略”到“在线策略”的校正，由此便得到了TRPO。

## 段落 24

**英文**: This A of T is a lower variance version of ROZ. I'm not going to talk about advantage estimation in much more detail. PPO is a is a simple extra step beyond this. It says, you know, instead of doing kale divergences, I'm just going to clip the advantages. And this is naturally going to force my policy to remain close, because if I go too far, then I'm not going to be able to get higher and higher rewards. These rewards will just get clipped off sort of one minus epsilon or one plus epsilon. There's an upper bound to how much reward I can collect. So there's no incentive for the RL algorithm to make the policy really different from the current one. Right. So it's a soft version that you can say, think of kind of this idea.

**中文**: 该A of T是ROZ的一种低方差变体。我不会更详细地讨论优势估计。PPO则是在此基础上增加的一个简单步骤：它指出，与其计算KL散度，不如直接对优势函数进行裁剪。这种做法自然会迫使策略保持与当前策略接近，因为一旦偏离过大，就无法持续获得更高的奖励——这些奖励将被限制在（1−ε）至（1+ε）的范围内。因此，我能获取的奖励存在一个上限，强化学习算法也就没有动力使新策略与当前策略产生显著差异。这实际上是一种软性约束，你可以这样来理解这一思想。

## 段落 25

**英文**: Okay. So PPO, very successful RL algorithm, used in many different places, you know, used in sort of very toy RL environments. It was used in open AI sort of doda bot works very well in such actual RL tasks. Oh, yeah, there was a video. I forgot about that. Yeah, look, you can run around. Yeah. Okay. And at a conceptual level, it's not that complicated. Right.

**中文**: 好的。PPO 是一种非常成功的强化学习算法，被广泛应用于许多不同场景，比如一些简单的强化学习环境。它也被用于 OpenAI 的 Dota 机器人，在这类实际强化学习任务中表现非常出色。哦，对了，之前还有一段视频，我差点忘了。是的，你看，它可以自由跑动。好的。而且从概念层面来看，它其实并不复杂，对吧？

## 段落 26

**英文**: If you go look at the opening eye documentation for PPO, it looks fairly simple. Right. You see that equation that I listed above. It's this PPO clip objective. And really the only thing that's maybe slightly complicated is that this A of T is actually calculated by using something called a value function. So you need a second neural network to essentially compute kind of the expected rewards. And that will be used to in some sense lower the variance of my gradients. Right. I'm not going to go into the details once again of sort of all the RL details. But you know, the one implementation difference here that matters is that we need this value function and that will become important in a moment here.

**中文**: 如果你查阅PPO的OpenAI官方文档，会发现其内容看起来相当简单。没错，你看到上面列出的那个公式，即PPO裁剪目标函数（PPO clip objective）。实际上，其中唯一可能略显复杂之处在于：这里的A(t)是通过一种称为“价值函数”（value function）的方法计算得出的。因此，你需要第二个神经网络来估算预期回报。该价值函数在某种意义上可降低梯度的方差。我将不再重复讲解所有强化学习的细节。但此处一个关键的实现差异在于：我们需要这个价值函数，而这一点在接下来将变得至关重要。

## 段落 27

**英文**: But PPO in practice is a very, very different beast from PPO in theory. And you know, you're in for a very bad time. If you know, there's a blog post that says 37 implementation details of PPO. You know, I don't want to know all 37 of those guys. And you know, there's a huge long list of sort of different PPO variants in this blog post and all of them have the different scores on the RL benchmarks. And there's a whole paper written on why implementation details matter in PPO and how if you really mess it up, you're not even computing the policy gradient correctly anymore, but it actually works better. Right. So it's kind of a crazy place. If you look at the implementation details of PPO. And so we actually do have to maybe look at a live implementation detail.

**中文**: 但在实践中，PPO与理论上的PPO截然不同。你将面临极其糟糕的处境。例如，有一篇博客文章列出了PPO的37个实现细节——我可不想逐一了解这37项内容。该博客还罗列了大量不同的PPO变体，而它们在强化学习基准测试中的得分各不相同。甚至有一整篇论文专门探讨PPO中实现细节的重要性，并指出：若实现严重出错，你甚至已无法正确计算策略梯度，但算法反而效果更好。没错，这确实是个相当疯狂的领域——只要你细看PPO的实现细节就会发现这一点。因此，我们或许的确需要参考一个实际运行中的实现细节。

## 段落 28

**英文**: I'll actually go over quickly because PPO isn't the point of this lecture. But what I want to get across to you is, you know, you all haven't been necessarily working on this post training space. Like why do people care so much about alternatives to PPO. It is because of things like this, right. PPO works well, but it is kind of a beast. And this is one example. I think from the early days of RLHF, there was this nice diagram describing, you know, this is how you would do a pretty standard implementation of PPO. This diagram is pretty gnarly, right. We've got a reward model for RLHF. We've got a value model that's supposed to keep track of kind of the expected reward.

**中文**: 我实际上会快速过一遍，因为本节课的重点并非PPO。但我想向大家传达的是：你们可能并未深入涉足后训练（post-training）这一领域。那么，为什么人们如此关注PPO的替代方案呢？原因正在于此——PPO虽效果良好，却相当复杂棘手。以上便是一个例证。我认为，早在RLHF发展初期，就曾出现过一张清晰的示意图，用以说明如何实现一种相当标准的PPO流程。这张图确实非常繁杂，对吧？其中包含了用于RLHF的奖励模型，以及一个旨在追踪预期奖励的价值模型。

## 段落 29

**英文**: We've got generalized advantage estimation. I haven't even explained what that is. And that's going to go into the policy language model. And we're going to do, you know, our policy gradient updates. So there's all this sort of machinery that has to go into place for PPO to work. So we're going to look at implementation a few years back now actually. Some like students and I did, you know, re-implementation of algorithms like PPO. And other people have used it. So this is an implementation that's been somewhat tested. Not to say that this is a good implementation.

**中文**: 我们采用了广义优势估计（GAE），但我甚至还没解释它是什么。该方法将被整合进策略语言模型中，然后我们将执行策略梯度更新。因此，要使PPO正常运行，需要部署一整套相关机制。实际上，我们接下来将回顾数年前的实现——当时我和一些学生曾重新实现了PPO等算法，其他人也使用过该实现。因此，这一实现已经过一定程度的测试，但并不意味着它就是一种优秀的实现。

## 段落 30

**英文**: I'm sure, you know, something like more modern implementations from volcano engine and other things probably work a little bit better. But I just want to walk you through all the different components also because as you write garpio in your assignments, your structure will probably mirror some aspects of this, right. All RL algorithms have similar kind of outer loop. So if we look at this, right. So a PPO step looks very similar to a standard gradient update step in a language model. So this is the outer sort of loop. What we're going to do is we're going to, you know, get a bunch of rollouts. And once we get, you know, a bunch of rollouts, what we're going to do is we're going to compute some losses. And then we're going to just take backwards and then gradient steps with, you know, clip norm. And that's it, right.

**中文**: 我确信，你知道，像火山引擎（VolcEngine）等更现代的实现方案可能效果略好一些。但我还是想为你逐一介绍所有不同组件，因为你在作业中编写Garpio时，其结构很可能在某些方面与之类似，对吧？所有强化学习算法都具有相似的外层循环结构。我们来看这里：PPO的单步更新与语言模型中的标准梯度更新步骤非常相似，这就是外层循环。具体而言，我们将首先采集一批轨迹（rollouts）；待获得这批轨迹后，便计算若干损失函数，接着执行反向传播，并采用梯度裁剪（clip norm）进行梯度更新——仅此而已，对吧？

## 段落 31

**英文**: Very unintamidating. The outer loop of RL is usually fine, right. You know, loss computation is also basically the same thing. So this is probably a small for those of you in the back. But if you look at this, you know what you have is one block that's computing the loss for the value function. Remember, PPO has both a value function and a policy. So we have to update both of those at once. So we're computing how close is the value function, the kind of the actual sort of returns, the rewards that we saw. And we want to keep those close because the value function is supposed to provide variance reduction. And then we've got the actual sort of rewards that we're going to update on for PPO.

**中文**: 非常平易近人。强化学习的外层循环通常没什么问题，对吧？你知道，损失计算本质上也差不多。因此，对于后排的各位来说，这可能显得比较小。但如果你仔细观察，就会发现这里有一个模块专门用于计算价值函数的损失。请记住，PPO 同时包含价值函数和策略函数，因此我们必须同时更新这两者。我们正在计算价值函数预测值与实际回报（即我们观测到的奖励）之间的接近程度；我们希望二者尽可能接近，因为价值函数的作用之一就是降低方差。此外，我们还拥有将用于 PPO 更新的实际奖励。

## 段落 32

**英文**: And then we have sort of the clipping constants, 1 minus epsilon, 1 plus epsilon. This code is literally just copying this back over. And just to give you sort of some, you know, intuition for hyper parameters, you know, a typical clip range might be something like 0. 2 here. Right. So you would be allowed to go essentially from 0. 8 to 1. 2 in in likelihood ratio between your old and new policy. The rollouts is where things start to get gnarly, but this is still essentially just calling inference. The left side is really just sampling, you know, rollouts from your current policy.

**中文**: 然后是我们所谓的裁剪常数：1减去ε、1加上ε。这段代码实际上只是将这些值简单地复制回去。顺便为你提供一些关于超参数的直观理解：此处典型的裁剪范围可能约为0.2，也就是说，新旧策略之间的似然比大致允许在0.8至1.2之间变动。而“rollouts”（轨迹采样）环节则开始变得复杂，但本质上仍只是调用推理过程；左侧实际上仅是从当前策略中进行采样，即生成若干条轨迹。

## 段落 33

**英文**: And then you evaluate the log problems of your samples and you move on to the right. And what do you see? Well, the only subtlety here is that your value and your reward and your policy may have different tokenizers. So you got to do retokenization. But otherwise you just feed your rollouts into the model. So okay, at this point, you're like looking at this code and you say, okay, that's fine. Like this is not not so horrible. But the places where things start to get a little bit tricky is first we have to do some reward shaping. Right. So what does that mean? So stepping slightly back, one thing that's weird about reinforcement learning for language models is if you think about it from the RL perspective, technically it's kind of like a contextual bandit. Well, what is a contextual bandit? A contextual bandit is something where you get an input and you have a bunch of possible, you know, actions to take and you immediately get a reward.

**中文**: 然后，你评估样本的对数概率问题，并向右继续推进。那么，你看到了什么？这里唯一的细微之处在于，你的价值函数、奖励函数和策略可能使用不同的分词器，因此需要进行重新分词。除此之外，你只需将生成的序列（rollouts）输入模型即可。好了，此时你看着这段代码，心想：“好吧，这没问题，还不算太糟糕。”但真正开始变得棘手的地方首先在于我们需要进行一些奖励塑形（reward shaping）。那么，这具体意味着什么？我们稍微退一步来看：强化学习应用于语言模型时，一个奇怪的现象是——若从强化学习的角度思考，它在技术上其实更类似于一种上下文相关的多臂老虎机（contextual bandit）。那么，什么是上下文相关的多臂老虎机呢？它是指：你接收一个输入，拥有一组可供选择的动作，且在执行动作后立即获得奖励。

## 段落 34

**英文**: Right. In language modeling, you get a prompt, you can give an output and you immediately get a reward. There's no like state transitions. There's no environment exploration. There's none of this like complexity. Right. Reward shaping here is essentially constructing. Per token losses in order to be able to give you something more easy to learn for the RL algorithm. And so what happens in practice for ppo and for grpo, which you'll be implementing. Is that the KL terms, essentially the regularization that we're applying is actually computed per token, whereas the actual true reward, like the did you complete the task or not kind of thing, that's computed at the very last token.

**中文**: 没错。在语言建模中，你输入一个提示（prompt），模型生成输出后即可立即获得奖励；其中不存在状态转移，也无需与环境交互探索，更没有此类复杂性。因此，此处的奖励塑形本质上是构建每个词元（token）的损失函数，以便为强化学习算法提供更易于学习的目标。实践中，在PPO和你将要实现的GRPO中，KL散度项（即我们所施加的正则化项）实际上是按每个词元分别计算的；而真正的奖励（例如“任务是否完成”这类整体性判断）则仅在最后一个词元处进行计算。

## 段落 35

**英文**: Right. So you see that there's essentially a per token reward for the regularization and there's a single terminal reward at the end for success or not. Yes. What does it mean? What does not work? Oh, right. This is a, this is a, right, right. This is a kind of funny. This is basically we're only adding port token KL penalty. But once again, this is one of those funny ppo implementation things. The second line here, this is the actual true KL calculation. But when this number goes to negative, it becomes numerically unstable and you can actually hit that pretty often.

**中文**: 没错。因此您可以看到，正则化部分本质上是按每个词元（token）给予奖励，而成功与否则仅在最后给出一个单一的终止奖励。是的。这是什么意思？哪里出了问题？哦，对了，这个……这个……对，对。这有点儿奇怪：我们实际上只对输出词元施加了KL散度惩罚。但再次强调，这又是PPO实现中那些奇怪的问题之一。此处第二行才是实际、真实的KL散度计算；但当该数值变为负数时，就会导致数值不稳定，而且这种情况其实相当常见。

## 段落 36

**英文**: And so this gets clamped off at zero, which, you know, all of you know if you clip off a log likelihood ratio at zero, that's not a KL anymore, but it is some approximation to a KL. Yes. So we've done really boss function whatever update steps is actually really close to relates to the reward. Is that the same as other? Yeah. Yeah. Good point. So the updates, you know, as I was saying in this outer loop is actually just taking gradient steps. So in terms of your code, it will look no different than just taking, you know, normal gradient steps. What is happening here, though, is in some sense, let's go back to the policy gradient equation here. Right.

**中文**: 因此，该值会被截断为零。众所周知，若将对数似然比截断为零，则其已不再是对称KL散度，而只是对KL散度的一种近似。是的。因此，我们所采用的所谓“强势函数”（boss function）的更新步骤，实际上与奖励高度相关。这与其他方法是否相同？是的，是的，很好的观点。因此，如我之前所述，在外层循环中进行的更新，本质上仅是执行梯度下降步。就代码实现而言，其形式与常规的梯度下降步完全一致。然而，此处实际发生的情况是——让我们回到此处的策略梯度方程。

## 段落 37

**英文**: If you write down this thing, this is r of z times p theta z. This is kind of like just taking gradients with respect to theta of r z, p log p theta z. It's a weighted loss that you then take gradients of. And what you're not doing is technically if you were taking a real gradient, you should also take a gradient into p theta, but you don't do that. There's kind of a stop implicit stop gradient. And so all you do is you compute this inner loss, you feed it to the autograd and you'll get the right steps for the, for the policy gradient. Yeah, you'll have to do that for for the assignment as well. And we have like a little tutorial that explains kind of what's going on in case that that quick explanation was not clear. Okay. And finally, the part that I think is the gnarliest is there's a the box that I did not explain called generalized advantage estimation, right.

**中文**: 如果你写下这个式子，它就是 $ r(z) \times p_\theta(z) $。这类似于对 $ r(z) $ 关于 $ \theta $ 求梯度，再乘以 $ \log p_\theta(z) $ 的梯度；即你对一个加权损失函数求梯度。而你实际上并未做的是：若严格按真实梯度计算，本应对 $ p_\theta $ 本身也求梯度，但你并不这么做——这里隐含了一个“停止梯度”（stop-gradient）操作。因此，你只需计算该内部损失，将其输入自动微分系统（autograd），即可得到策略梯度的正确更新步长。是的，作业中你也需要这样做。我们还提供了一个简短教程，解释其中的原理，以防刚才的快速说明不够清晰。好的。最后，我认为最复杂的一部分是那个我尚未解释的模块，即“广义优势估计”（Generalized Advantage Estimation，GAE）。

## 段落 38

**英文**: So what you need to do whenever you have a policy gradient, the variances of the gradient are often very high. Right. And so you want to do as much variance reduction as you can. And instead of multiplying the gradients with the rewards directly, you can instead you can show that the following a of t quantity, which is essentially a discounted advantage estimate. That's a sort of RL term. And so one of the things that pPO does that's very different is to use this advantage estimate where you can tweak lambda and gamma to trade off bias and variance of your gradients. And funny things, though, is despite kind of the annoyance of this, this implementation complexity of like maintaining this value function and estimating all these quantities, you can just pick gamma equals lambda equals one, which reduces this whole thing basically to a baseline policy gradient. So you're got staking R minus the implied value and that works well too. And so the point of making you go through this was a lot of the implementations details for pPO are both simple in that it is just the outer loop RL. So you do just take gradients and also kind of annoying right you have to do all these sort of clipping things you have to think about what I'm going to do with the generalized advantage estimate.

**中文**: 因此，每当使用策略梯度时，梯度的方差往往非常高。为此，你需要尽可能地降低方差。与其直接将梯度与奖励相乘，不如采用如下所述的 $ a_t $ 量——它本质上是一种折扣优势估计（这是强化学习中的一个术语）。PPO 的一项显著特点，正是使用这种优势估计，并通过调节参数 $ \lambda $ 和 $ \gamma $ 来权衡梯度的偏差与方差。有趣的是，尽管这一方法在实现上略显繁琐（例如需维护值函数并估计诸多量），你却可直接取 $ \gamma = \lambda = 1 $，此时整个方法便简化为一种带基线的策略梯度：即用奖励 $ R $ 减去隐含的价值估计，该简化方法同样效果良好。之所以让你了解这些细节，是因为 PPO 的许多实现要点既简单又烦琐：说它简单，是因为它本质上只是外层循环的强化学习——你只需计算梯度；说它烦琐，是因为你还必须处理各种裁剪操作，并需仔细考虑如何使用广义优势估计。

## 段落 39

**英文**: What I'm going to do to train the value estimate. But you expect to see you know overall increasing rewards, including reward models and sort of negative KL rewards kind of go down. This is a once again, a contextual bandit. So I expect to see pretty reasonable training curves, not like crazy RL ones. Okay, so I went through pPO that was kind of a whirlwind tour, but I think hopefully you get the context of a what pPO is and be that it is sometimes a little bit tricky to get working. And so you know this has motivated a lot of people to try to find alternatives to pPO. What we're going to want to do is to apply you know essentially these RL algorithms to settings where pPO applies and pPO applies very well to general RL settings where you have sort of rewards. But we don't want the complicated implementation and maybe more importantly we want to get rid of the value model that is actually if you tried implement pPO really really annoying right because the value model is usually as big as your policy. So now in terms of GPU memory, you're paying twice the cost of your language model right now you might say okay, why can't we use DPO well DPO is well suited for like pairwise comparisons like Bradley Terry comparisons. It's not so good if what you want to do is let's say do reinforcement learning on math questions and check whether or not the answers are correct right there's no pairwise structure so to speak inherently there so maybe DPO is not great right.

**中文**: 我将采用的方法是训练价值估计器。但你预期会看到整体奖励呈上升趋势，包括奖励模型的输出以及负KL奖励等指标则会逐渐下降。这再次属于上下文赌博机（contextual bandit）问题，因此我预期会观察到较为合理的训练曲线，而非像某些强化学习方法那样剧烈波动。  
好的，我已快速介绍了PPO（近端策略优化），虽属走马观花，但希望你已大致理解PPO的含义，并意识到其实际部署有时颇具挑战性。正因如此，许多人致力于寻找PPO的替代方案。我们的目标是将这些强化学习算法应用于PPO原本适用的场景——即存在明确奖励信号的一般强化学习任务。但我们不希望引入复杂的实现过程；更重要的是，我们希望摒弃价值模型，因为若真正尝试实现PPO，价值模型往往令人十分头疼：其参数量通常与策略模型相当。这意味着在GPU显存占用方面，当前需承担两倍于语言模型本身的开销。此时你或许会问：“那为何不直接采用DPO呢？”——DPO确实适用于布拉德利-特里（Bradley-Terry）式的成对比较任务，但在诸如数学题求解并验证答案正确性的强化学习场景中却并不理想，因为这类任务本身并不天然具备成对比较结构，故DPO可能并非最佳选择。

## 段落 40

**英文**: DPO also you know originally is is kind of an offline algorithm in a sense in that it has a whole bunch of sort of pairs that you initially collect and you just update your model on those you could make it online by iterating but that's not usually the way in which people apply DPO. So then now this brings us to the new hotness which I think is GRPO right so GRPO is actually very very simple both in motivation and in actual implementation so where you start with is conceptually you start at pPO right you start with very similar pieces. You think about kind of this clipping thing you think about policy updates in very similar ways. But what you do is you remove the really complicated generalize the advantage estimation you just get rid of it completely and you replace it was something that is much much simpler right so what is the much simpler thing well we are going to replace the advantage which used to be this like GAE thing it was the sum over returns and had the value function in there instead it's going to be this equation three. At the bottom of this slide here and what is this the advantage of you know response I is equal to the reward that response I received minus the mean of the responses within my group and I'll define what a group is an improvement and then divided by the standard deviation of the rewards within the group so this is a Z score if you know what that is of the rewards within the group right now what is a group a group is a very in some sense natural object for language model RL you have an input question let's say I have a group of this math problem right that is a group and I have many different candidate responses I have capital G different responses and those are all the responses within my single group right so the nice thing is if you think about it right maybe problems are harder or easier right you know some are harder than others and because of that the average reward that my sort of other samples receive is a natural baseline for myself and people have explored exactly these kinds of algorithms if you look up like reinforcement learning with leave one out or sorry policy gradients would leave one out this is the baseline in action minus the standard deviation piece right here the yes. yes the group that we are finding while you are like so you said it's like they don't have a vast amount of questions right and then there are answers to it which are reported so like each like answer only correspondingly one new question or maybe like multiple like kind of like trajectories of the same to say multiple answers per question and that's how you get kind of variance reduction and also like multiple answers for each of the questions is that really for each question no no so for each question q g r p o samples a group of outputs right so so I mean this is not batch like you're normally you would if you were doing this for real you would have multiple questions and for each question you'd have G responses and those would get baseline together across questions you would have no baseline or any interaction really other than the policies get updated together okay so the baseline is only within the same question and that's why the baseline makes sense right because a question is like hard or easy. and the mean of that that reward is in sometimes capturing the question difficulty and you're subtracting that guy out okay good and the other thing this is a fun note I'm going to mention it because I just kind of think this is fine and cool this dkl if you've seen lots of kale divergence computations this is actually a little non-standard right because the natural kale divergence estimate is actually just you take a bunch of samples and then you compute the average of the log ratio which is kind of this inner term over here right but this one from from g r p o actually has these two extra terms it has the log or it has the ratio of pi ref over pi theta and it has this minus one and you can kind of convince yourself that if you take the expectation of this with respect to pi theta you know this is just going to be canceling with this one over here so this is a control variant scheme that reduces the variance of this kale divergence estimate which is cool because maybe you all need to estimate kale divergence from samples this equation is just a slightly nicer way of estimating that exact same thing okay and g r p o is really nice one last note if you're only taking one step which is doing the pure online case right all this clipping stuff just kind of disappears. and all you're doing is policy gradients right policy gradient is just uploading good stuff and down waiting bad stuff where the rewards that I'm multiplying the policy gradients by is this a of i so it's just like an incredibly simple algorithm for the truly online case where you're not doing multiple steps on a single example so there's multiple kind of different repositories for g r p o implementations on I can point you to several including this one that I sort of copied and put on to the slide here but basically I can just say that it's exactly the way you think it would go sort of in the outer loop you would compute the reward for each rollout you'd normalize for each group the mean and the variance of the rewards you'd compute the kale term in this case per sequence which isn't quite right for the more heavyweight implementations and then you do grading updates on the loss and this is one example of the loss computation that you also have here and the advantage computation unlike in the p p o case is just really really simple this is almost exactly line for line the equations that I showed you with just one minor difference that's not shown in the equation which is that you know as you do in almost everything because we're dividing by the standard deviation you have a tiny fudge factor of 1 e negative 4 to make sure it doesn't numerically blow up on you right and you'll have to do this in the assignment as well you'll have to add a little epsilon to your g r p o setup. so how well does this work? g r p o works pretty well this is from the original deep seek math paper and I'll get back to this later because it is interesting to look at this plot and this result in light of later deep seek r1 results they show essentially the two fine tuning based methods rft and online rft this is basically a fairly weak baseline I would say where what you're doing is you're only looking at examples that sort of get the right answer they're doing math in this case so you only get examples where you get the right answer and you fine tune on your own outputs that got the right answer reinforcing correct answers with fine tuning. g r p o without cut level rewards where you only get a correct or not answer is the yellow the blue one is process level rewards where it's kind of you got a system that looks at each step of your reasoning and sort of gives you a grade for that and they're arguing you know maybe process rewards are better we'll talk a little bit more about that later but in either case you see that g r p o works and it works pretty well.

**中文**: DPO（直接偏好优化）大家也都知道，本质上最初是一种离线算法：它需要预先收集大量成对样本，然后仅基于这些样本更新模型；虽然理论上可通过迭代方式将其改为在线形式，但实践中人们通常并不这样使用DPO。接下来我们就进入当前的热点——我认为是GRPO（群组相对策略优化）。GRPO无论在动机上还是实际实现上都极为简洁。其出发点在概念上与PPO（近端策略优化）非常相似：同样采用裁剪机制，策略更新方式也高度类似。但关键区别在于，它彻底摒弃了原本极其复杂的广义优势估计（GAE），代之以一种简单得多的方法。这种更简单的方法是什么呢？我们将原先那种包含回报累加与价值函数的GAE式优势，替换为本页幻灯片底部公式（3）所定义的形式：即第i个响应的优势，等于该响应所获奖励减去其所在组内所有响应奖励的均值（我随后将明确定义“组”的含义），再除以该组内奖励的标准差——这实际上就是组内奖励的Z分数。那么，“组”又是什么呢？对语言模型强化学习而言，“组”是一个非常自然的概念：给定一个输入问题（例如一道数学题），所有针对该问题生成的不同候选回答便构成一个“组”；假设有G个不同回答，它们就全部属于该单一问题所对应的组。这一设计的巧妙之处在于：不同问题难度各异（有些更难、有些更易），因此，同一组内其他样本所获得的平均奖励，自然成为评估自身表现的合理基线。事实上，学界已有人探索过此类算法——比如“留一法强化学习”或“留一法策略梯度”，其核心正是以组内均值作为基线，再减去标准差项（没错，正是此处所示）。我们所定义的“组”，正如您所说，并非海量问题集合，而是每个问题对应一组被报告的答案；每个答案唯一对应一个问题，或者更准确地说，是同一问题下生成的多条推理轨迹（即每个问题对应多个答案）。这种设置既实现了方差缩减，也确保了每个问题均有多个答案。需要澄清的是：并非每个问题只对应一个答案，而是对每个问题q，GRPO会采样一组输出（共G个响应）；换言之，这不是传统意义上的批量处理——实际应用中，您会拥有多个不同问题，对每个问题分别生成G个响应，且基线计算仅在各自问题内部进行（跨问题间无基线共享或任何交互），仅策略参数会联合更新。因此，基线严格限定于同一问题内部，这完全合理：因为问题本身具有固有难度，其奖励均值恰好能反映该难度特征，而我们正是通过减去该均值来消除难度偏差。此外，还有一个有趣细节值得指出（我提一下，因为我觉得这点既合理又酷）：关于KL散度（DKL）的计算——如果您接触过大量KL散度估算，会发现此处写法略显非标准。通常，自然的KL散度估计就是采集一批样本，然后计算对数比值（即此处内层项）的均值；但GRPO中的这一版本额外增加了两项：一项是π_ref与π_θ的比值的对数，另一项是减1。您可自行验证：若对该式关于π_θ取期望，则这两项恰好相互抵消。因此，这是一种控制变量技巧，旨在降低KL散度估计的方差——这很有价值，因为我们在实际中往往需从有限样本中估计KL散度；该公式只是对同一目标量的一种更优估计方式。最后补充一点：GRPO非常友好。若您仅执行单步更新（即纯粹的在线场景），所有裁剪操作都会自然消失，此时您所做的就纯粹是策略梯度更新——即根据奖励信号提升好行为、抑制坏行为，而此处策略梯度所乘的奖励正是前述A(i)优势值。因此，在真正在线、不对单个样本进行多步更新的场景下，GRPO堪称一种极其简洁的算法。目前已有多个GRPO开源实现库，我可以为您推荐若干，包括幻灯片中引用的这个（我稍作调整后放入此处）；简言之，其流程完全符合您的直觉：外层循环中，先为每次采样 rollout 计算奖励；再对每组响应归一化其奖励的均值与方差；接着按序列计算KL项（注意：此做法在更重型的实现中未必完全严谨）；最后基于损失函数执行梯度更新。此处展示的即为一种损失计算示例，而优势计算（不同于PPO）则异常简单——几乎与前述公式逐行对应，唯一未在公式中体现的细微差别是：由于需除以标准差，我们引入了一个极小的扰动因子1e-4，以防数值计算时出现溢出（您在后续作业中也需如此处理：务必在GRPO实现中加入微小的ε值）。那么，GRPO效果究竟如何？效果相当出色。这是源自DeepSeek-Math原始论文的结果，我稍后会再次回看此图，因为结合后续DeepSeek-R1的研究成果重新审视该图表与结果将十分有趣。图中展示了两种基于微调的方法：RFT（监督式微调）与在线RFT（Online RFT），我认为这基本属于较弱的基线方法——其做法仅选取“答对”的样本（本例中为数学题正确解答），并仅用模型自身生成的正确答案进行微调，即通过微调强化正确答案。而GRPO在仅提供“正确/错误”二元奖励（无截断层级奖励）时的表现由黄色曲线表示；蓝色曲线则对应过程级奖励（即构建一个系统，对推理过程的每一步进行评分），作者认为过程奖励可能更优——我们稍后会进一步探讨这一点。但无论如何，您都能看到GRPO表现优异，且效果显著。

## 段落 41

**英文**: Any questions about the basic g r p o piece before we move on to kind of details about g r p o and thinking kind of deeply about what's actually happening in the algorithm. So now let's think about the difference between g r p o and p p o and what we've done and what's different. So really there's you know as I was saying only one difference although it's a really important difference it's replacing the advantage estimator with this thing right the mean or the z score let's say of the rewards. And so now I'm going to kind of go back to the policy gradient theorem or the policy gradient result and I'm going to think through this result with you right so when we take a policy gradient update right what can we do so I'll just go back couple slides right. On this slide we've got right here at the very top policy gradients this is the most basic rl algorithm that you can do right you take gradients where you multiply the log probability gradients with the reward right this is something that I'm always allowed to do this is a mathematical equivalence right. Now one other thing I can do is called baselining I can take this reward z and I can subtract any constant or any in fact any random variable that doesn't depend on a z itself and this would still be a valid policy gradient right so so this baseline thing is really important because what you're going to try to do is you're going to try to subtract constants that give you lower variances on this expectation right so that's called baselining. And if we go here you know that's kind of a classic result that you can look up in Sutton and Bartow they say okay look we've got the policy gradient you can subtract out any baseline D of s because B of s you know when we sum it up across the policy is going to be zero right. So this is fine right we can always baseline but let's look at this a of a of i is this a baseline well we're subtracting the mean and that's kind of a baseline because all the other rewards are not dependent on r of i so maybe that's okay I mean technically this notation includes r of i but if I remove that that's a valid baseline but one thing that's really weird is I'm dividing by the standard deviation here right that that's not something that really seems allowed according to this derivation in Sutton and Bartow. And that turns out to be a problem some folks that have gone and you know reanalyze your PO and its behaviors basically argue that grpo has two things that at least mathematically are a little bit off and the first thing is this division by standard deviation right as I was kind of talking you through just now this breaks that sort of you know contract that a baseline just needs to be subtracting a zero mean variable that's you know independent of my draw. And the other thing that grpo does which I did it kind of I'm sorry a gloss over when I previously presented it is that it's actually dividing kind of the rewards by the length of the output and that's going to have that's also a little bit weird according to the policy gradient theorem this is not something that would naturally show up.

**中文**: 在进入关于GRPO的细节以及深入思考该算法实际运行机制之前，大家对GRPO的基本框架还有任何疑问吗？接下来，我们来探讨GRPO与PPO之间的区别，以及我们所做工作的差异所在。实际上，正如我之前所指出的，二者之间仅存在一个区别——尽管这一区别至关重要：即用奖励的均值（或z分数）替代了优势估计器。现在，我将重新回到策略梯度定理（或策略梯度结果），并和大家一起逐步推导分析这一结果。当我们执行一次策略梯度更新时，具体能做些什么呢？我先回溯几张幻灯片。  

本页幻灯片最上方展示的是策略梯度公式——这是最基本的强化学习算法：我们计算梯度，即将对数概率的梯度与奖励相乘。这一操作始终成立，属于严格的数学等价关系。  

另一项可进行的操作称为“基线法”（baselining）：我们可以对奖励\(z\)减去任意常数，甚至减去任意一个不依赖于动作\(a_z\)的随机变量，所得结果仍构成有效的策略梯度。因此，“基线”概念极为关键，因为我们的目标正是通过减去恰当的常数，以降低该期望值的方差——这正是基线法的要义。  

如幻灯片所示，这是一个经典结论（可参阅Sutton与Barto的著作）：在策略梯度中，我们可以减去任意状态相关的基线函数\(b(s)\)，因为当对策略进行求和时，\(b(s)\)的期望贡献为零。因此，这种基线处理完全可行。  

那么，此处的\(A_i\)（即\(r_i\)的均值）是否构成一种基线呢？我们减去的是奖励均值，这在某种意义上确实属于基线——因为其余所有奖励均不依赖于当前的\(r_i\)；严格来说，该记号虽包含\(r_i\)本身，但若将其剔除，则所得基线完全有效。然而，此处真正异常之处在于：我们还除以了标准差。根据Sutton与Barto的推导，这种操作显然并不允许。  

事实证明，这的确是一个问题。一些研究者曾重新分析GRPO及其行为，并指出GRPO至少存在两个在数学上略显瑕疵之处：其一便是上述除以标准差的操作——正如我刚才所分析的，这违背了基线的基本要求，即仅需减去一个与采样动作无关、且自身期望为零的变量；其二则是GRPO（在我先前介绍时曾略过这一点）实际还将奖励除以输出长度，而依据策略梯度定理，这一操作同样缺乏理论依据，并不会自然地出现在标准推导之中。

## 段落 42

**英文**: And so these authors who did like this this pretty interesting study of grpo algorithms kind of argue that maybe we should just get rid of these two things and if you do then you'll actually have much shorter output length and higher reward without having much longer responses. And so let's talk about these results you know carefully for each one of these two fixes and hopefully by talking through these you will gain an intuition about how the rl algorithm works right so first I want to talk about the standard deviation this ones maybe somewhat obvious what it's doing. Okay let me go back because I think it's easier to talk about when I'm highlighting the equation here so I'm dividing the advantage by the standard deviation so what does that mean when the standard deviation is small right the reward is going to be amplified it's going to be more important for me to optimize that group when the standard deviations are small right and when is the standard deviation small well it's when the problem is too easy or it's too hard right because that's when the rewards are either all zero or they're all ones and so there's a bias in the standard deviation. So the standard deviation term that up weights problems that are too easy or too hard the authors are you this slows convergence maybe true at the very least it certainly breaks the validity of the policy gradient. The second thing which is subtle but also interesting is the length normalization. Now let's kind of look at what's happening here so we have this length normalization before the grpo reward now what does that do if my model got a question wrong right then the best thing to do is I'm receiving negative reward in here so the best thing to do is to make sure that you're going to get the best of the best of the best of the time. So I think the response really really long and if I get the answer right the best thing to do is to make the answer short so I can sort of maximize my positive reward right so what this does is it actually produces a model that kind of be as as aggressively as possible if the model thinks it can't get the answer right it just produces the longest possible response which is a very very bad incentive for you to give the model. And so if you fix this what happens is you know on various sort of toy tasks like gsm a k and so on you you can get a reward that's just as good the red one is sort of the modified version but the output length doesn't keep growing and growing and growing and growing and sort of like stabilize the certain point right. And so there is actually some interesting observation that like maybe some of the really long cot's that people are seeing in things like grpo are a result of these like actual implementation details and choices rather than inherently long cot's being a necessary part of the performance of these models and I think that's like a very interesting although not fully proven out class of hypotheses. Cool okay so that's the grpo algorithm hopefully now you're all familiar with it and I think you now have the background to go through all three of these papers now deep ckr1 kimi1.

**中文**: 因此，这些作者开展了一项颇为有趣的关于GRPO算法的研究，他们提出：或许我们应直接摒弃上述两种机制；若果真如此，模型输出长度将显著缩短，奖励值反而更高，且响应长度并不会因此大幅增加。接下来，我们需仔细分析这两项改进各自带来的效果；通过逐一探讨，你将对强化学习算法的运行机制形成直观理解。首先，我们来讨论标准差项——其作用或许相对明显。为便于说明，我先回到公式本身：此处我将优势函数除以标准差。这意味着，当标准差较小时，对应奖励会被放大，从而促使算法更优先优化该类样本；而标准差何时会变小？恰恰出现在问题过于简单或过于困难时——此时所有奖励要么全为零，要么全为一，因而标准差天然存在偏差。换言之，标准差项会过度加权那些过于简单或过于困难的问题，作者指出这可能拖慢收敛速度；即便暂不考虑收敛性，它至少已破坏了策略梯度的有效性。第二项改进虽更为微妙，却同样引人深思：即长度归一化。让我们观察其具体作用：当前在GRPO奖励计算前引入了长度归一化。若模型答错问题，此处将获得负向奖励，此时最优策略便是尽可能延长响应——以期“竭尽全力”；反之，若答对问题，最优策略则是尽量缩短回答，从而最大化正向奖励。结果便是：模型演化出一种极端倾向——一旦判断自身无法答对，便本能地生成尽可能长的响应；这对模型而言实属极差的激励机制。而一旦修正此项缺陷，在GSM8K等各类简化任务上，我们仍可获得与原版（红色曲线）相当的奖励水平，但输出长度不再持续膨胀，而是趋于稳定。由此产生一个颇具启发性的观察：人们在GRPO等方法中所见的冗长思维链（CoT），或许并非模型性能提升所必需的内在特性，而更多源于具体实现细节与人为选择；尽管这一假说尚未被完全证实，但它无疑开辟了一个极具价值的研究方向。很好，以上便是GRPO算法的全部内容；希望此刻你已对其了然于心，也具备了深入研读后续三篇论文——DeepCoder、R1及Kimi1——所需的知识基础。

## 段落 43

**英文**: 5 and a 23 any questions. Yes. So what's your ballpark where the operating is just to confirm my ground I guess this is that because like those kind of very easy or the very part cases we want to actually like aggressive the top plate right the ballpark. Yeah I think this gets into sort of wishy washy folk theorem territory but actually some of these papers will talk about this folk theory and so I'll mention it which is what you really want the rl algorithm to do is to get problems that it can be done. Like do somewhat well on like you can get summer ward on but is not so easy that it can like already solve them right so there's like kind of a curriculum effect where you want to feed the models the right you know level of difficulty and if you're maximizing the that standard deviation that's kind of the wrong direction right you're really maxing out the stuff at the extreme that you either already all know or just way too hard for you to solve. Cool okay so we're going to talk about all three of these papers today r1 kimi1. 5. And kimi1. 5 and kimi3. R1 and k1.

**中文**: 5 和 23，有问题吗？有。那么，您大致的设想是什么？运营层面只是为了确认我的理解——我想这大概是因为，对于那些非常简单或极端特例的情况，我们实际上希望更积极地优化顶层策略。没错，我认为这已进入某种模糊不清的“民间定理”范畴；但事实上，部分论文确实会讨论这一民间定理，因此我将简要提及：强化学习算法真正需要完成的任务，是解决那些具有一定挑战性、但又并非无法攻克的问题——即表现尚可（例如能获得一定奖励），却又不至于过于简单而被算法轻易解决。这就类似于一种课程学习效应：我们需要为模型提供难度适中的任务；而若一味最大化标准差，则方向恰恰相反——此时你所放大的，恰恰是两类极端情形：一类是你早已完全掌握的，另一类则远超你的求解能力。很好，明白了。今天我们将讨论这三篇论文：R1、Kimi1.5 和 Kimi3；其中 R1 和 K1……

## 段落 44

**英文**: 5 I think are pretty interesting because they came out roughly the same time sadly R1 was the only one to get like a gigantic social what's it called reception. And both of these actually show how to do rl based reasoning on like math and other things with llm's right and they because they're contemporaneous you can kind of see almost two parallel ways to tackle the same problem like which things are similar which things are different and so on which is great. KuiN 3 is the newest of the releases and they do some fairly interesting variations of ideas in r1 and also they have some new kind of tricks that r1 doesn't have which I think are pretty interesting things to look at especially if you're interested in things like inference efficiency of reasoning models. So I'll start with r1. I think r1 is kind of amazing in the sense that it's a archive paper that launched a whole social phenomenon never let your advisors tell you that your archive paper is will never matter you know this one. And then the last one was almost half a billion dollars of n video valuation you two can wonder maybe cause that kind of a way. R1 I think is quite remarkable because you know it in many ways replicates all of the qualitative properties of the oh one recipe in a way that is extremely simple. The key properties that I want to talk through and you know make sure you all understand. The first thing right is that you know it hits the performance targets the open a I01 set everyone was really excited about reasoning model so this is very exciting. The second thing is that you know it opens up a RL recipe that is not only just like a replica one but I think more importantly one that is extremely simple right it doesn't have any search it doesn't have any process reward models I think lots of people at the time thought maybe we will be able to do it.

**中文**: 5 我认为这些工作相当有趣，因为它们大致在同一时期发布；遗憾的是，R1 是其中唯一一个引发了巨大社会反响（即所谓“轰动效应”）的模型。实际上，这两项工作均展示了如何在数学及其他任务上，利用大语言模型（LLM）开展基于强化学习（RL）的推理。由于二者几乎同期问世，我们得以直观地看到解决同一问题的两种并行路径——哪些方面相似、哪些方面不同，等等，这非常有价值。KuiN-3 是最新发布的模型，它在 R1 的思想基础上进行了若干颇具启发性的变体，并引入了一些 R1 所不具备的新技巧；我认为这些新技巧尤其值得关注，特别是当你关心推理模型的推理效率等问题时。因此，我将首先介绍 R1。在我看来，R1 非常了不起——它是一篇预印本论文，却掀起了一场席卷全社会的现象；请永远别让导师告诉你：“你的预印本论文永远不会产生影响”，这篇就是反例！而最后一点是：英伟达估值飙升近500亿美元，你们或许会猜想，这背后是否也有类似因素在起作用。我认为 R1 确实非同寻常，因为它以一种极为简洁的方式，在诸多方面复现了 OpenAI “o1” 模型的核心方法论。接下来，我将重点阐述其关键特性，并确保大家充分理解：第一，它成功达到了 OpenAI o1 所设定的性能目标，而该目标一经公布便令整个推理模型领域为之振奋，因此这一点令人格外兴奋；第二，它提出了一套全新的 RL 方法论，这套方法不仅效仿了 o1，更重要的是，其设计极其简洁——无需搜索过程，也无需过程奖励模型；当时许多研究者甚至认为，这类方法或许根本无法实现。

## 段落 45

**英文**: So I thought maybe we need all these complicated pieces to get reasoning models R1 really shows you don't need any of that right and then finally there's lots of interesting insights about the interaction between supervised fine tuning and RL that I think continue to be really important. So the starting point of R1 is they build on deep seek map and actually some of the equations I showed you of GRPO are from deep seek math where they originally proposed. GRPO as an alternative simpler or system more systems efficient variant of PPO right to them actually the most important piece was they wanted to get rid of the value model just because it's really annoying to have around. But one thing that's really interesting is is you know they actually go for this yellow line thought the outcome supervision which is not actually the best performing model in deep seek math I'll talk about that again at the very end of this this section here. So I'll walk through all the different pieces of R1 so I'll start with R10 which I think of as the controlled setting right so R10 is a very pure form of RL learning it basically takes what essentially the model that is pre trained plus mid trained. Before doing any RL like Jeff or instruction tuning and then throws it into the math RL loop and then they try to find out how well does that do right. So the details here how do they do the reinforcement learning while they have a bunch of sort of math ish tasks the data is not public they take deep seek V3 as their kind of base model and then their rewards there's two forms of rewards that they use one of them is an accuracy reward so like did it get the math question. Correct right it's a correct or not reward it's binary they have a format reward that basically forces the model to put it's COT within like thinking tax like thinking start thinking end of thinking tax right and that's important if you want your model to have these like long COT's being used. Now the format reward feels like something that doesn't matter but from many papers and from having talked to many people apparently it is a pretty critical part of actually getting this whole like reasoning RL think the work. Once you do this right all they're doing is is doing RL on top of the base model nothing very fancy but the results are are pretty striking they get performance that is getting pretty close to open a I01 by just doing some RL on top of the model that they already had right.

**中文**: 因此，我原本以为可能需要所有这些复杂的组件才能实现推理模型；但R1实际上表明，你根本不需要其中任何一项。最后，关于监督微调（SFT）与强化学习（RL）之间相互作用的诸多有趣洞见，我认为至今仍极为重要。R1的起点是建立在DeepSeek-Math基础之上，而我之前展示的部分GRPO公式实际上就源自DeepSeek-Math——正是他们在该工作中首次提出了GRPO，作为一种比PPO更简洁、系统效率更高的替代方案。对他们而言，最关键的一点是希望彻底摒弃价值模型（value model），只因该模型实际部署时非常烦人。但一个非常有趣的现象是：他们实际采用的是图中黄色曲线所代表的“结果监督”（outcome supervision）方法，而该方法在DeepSeek-Math中并非性能最优的模型——这一点我将在本节末尾再次详述。接下来，我将逐一解析R1的各项组成部分。首先从R10讲起，我将其视为一种受控设定：R10是一种极为纯粹的强化学习形式，其基本思路是直接采用预训练加中期训练（mid-trained）后的模型（即尚未进行任何类似JeF或指令微调的RL前处理），然后将其直接投入数学任务的RL训练循环中，进而评估其实际效果。具体实施细节如下：他们使用了一批偏数学类的任务（相关数据未公开），以DeepSeek-V3作为基础模型；奖励机制则包含两类：其一是准确率奖励（accuracy reward），即判断模型对数学题的回答是否正确——这是一种二元的“正确/错误”奖励；其二是格式奖励（format reward），强制模型将思维链（CoT）严格置于“思考开始”与“思考结束”标记之间。若希望模型能有效生成较长的思维链，这一约束至关重要。尽管格式奖励看似无关紧要，但大量论文及与众多研究者的交流均表明，它实为整个“推理型强化学习”得以成功的关键环节之一。一旦完成上述设置，他们所做的仅是在基础模型之上单纯运行强化学习，技术上并无特别花哨之处；然而结果却极为显著：仅通过在既有模型上施加强化学习，其性能已十分接近o1。

## 段落 46

**英文**: Without any like you know COT fine tuning or anything like this and there's kind of two things that they know in their paper as being really interesting about R10. I want to talk about this because I think it's important to carefully examine what is happening in R10 so the first thing they say is oh it's very cool that if you just let the model do RL on this this verifiable rewards the length of the COT just kind of increases like pretty predictably and you know that the and you know in commentary that I don't know if I necessarily agree with you know in the paper they're like oh it's learning to solve harder and harder problems by thinking harder and harder it's like well maybe. They also you know point out it's kind of cool that you know they learn you know phenomena like backtracking they call this the aha moment I think much has been made about this in sort of public discourse that wow it's cool that RL training can give models these kind of emergent insights. I'll kind of refer you back to the doctor GRPO paper the one that was talking about the corrections to the GRPO and I think they have honestly pretty good and interesting arguments that both of these are not particularly interesting phenomena like first of all they argue that like the length just goes up because of the biased objective not because you know it's an inherently interesting object. And second they argue well if you just run deep seek V3 on a bunch of math questions it'll also sometimes output things like aha I can do this or that which is maybe not like a deeply new phenomenon that arises from RL you know both of these seem like given more recent evidence kind of credible things that like maybe there's nothing like emergent and special about R10 but it is actually working very well but it is a good math model. So R10 you can think of as kind of a research setting right they're taking a controlled model they're doing something very controlled on top of it which is math RL and they get a good model out right but if you're trying to build a really strong model that you're going to ship to the world this is not what you're going to do right you're going to like basically do everything that you can to get the best model that you can right so what would you do in that sort of more unrestricted setting well you're going to maybe insert some supervised fine tuning you're going to take. So you know COT's from you know some undisclosed source and you're going to fine tune your deep seek model on that before you do your RL and after you do that right you don't want your model to be kind of like this math so that can't do anything else so you're going to you know apply your usual post training pipeline on top of that to make sure that it can do all the other tasks that you know people normally want to use these models for right so this is kind of the pipeline differences and so the key differences. Both within the pipeline and within the RL is they do SFT initialization to try to get the model to know how to do long COTs without starting with RL they add a language language consistency reward in order to make sure that the chains of thought remain in a single language and then they do you know a secondary RLHF stage kind of at the end. So I think this makes a lot of sense right every time you want to do something advanced like reinforcement learning you're probably going to start by doing a little bit of supervised fine tuning and so even in sort of reasoning models or long COT models like deep seek R1 this is the case right you start with long COT supervised fine tuning data and then you're going to you know do RL. I will point out that the description of where they get this data and what this data is remains very very vague like they don't tell us like the was the COT data derived from how did they filter it I don't really have any idea based on reading the R1 paper.

**中文**: 无需任何类似思维链（COT）微调或其他此类技术，他们在论文中指出了R10的两个特别引人关注的现象。我想就此展开讨论，因为我认为有必要审慎考察R10中实际发生的情况。首先，他们指出：若仅让模型在这一可验证奖励机制下进行强化学习（RL），其思维链长度便会相当可预测地持续增长——这看起来非常酷。但文中评论（我未必完全认同）称：“模型正通过越来越深入的思考，学会解决难度逐级提升的问题。”对此，我们或许应持保留态度。其次，他们还强调了一个颇为有趣的现象：模型在训练中自发学会了“回溯”等行为，他们称之为“顿悟时刻”（Aha Moment）。在公开讨论中，这一点常被大加渲染，认为“强化学习训练竟能赋予模型这类涌现式洞见”，令人惊叹。不过，我建议大家回顾一下关于GRPO修正的那篇博士论文；该文提出了相当扎实且富有启发性的论点，指出上述两种现象其实并无特别之处：首先，思维链长度的增长源于目标函数本身的偏差，而非因其本身具有某种内在趣味性；其次，若直接用DeepSeek-V3模型回答大量数学题，它有时也会输出“啊哈！我可以这样解”或类似表述——这或许并非源于强化学习所催生的某种深刻新现象。基于近期更多证据来看，这两种现象似乎都缺乏真正的“涌现性”与特殊性；R10之所以表现优异，很可能只是因为它本身就是一个优秀的数学模型。因此，可将R10视作一种研究型设定：研究人员采用一个受控的基础模型，在其上施加高度可控的数学领域强化学习，最终获得一个性能良好的模型。然而，倘若你的目标是构建一个真正强大、即将面向全球用户发布的模型，这种做法显然并不适用——你必然会竭尽所能，以获取最佳可能的模型。那么，在这种更开放、不受限的场景下，你会怎么做呢？你很可能会引入监督微调（SFT）环节：例如，从某个未公开来源获取思维链数据，先对DeepSeek模型进行监督微调，再开展强化学习；完成强化学习后，你也不会希望模型变成一个只会做数学题的“单功能模型”，因此还需在其基础上应用常规的后训练流程，确保其能胜任用户通常期望的各类任务。这便是整体流程的关键差异所在。具体而言，无论在训练流程内部还是强化学习环节内部，他们都做了三点关键调整：一是采用监督微调（SFT）初始化，使模型在启动强化学习前即掌握长思维链生成能力；二是增设语言一致性奖励（language consistency reward），确保思维链全程保持单一语言；三是在流程末尾额外增加一轮基于人类反馈的强化学习（RLHF）阶段。我认为这种设计十分合理：凡涉及强化学习等进阶技术时，通常都应先进行一定量的监督微调。事实上，即便是DeepSeek-R1这类专注推理或长思维链能力的模型，也遵循此范式——先使用长思维链监督微调数据进行训练，再辅以强化学习。需要指出的是，论文对监督微调所用数据的来源及具体构成描述极为模糊：既未说明思维链数据如何生成，也未交代筛选标准——仅通读R1论文，我实在无法获知任何相关信息。

## 段落 47

**英文**: The claim to benefit of this is that if you see the model on sorry if you SFT the model on long but say English COTs then this gives you an interpretability benefit right as you do RL you're not going to get like we are gibberish it's going to kind of keep the model closer to these like more interpretable COTs that you started out with. So and that would be kind of good for users right like as you're using a math model will be nice to see its reasoning as it goes. And an additional thing right so when they do SFT initialization they use a ton of data but one really interesting thing is that for a lot of models even a tiny amount of SFT on these kinds of like long COT data can be good one thing that some of my students in collaboration with Percy we did was basically take a bunch of long COTs from Gemini 2. 0 Flash thinking and you know fine tune quite 2. 5 and maybe surprisingly you know with just the thousand of them. You know with just the thousand examples you know you get really really high you know math benchmark accuracy with just a little bit of long COT fine tuning so I think both of these are really pointing to the fact that the base model already has a lot of kind of like thinking capabilities that you're just kind of priming and extracting from the model and after that of course you're going to do RL right. So after you've gone the model setup with SFT you know as with kind of the instruction tuning and RLHF pipeline so you start with SFT and then you do RL to basically get the model to actually optimize the rewards you're looking for. The RL part is basically the same as R10 not not huge differences but with a minor difference that you're going to add a language consistency loss and they have I think I think this note is pretty interesting this is a minor no but I'll describe it anyway. Where they basically say you know they add this like language consistency reward because you know during the training process if they just let the model RL they find that actually the COT will language mix like it will switch between languages and if many of you have kind of seen people playing with reasoning models I've seen people on the internet post things like oh it's kind of weird that like grok 3 like suddenly switches to Chinese and the COT and this is kind of consistent with those kinds of things that if you aggressively RL a model like actually the RLHF pipeline is going to be a little bit more difficult to do. So like actually you know there's natural tendencies for models to language mix rather than staying in a single language so it actually requires an additional reward to keep it in the single language.

**中文**: 这样做的好处在于：如果你在长篇英文思维链（CoT）数据上对模型进行监督微调（SFT），那么在后续强化学习（RL）阶段，模型将更倾向于保持初始训练时所具备的、更具可解释性的思维链形式，而不会退化为难以理解的“胡言乱语”。这对用户而言显然很有价值——例如使用数学模型时，能清晰看到其逐步推理过程，体验会更好。

此外，在SFT初始化阶段，他们虽使用了海量数据，但一个非常有趣的现象是：对许多模型而言，仅需极少量针对长篇CoT数据的SFT，即可带来显著提升。例如，我和佩西（Percy）合作指导的一些学生曾尝试直接采用Gemini 2.0 Flash的大量长篇CoT样本，对Qwen2.5模型进行微调；令人意外的是，仅用约一千个样本，模型在数学基准测试上的准确率就已大幅提升。这表明：基础模型本身已具备相当强的推理能力，SFT的作用主要是激发并提取这些固有潜能。当然，完成SFT后，还需进入RL阶段——即沿用常规的指令微调与基于人类反馈的强化学习（RLHF）流程：先做SFT，再通过RL使模型真正优化你所设定的目标奖励。

RL部分整体与R10类似，差异不大，但有一个细微调整：额外引入了一项语言一致性损失（language consistency loss）。此处的说明颇为有趣（虽属细微调整，仍值得说明）：他们之所以加入这一语言一致性奖励，是因为在纯RL训练过程中发现，若不加约束，模型生成的思维链极易出现语码混用现象（即在不同语言间随意切换）。许多接触过推理模型的用户可能已有体会——网上常有人发帖称：“Grok-3的思维链怎么突然就切到中文了？真有点奇怪。” 这类现象正与此一致：若对模型施加过于激进的RL训练，RLHF流程反而会变得更难控制。事实上，模型天然倾向于混用语言，而非始终坚守单一语言；因此，必须额外设计一项奖励机制，强制其保持语言一致性。

## 段落 48

**英文**: And then finally after you've done RL on like math and other verifiable domains you basically layer on the usual post training so you do instruction tuning and then you do sort of the pairwise preference tuning afterwards right so they do an SFT step where they combine both RLHF and other RLHF pipeline. Where they combine both reasoning data on non verifiable tasks like write a proof of something these are not verifiable and then so they use their own model as a judge for whether or not they got the answer correct. They have non reasoning data like sort of you know write a nice essay and they use the same SFT data set as what they used for deep seek B3. And then finally for RLHF they actually still use GRPO for RLHF which is kind of cool they use the same RL algorithm for everything and then they basically just follow the V3 RLHF pipeline like there's not really anything different for this post training part right. And how well does it work works very well I think many of you probably experienced this as well right like R1 was in many ways a shock because it matched the O1 performance you know really kind of across the board on a very simple recipe right like as I described this I don't think any of you found any of it particularly surprising. But the outcomes kind of speak for themselves you know you've got sort of on the English tasks basically you know tied or matching O1 on across the board really slightly worse on code models but very very close really across all these different tasks. The final thing that the R1 paper showed is that you can take these big big models and you can distill them into other models you can take your big deep seek R1 and you can take those chains of thought like in their case they take almost a million chains of thought and then they fine tune when with those chains of thought and they actually get big boosts in sort of math performance relative to the base model which is only getting something like 50% performance on Amy for the 32 B model. So you get 25 plus percent boost on this task which is pretty surprising. Cool and then finally there's two I think interesting and good observations from R1 and I think scientifically maybe this was the biggest contribution of R1 in a way. So I think R1 maybe had three contributions scientifically right one of it was it showed that outcome based rewards with GRPO works right it's like the positive proof.

**中文**: 随后，最终在数学及其他可验证领域完成强化学习（RL）训练后，你基本上会叠加常规的后训练流程：先进行指令微调（instruction tuning），再进行成对偏好微调（pairwise preference tuning）。具体而言，他们执行一个监督微调（SFT）步骤，将基于人类反馈的强化学习（RLHF）与其他RLHF流程相结合：一方面整合了非可验证任务（例如“撰写某命题的证明”）的推理数据——这类任务本身不可验证，因此他们使用自身模型作为评判器，判断答案是否正确；另一方面则纳入非推理类数据（例如“撰写一篇优美的文章”），并沿用与DeepSeek-V3 B3模型相同的SFT数据集。最后，在RLHF阶段，他们依然采用GRPO算法——这一点颇具新意：全程统一使用同一种强化学习算法；同时整体遵循V3版RLHF流程，该后训练环节实际上并无显著改动。

这一方法效果极佳。我想在座许多人都已亲身体验过：R1模型在诸多方面堪称震撼，因其性能几乎全面匹敌O1模型——而所依赖的却是一套极为简洁的训练范式。正如我此前所述，其中任何环节想必都未令各位感到意外；但结果本身已不言自明：在英文任务上，R1基本与O1持平甚至并驾齐驱；在代码任务上虽略逊一筹，但差距微乎其微；整体表现覆盖各类任务，均极为接近O1。

R1论文还揭示了另一关键发现：可将此类超大规模模型进行知识蒸馏，迁移至其他模型。例如，以大型DeepSeek-R1模型为教师模型，提取其生成的思维链（chain-of-thought）——文中案例即采集近百万条思维链，再以此对目标模型进行微调。结果表明，相较于基线模型（32B参数模型在AMC测试中仅达约50%准确率），数学能力获得超过25%的显著提升，这一增益颇为惊人。

此外，R1还带来两项颇具启发性且价值突出的观察结论，从科学角度看，这或许正是R1最重大的贡献所在。总体而言，R1的科学贡献可归纳为三点：其一，它确证了基于结果的奖励机制配合GRPO算法的有效性，堪称一项正面实证。

## 段落 49

**英文**: And also had two other kind of like negative results kind of contributions and they are sort of contained in the very last part of the R1 report and they basically say okay like we tried two things like pretty extensively we tried PRMs and we also tried MCTS and neither of those really helped us at all on like replicating something like O1 right and so to get into a little bit of detail right PRMs are basically process reward models those are kind of like systems that can give you in the same way. So when your model is giving a chain of thought a PRM would be able to say oh you went wrong at this step in the middle right and obviously that is much richer and very powerful form of feedback on RL algorithm can make really really good use of a PRM but unfortunately it's also very difficult to get a PRM in the first place. And so you know R1 and the deep seek math people like they had gone down this road of doing PRMs for a while and they kind of concluded this doesn't work quite as well as outcome based rewards and thus far I think outcome based rewards remain the way that in which you would build these models. The second thing that I think hasn't really panned out is search based methods I think lots of people were interested in search based approach as the reasoning thus far at least it hasn't really panned out in the same way that RL now come based rewards has that remains I think kind of the strongest kind of baseline and system in this universe. Okay so any questions about R1 like kind of their setup or any of the other findings yes. No, the GRPO and PRM are kind of two different. Oh, sorry, sorry I said I was going to mention it but I didn't so that is totally on me that's right exactly. And so I think especially for the PRMs I think it's really interesting and telling that in deep seek math you know they were very much convinced by sort of the strength of the PRMs which is this blue line with PS. And then in R1 they you know concluded that actually this approach that had worked for deep seek math was not really going to work for R1 and they had gone without. R1 and they were all based rewards.

**中文**: 此外，还有另外两种类似负面结果的贡献，它们大致包含在R1报告的最后部分。其核心观点是：我们曾相当深入地尝试了两种方法——PRM（过程奖励模型）和MCTS（蒙特卡洛树搜索），但二者均未能帮助我们成功复现类似O1的效果。具体而言，PRM本质上是过程奖励模型，这类系统能以更精细的方式提供反馈：当模型生成思维链时，PRM可精准指出“你在中间某一步出错了”。显然，这种反馈信息更丰富、更强大，强化学习算法若能充分利用PRM，效果将非常显著；但遗憾的是，构建一个高质量的PRM本身极为困难。因此，R1团队及深度求索（DeepSeek）数学团队此前曾长期探索PRM路径，最终却得出结论：PRM的效果远不如基于结果的奖励机制；截至目前，基于结果的奖励机制仍是构建此类模型的主流方式。

第二项未达预期的方法是基于搜索的技术。此前许多人对基于搜索的推理方法抱有浓厚兴趣，但至少目前来看，该方法尚未取得与基于强化学习及结果奖励机制同等的成功。后者至今仍被视为该领域最稳健、最有效的基线方法与系统架构。

那么，关于R1的实验设置或其他研究发现，大家有什么问题吗？  
——没有。GRPO和PRM是两种不同的方法。  
——哦，抱歉，抱歉！我本打算提及这一点，却遗漏了，这完全是我的疏忽。没错，正是如此。  
尤其值得注意的是PRM：在深度求索数学项目中，团队曾高度认可PRM的强大能力（即图中带PS标记的蓝色曲线所示）；但在R1项目中，他们却得出结论：此前在深度求索数学中行之有效的PRM方法，在R1中实际并不可行，因此R1最终放弃了PRM，转而全面采用基于结果的奖励机制。

## 段落 50

**英文**: Yeah thank you for reminding me I was about to forget my promise there. Yes. The line is not for I guess these are what you're saying. You are simply to know how it takes performance. Yeah so yeah I did this note here they basically say if you have laid away the language consistency experiment it results in degradation of the model's performance. But they're going to put it in any way because they prefer to have COTs that are more readable to humans. It's an interesting trade off. There have been lots of research about whether COTs are faithful and they're not truly faithful we kind of know that. But maybe it's better to have a slightly more faithful COT than to have the extra I don't know half percentage point performance on Amy. Yes.

**中文**: 是的，谢谢提醒，我差点就忘了我的承诺。没错。这条线大概不是……我想您说的就是这个意思。您只需了解它对性能的影响即可。是的，因此我在这里做了个笔记：他们基本上指出，如果放弃语言一致性实验，会导致模型性能下降。但他们仍会采用这种方式，因为他们更倾向于让思维链（COT）对人类而言更具可读性。这是一个有趣的权衡取舍。目前已有大量研究探讨思维链是否忠实可靠，而我们大致知道它们实际上并不真正忠实。但或许，拥有一个略微更忠实的思维链，比在Amy上额外获得那不到0.5个百分点的性能提升更有价值。是的。

## 段落 51

**英文**: If you wait here, what is this?. So I mean I'm sure they can do like translations or other kinds of like post processing to make the COTs better. In some ways you might think of opening eye and these other vendors efforts to like summarize the COTs as being very similar right like because the raw COTs are probably much messier and then they probably like sort of rationalize it away. That is one way and I think that is an effective way to get interpretability. But I do think if you're interested in somehow if you aesthetically believe that the raw COT is very important for monitoring and closer interpretability then I do think you do want something like this right. Cool. Okay. So now we're going to move on to Kimi K1. 5. And why do we study this one? If you look at the time sensor when R1 K1.

**中文**: 如果你在这里等待，这是什么？我的意思是，我确信他们可以进行翻译或其他类型的后处理，以提升思维链（COT）的效果。从某种角度看，你或许会认为“开眼”（Opening Eye）及其他厂商对COT进行摘要的努力，与之非常相似，因为原始COT可能相当杂乱，而他们很可能对其进行了某种形式的合理化处理。这是一种方法，我认为它在提升可解释性方面是有效的。但如果你在某种程度上秉持一种审美信念，即原始COT对于监控和更精细的可解释性至关重要，那么我确实认为你需要类似这样的方案。很好。好的。接下来我们将转向Kimi K1.5。我们为何要研究它？如果你查看R1 K1的时间传感器……

## 段落 52

**英文**: 5 released it's kind of contemporaneous. And it achieves very similar results. It does so using outcome based rewards in RL. It doesn't use the same algorithm. It has kind of different details and different interesting insights. And so we can learn about what's the same, what's different and maybe what parts are maybe important in this process. So to show you the headline result before we start just to get you to believe that this is a paper that's worth being discussed here right. Kimi K1. 5 is the dark blue bars here. You can see OpenAI's O1 as the next highest bar.

**中文**: 5 发布了，其时间点大致相近，且取得了非常相似的结果。它通过强化学习中的基于结果的奖励机制来实现这一目标，但并未采用相同的算法，而是在细节和一些有趣的见解上有所不同。因此，我们可以借此了解这一过程中哪些方面是相同的、哪些是不同的，以及哪些部分可能更为关键。为便于大家理解，我们先展示该论文的核心成果，以说明本文确实值得在此展开讨论。图中深蓝色柱状图代表 Kimi K1.5，紧随其后、高度次高的柱状图则代表 OpenAI 的 O1。

## 段落 53

**英文**: So they're beating or matching O1 across the board on a bunch of important tasks. And they do basically things similar to R1. So they do SFT, they do RL, they have a different RL algorithm but they do RL. They also describe their data set construction in a little bit more detail. And this actually gets used in Q1 3 later. So it's worth discussing. So let me talk through data. As Percy said earlier, maybe data is the most important thing in your whole pipeline. And so we should always pay attention when people talk about their kind of data curation strategies in like a large scale training paper. And so Kimi K1.

**中文**: 因此，它们在一系列重要任务上全面超越或媲美O1。其做法与R1基本类似：同样进行监督微调（SFT），同样开展强化学习（RL），尽管所用的RL算法有所不同；此外，它们对数据集构建过程的描述也更为详尽，而这一数据集后续实际上被用于Q1-3中，因而值得专门讨论。下面我来具体谈谈数据部分。如珀西此前所言，数据或许是你整个训练流程中最重要的环节。因此，在大型模型训练论文中，一旦作者详细阐述其数据筛选与构建策略，我们就应格外关注。接下来介绍Kimi K1。

## 段落 54

**英文**: 5 does several things to try to curate their data set. So what they first do is they try to balance across different domains. Like they have automated, I'm guessing, LM-based tagging system to categorize basically math questions by different domains and disciplines. And then they kind of balance across these to try to get diversity across different domains. They exclude, even though these are verifiable, multiple choice and true false questions because they argue that these are too easy to hack or to randomly guess. So they're only looking for verifiable answers that are kind of short and can be evaluated by things like reg X or LL. And this is maybe the most interesting piece of the curation here. What they do is they take the model that doesn't do any reasoning, you know, their SFT model. And they have this model generate 10 answers. And the pass rate is used in order to determine whether to include that example or not.

**中文**: 5 采取了多项措施来筛选和优化其数据集。首先，他们力求在不同领域之间实现平衡。例如，他们可能采用了基于大语言模型（LM）的自动化标注系统，将数学题目按不同领域和学科进行分类，然后据此调整各类题目的比例，以确保数据集涵盖多个领域的多样性。尽管某些题目（如选择题和判断题）具有可验证性，但他们仍将其排除在外，理由是这类题目过于简单，容易被“破解”或通过随机猜测作答。因此，他们仅保留答案简短、且可通过正则表达式（regex）或大语言模型（LLM）等方法进行自动评估的可验证题目。这或许是整个数据筛选过程中最有趣的一环：他们使用一个不具备推理能力的基础监督微调（SFT）模型，让该模型为每道题目生成10个答案，并以该模型的通过率（pass rate）作为是否纳入该题目的判定依据。

## 段落 55

**英文**: And I think the exact thing they use for their latest selection strategies, they only select examples that fail best of eight. So if they can get any one out of a correct, then it's excluded for being too easy. SFT data similar to R1, very little description, who knows where they got that from. They just say they do some prompt engineering, so clearly it was distilled from something else, but we don't really know what they distilled that off of. So I'll talk about the RL algorithm. The Kemi one's kind of interesting. It's a different variation. It's actually maybe closer to DPO in a way. But you end up with an algorithm that I think you'll very much recognize. So you can kind of think of this in a very interesting way as convergent evolution of RL algorithms.

**中文**: 我认为，他们在最新选择策略中采用的正是这种精确方法：仅选取在八次尝试中最易失败的样本。换言之，只要某样本在八次尝试中至少有一次答对，就会因其过于简单而被排除。监督微调（SFT）数据与R1类似，描述极少，其来源不明。文中仅提到他们进行了某些提示工程，因此显然该数据是经由其他数据蒸馏而来，但我们并不清楚其原始数据来源。接下来我将介绍强化学习（RL）算法。Kemi算法颇具趣味性，它是一种不同的变体，某种程度上甚至更接近DPO。但最终所得算法，我想各位会非常熟悉。因此，不妨以一种饶有趣味的方式将其视作强化学习算法的趋同进化。

## 段落 56

**英文**: So you start once again at the very top. This is our classic goal. We're sampling from a data set. We're sampling from our policy. We want to maximize rewards. We don't want to be too far from our base policy. So this is the KL regularizer. If you remember our DPO derivation, you make a non-parametric assumption. You say pi star is the optimal policy is an arbitrary function. That means that the reward can be written as the log normalizer plus the ratio of policies.

**中文**: 因此，你再次从最顶层开始。这是我们经典的优化目标：从数据集中采样，同时从我们的策略中采样；目标是最大化奖励，但又不希望偏离基础策略过远，因此引入了KL正则项。如果你还记得DPO（直接偏好优化）的推导过程，其中我们做了非参数化假设，即最优策略π*是一个任意函数。这意味着奖励函数可表示为对数归一化项加上策略比值的对数。

## 段落 57

**英文**: This is the exact same thing we did in DPO. And now in DPO what we did was we took these rewards and we plugged them in to the kind of the Bradley Terry preference function. We don't have this here. We're not doing pairwise preferences. So we don't actually take that step. Instead we write down this equation and we say we know that for the optimal policy, we're going to have equality here. And so all we're going to do is we're going to make this a difference and we're going to add a squared loss on top. We're going to try to drive the left and the right side is close by just adding a squared loss. It's a reasonable thing to do. People have done things like this before.

**中文**: 这与我们在DPO中所做的一模一样。而在DPO中，我们当时是将这些奖励值代入布拉德利—特里（Bradley-Terry）偏好函数中。但此处我们并未采用该方法，因为我们并不进行成对偏好比较，因此实际上并未执行这一步骤。取而代之的是，我们直接写出这一方程，并指出：对于最优策略，此处应满足等式关系。因此，我们仅将等式两边作差，并在其上添加一个平方损失项，即通过最小化该平方损失，促使等式左右两侧尽可能接近。这是一种合理的方法，此前已有人采用过类似做法。

## 段落 58

**英文**: And this gives us our loss. It's basically trying to drive the right side and the left side of something that should be an equality for the optimal policy close together. And well, this is a little bit of an exotic looking object or maybe exotic looking initially. But if you take the gradient, it just looks a lot like GRPO. You know, you've got your gradients of your policy, right? This is your policy gradient stuff. And you've got a baseline reward. And actually what's the baseline reward? I'm just going to average the R within my batch, right? So here, this is actually doing something different. This is the normalizing constant. I think over the batch. But you know, we're doing essentially similar kinds of baselining as GRPO.

**中文**: 而这便构成了我们的损失函数。其本质在于，促使本应在最优策略下相等的等式两侧（即右侧与左侧）尽可能接近。这一形式初看略显奇特，甚至有些“另类”。但若对其求梯度，则其形式与GRPO非常相似：你拥有策略的梯度，这正是策略梯度方法的核心；同时你也拥有一个基线奖励。那么，这个基线奖励具体是什么？我只需对当前批次内的所有奖励R取平均值即可。此处所采用的实际上是一种不同的处理方式——即作为批次内归一化常数（我认为是针对整个批次进行计算）。不过，总体而言，我们所采用的基线设定方式与GRPO本质上是相似的。

## 段落 59

**英文**: And we've got a slightly different like square of the the law gloss regularization to keep my policy close rather than doing clipping, right? So zooming out what's happening here, very similar to GRPO in that this first part is a baseline loss, but there's no standard deviation thing happening. The second part is a now gets to the clipping that happens in GRPO. But instead of doing clipping, we're explicitly regularizing the policy, right? So we've got the same ingredients just in slightly different form. And so hopefully you can see, you know, that as long as you kind of have this policy gradient thing and the right baselining and something that looks like regularization, you can get a working RL algorithm. The other thing that the kidney folks do, and in some ways, I think this is more forward looking or they got this more right than the R1 folks, which is that they realize that, you know, if you're shipping a reasoning model, but you really care about is inference cost. And if you care about inference cost, you had better try to control the length of your COTs, right? If you have really long thinking chains, that's going to cost you a ton of money or your cost your users a ton of money. And so instead of celebrating the really long COTs, the kidney folks say, we want to really compress the COTs as much as possible while keeping performance high. And so they have this length reward thing here, where what they're doing is for kind of each batch, they're looking at the maximum and the minimum length, and what you have is lambda, where lambda is roughly like, you know, where are you in the range of length within your batch, right? If you're at plus point five, you know, you're really short and negative point five, you are really long, right? And the reward is basically going to be lambda whenever you get the answer to be correct. If your answer is correct, you're going to incentivize yourself to be really short at the very shortest end of this range. Whereas if your answers are incorrect, you're in this bottom part of this length reward, which means that you're incentivizing the COT lengths to be roughly shorter than the center of the range of the rollouts.

**中文**: 此外，我们采用了一种略有不同的、基于平方律的光滑正则化方法（law gloss regularization），以使策略保持接近而非采用裁剪（clipping）方式，对吧？  
从宏观视角来看，此处发生的过程与GRPO非常相似：第一部分同样是基线损失（baseline loss），但并未引入标准差相关机制；第二部分则对应GRPO中的裁剪操作，但我们并未直接裁剪，而是显式地对策略施加正则化约束，对吧？因此，我们所用的组件本质上相同，仅形式略有差异。希望您能理解：只要具备策略梯度机制、恰当的基线设定以及某种形式的正则化，即可构建出一个可行的强化学习算法。  

另一项由“Kidney”团队提出的方法（在某种程度上，我认为该思路更具前瞻性，甚至比R1团队更准确）是：他们意识到，若部署推理模型，真正关键的是推理成本；而若关注推理成本，则必须设法控制思维链（COT）的长度——因为过长的思维链将大幅增加自身或用户的经济负担。因此，“Kidney”团队并未推崇冗长的思维链，而是主张在维持高性能的前提下，尽可能压缩思维链长度。为此，他们设计了这一“长度奖励”机制：在每个批次中，他们统计思维链长度的最大值与最小值，并引入参数λ——λ大致表示当前样本在该批次长度范围内的相对位置（例如，λ=+0.5表示极短，λ=−0.5表示极长）。当答案正确时，奖励即为λ本身：这会激励模型趋向于该批次长度范围的最短端；而当答案错误时，模型落入该长度奖励机制的底部区间，从而被激励生成略短于采样长度范围中位数的思维链。

## 段落 60

**英文**: So some of funky loss to me, to be honest, but you can kind of understand the dynamics of this loss, which is you're incentivizing sort of correct answers to be as short as possible, and incorrect answers are incentivized to be kind of average ish, right? And so you don't have as strong of an optimization pressure to push down incorrect answers to be short. And one final note on this length stuff is the Kimi folks realize that if you add this reward early on in training, it stalls RL because, you know, it basically forces the model to say, I'm in a local minimum, I don't get any of my answers correct. The best I can do is to have my COT is really short, and then you just can't get out of that sort of local minimum. And so they actually only turn this on later on during training, so they kind of initially do a bit of unconstrained RL, and then they add this like throw it in afterwards. And they also have additional cool details, who knows how much of this stuff is necessary or important, but they actually have a whole curriculum setup. They basically have assigned difficulty labels, so the data set just kind of like top down like they just manually or via LMS kind of annotate the difficulty labels. And then they go from easy to hard in that order, and then they also as they're going on sample problems, proportion of the one minus success rate. So if you're 100% succeeding, you just never sample that question ever again. And for the rewards, they basically for code, they take problems with ground truth solutions, and they generate a bunch of test cases. And for math, they basically use actually a reward model that's used to compare like ground truth, human written answers to the LM output.

**中文**: 说实话，这种奇特的损失函数对我来说有些难以理解，但你大致能明白其作用机制：它会激励模型将正确答案尽量缩短，而对错误答案则倾向于使其长度趋近于平均水平，对吧？因此，该损失函数对压缩错误答案长度的优化压力并不强。关于这个长度相关机制，最后再补充一点：Kimi 团队发现，若在训练初期就引入该奖励机制，会导致强化学习（RL）训练停滞——因为这实际上迫使模型陷入局部极小值：模型意识到自己几乎无法给出正确答案，此时唯一能做的就是尽可能缩短思维链（CoT）长度，从而再也无法摆脱这一局部极小值。因此，他们实际仅在训练后期才启用该机制：先进行一段无约束的强化学习，之后再加入该奖励机制。此外，他们还设计了一套完整的课程学习体系（curriculum setup），其中为数据集中的每条样本手动或借助大语言模型（LLM）标注了难度等级，并严格按由易到难的顺序进行训练；同时，在采样问题时，还会根据成功率动态调整采样概率——具体而言，采样概率正比于“1 减去当前成功率”。例如，若某道题的成功率已达 100%，则后续将不再采样该题目。至于奖励设计：对于编程任务，他们选取具有标准答案的问题，自动生成大量测试用例；而对于数学任务，则采用一个专门训练的奖励模型，用于比较模型输出与人类撰写的参考答案之间的优劣。

## 段落 61

**英文**: So instead of using something like a reg X or using SimPi, which is what other people have done, the Kimi folks actually use a model to do equivalence checking. It's kind of surprising to do this in a verifiable reward case, but they don't seem to have a problem. Their reward models are very, very accurate because it's really all it's doing is advanced string matching. One of the things that's really cool about the Kimi paper is that they also talk about kind of the infra issues that arise doing RL. I think I don't think I've seen any of the other RL reasoning papers actually talk about systems almost at all. So it's nice to see that they're kind of talking about this, what their structure is, what their layout of this is. You'll have to deal with this in a five, like I think a very mini version of this as you implement things like rollouts in RL. But one thing I'll note is, why is RL so hard to make efficient? In many ways it's harder than normal pre training to have your GPUs fully utilized during RL. And the reason I think is because there's rollouts involved. So you have to be generating sequences.

**中文**: 因此，Kimi 团队并未采用类似正则表达式（reg X）或 SimPi 等方法（其他研究者曾使用过），而是直接利用一个模型来执行等价性验证。在可验证奖励（verifiable reward）场景下采取这种方式颇为出人意料，但他们似乎并未因此遇到问题。其奖励模型极为精准，原因在于该模型本质上仅执行高级字符串匹配。Kimi 论文另一大亮点在于，它还专门探讨了开展强化学习（RL）过程中所面临的基础设施（infra）挑战。据我所知，此前其他 RL 推理类论文几乎从未涉及系统层面的讨论，因此看到 Kimi 团队对此展开分析——包括其系统架构设计与整体布局——实属难得。你在实际实现 RL 中的 rollout 等环节时，哪怕只是极小规模地复现这一流程，也必然要直面这些挑战。此外，值得指出的一点是：为何 RL 如此难以实现高效运行？在诸多方面，让 GPU 在 RL 过程中保持满负荷运转，其难度甚至高于常规预训练阶段。我认为，根本原因在于 RL 涉及 rollout 过程，即必须持续生成序列。

## 段落 62

**英文**: And whenever you're generating sequences, not only are you kind of slow in the sense of inference is slow, but also you have this other issue, which is that you have to switch from RL to inference and back. And you have to be passing sort of data back to the RL worker and the RL worker has to pass model weights to the inference server and vice versa. So you've got all this sort of message passing that can happen. And finally, this is unique to long COT models. But if you have really long COTs, batches can become very, very uneven. So you have to handle that cleverly somehow. And so the Kimi folks do this relatively nice, but fairly standard thing of having different workers that are assigned to do the RL updates, having different workers assigned to do inference. And they have basically kind of message passing to be able to pass the weights to the inference worker and the inference worker can basically make data sets for the RL worker. And they have almost the same kinds of setups that you will have. They use VLM for inference.

**中文**: 而且，每当生成序列时，不仅推理速度本身较慢，还会出现另一个问题：你必须在强化学习（RL）与推理之间反复切换。你需要将数据传回RL工作节点，RL工作节点则需将模型权重传给推理服务器，反之亦然。因此，整个过程中会产生大量消息传递操作。最后，这是长思维链（COT）模型所特有的问题：若思维链非常长，批次之间的长度会变得极不均衡，因而必须以某种巧妙的方式加以处理。Kimi团队采用了一种相对简洁但较为常规的做法：分别设置专门负责RL更新的工作节点和专门负责推理的工作节点；通过基本的消息传递机制，将模型权重发送给推理工作节点，而推理工作节点则可为RL工作节点生成训练数据集。其整体架构与你所采用的架构几乎完全相同，且推理阶段同样使用视觉语言模型（VLM）。

## 段落 63

**英文**: And they also do very advanced stuff where they have to have VLM with dummy weights and they have to kill it because of the complexities of doing kind of this passing of weights from one worker to another. Finally, I think one thing that I think is interesting that the Kimi people show in their results is they have like per iteration results where they show like as RL proceeds, like how does performance scale. And they show really nice scaling, which is in the blue. And they also show the growth of the length of the responses. And much like in Dr. G. R. P. O. because I don't think their RL algorithm is inherently sort of balanced or sorry, biased towards longer responses.

**中文**: 此外，他们还进行一些非常前沿的研究工作，例如需使用带有虚拟权重的视觉语言模型（VLM），并因在不同工作节点间传递权重所引发的复杂性而不得不终止该模型。最后，我认为 Kimi 团队在其结果中展示的一个有趣现象是：他们提供了每轮迭代的详细结果，即随着强化学习（RL）过程的推进，性能如何变化；他们展示了非常优异的性能扩展趋势（图中以蓝色表示）。同时，他们还展示了模型生成响应长度的增长情况。这一点与 Dr. G. R. P. O. 的研究类似，因为我认为他们的 RL 算法本身并非天然地倾向于生成更长的响应（或者说，并非固有地偏向于生成更长的响应）。

## 段落 64

**英文**: Sort of response is actually sort of plateau out at a sort of target length like we saw with Dr. G. R. P. O. rather than grow unboundedly like we did with the vanilla G. R. P. O. Cool.

**中文**: 此类响应实际上会在某个目标长度处趋于平稳（如我们之前在G.R.P.O.博士的案例中所见），而不会像基础版G.R.P.O.那样无限制地增长。很酷。

## 段落 65

**英文**: Any questions on Kimi before we finally conclude with Qn3?. So Dr. like the RL send up in detail. Yeah, I was wondering, so I guess like when you read the RL on steering like training, the RL on the inference is down by VLM, right? And as an author in the program, I would be that's like, you know, as an author in the blog, they also do sync that to the VLM inference like worker. And that's what this is for right. Yeah, so I think, you know, the most annoying part, at least with current libraries of this process is essentially that step of taking RL weights and putting it into VL. There is an experimental API that is supposed to allow you to use NCCL collective calls to shove a set of weights into VLM. But at least we were thinking about having you use that for the assignment actually. But it has too many undocumented parameters for it to be a little bit mature. And so maybe next year's iteration, this will actually be, you know, the fairly mature technology.

**中文**: 在我们最终以问题3（Qn3）结束之前，大家对Kimi还有什么问题吗？  
所以，博士，能否详细说明一下强化学习（RL）的部署流程？  
是的，我一直在想：当我们阅读关于“转向”（steering）训练的RL相关资料时，推理阶段的RL其实是通过视觉语言模型（VLM）来实现的，对吧？  
而作为该项目的作者，我理解的是——就像博客文章中提到的那样，他们也会将RL同步到VLM的推理工作节点上。这正是本环节的设计目的，对吗？  
是的。我认为，至少就当前相关库而言，整个流程中最令人头疼的环节，本质上就是将RL训练所得的权重加载进VLM这一步。  
目前存在一个实验性API，本意是允许用户借助NCCL集合通信调用，将一组权重直接注入VLM。  
我们原本甚至考虑过让各位在本次作业中使用该API；但因其存在大量未文档化的参数，目前尚不够成熟。  
因此，或许到明年迭代时，这项技术才能真正发展得较为成熟。

## 段落 66

**英文**: But right now, I think a lot of people do things like start at VLM with dummy weights, the weights then get loaded somehow into memory with sort of hacks on top of VLM. And each iteration, they often tear down VLM to make sure they can free the GPU memory fully. Yeah, there's a lot of like, I think, you know, the RL for LMS, I think is fairly new still. So I think the infrastructural support remains a little bit immature. But I think it may be a year, things will actually just get much nicer. Yeah, question back there. You have a lot of like accuracy rewards and like, A-words for other words, how are you supposed to combine them together? Yeah. So you, how do you combine the different rewards?. I think this is one of the RL, not quite black magic, but really, you know, like RL, magic things of the, you just tune weights. Like in all cases, all the rewards are just added together, but with weights.

**中文**: 但目前，我认为许多人会采用类似这样的做法：先用虚拟权重启动视觉语言模型（VLM），再通过一些临时性的变通方法将实际权重加载到内存中；而且在每次迭代时，往往需要彻底卸载VLM，以确保GPU内存被完全释放。是的，这方面存在大量问题——我认为，比如针对大语言模型（LLM）的强化学习（RL），目前仍处于较新的发展阶段，因此相关基础设施支持尚不够成熟。但我预计，大约一年后，情况将显著改善。  
好的，后面那位朋友有问题？  
您提到了很多诸如准确性奖励以及针对其他词汇的A类词奖励等，那么该如何将它们综合起来呢？  
是的，您是如何整合这些不同类型的奖励的？  
我认为这是强化学习领域中一个并非完全“黑箱”、却颇具“魔法”色彩的问题——实际上，您只需调整各奖励项的权重即可。在所有情况下，各类奖励都是简单加总，但需各自乘以相应的权重。

## 段落 67

**英文**: And how are the weights determined, empirically in order to maximize the downstream performance? Especially for things like format rewards, you can almost think of them as like, shaping or like surrogate rewards. Like you don't necessarily really care about the formatting rewards. It's more of a means to an end to get a good long COT within the tags that will get you the answer. Cool. Okay, so the final one I want to talk about is quen3. And thankfully, quen3 released their report before the end of the class. I get to include it. And, you know, I think this is the most recent and modern of the RL for reasoning and kind of models to have come out. And so we can kind of see how they've like built upon the previous works, like where they've changed things. And they have actually pretty interesting scaling and data results that are new and unique.

**中文**: 那么，这些权重又是如何通过经验方法确定，以最大化下游任务性能的呢？尤其是格式奖励这类指标，你几乎可以将其视作一种塑造性奖励或代理奖励：你实际上并不真正关心格式奖励本身，它只是一种实现最终目标的手段——即在标签内生成高质量的长链式思维（CoT）推理过程，从而得出正确答案。很好。好的，最后我想讨论的是Quen3。幸运的是，Quen3在课程结束前就发布了其技术报告，因此我得以将其纳入本次讲解。我认为，这是目前最新、最前沿的面向推理任务的强化学习模型之一。由此，我们也能大致看出它是如何在先前工作基础上进行演进与改进的，例如在哪些方面做出了调整。此外，他们在模型扩展性与数据规模方面的实验结果也颇具新意且十分有趣。

## 段落 68

**英文**: So the overall picture is very similar to what R1 and Kimi have done. So quen basically take their base models. They'll do a long COT SFT state. So that's this first stage over here. They'll do their reasoning RL. They'll do something funky that I'll talk about later called thinking mode fusion. And then they'll do RL, HF, RL. And then that's the model that they ship. Of course, they then distill that in various ways, but we can kind of forget about that for the moment. Right? So we've already seen this in R1, right?.

**中文**: 因此，整体流程与R1和Kimi的做法非常相似。Quen基本上采用其基础模型，首先进行长链思维（Chain-of-Thought, COT）监督微调（SFT），即此处的第一阶段；接着进行推理强化学习（Reasoning RL）；再执行一种我稍后会介绍的、名为“思维模式融合”（Thinking Mode Fusion）的特殊操作；随后进行强化学习（RL）、人类反馈强化学习（HF RL）以及再次强化学习（RL）。最终发布的模型即为此版本。当然，他们后续还会以多种方式对模型进行蒸馏，但目前我们可暂且忽略这一环节，对吧？这一点我们在R1中其实已经见过了，对吧？

## 段落 69

**英文**: RL, HF, comes after reasoning. And then distillation comes, yeah, after that. And we already know a lot of the playbooks. I can actually go through this fairly fast. Much like Kimi, they basically curate the data by filtering for difficulty using best events. So if your base model that hasn't been RL, can already answer it. If you sample like n times, then you can just get rid of it, right? They also do some like decontamination things where they remove things that are too similar to validation data. And then they also manually filter essentially their SFT set. So for their initial SFT data, for long COTs, they manually filter it for whether they are guessing or whether they actually got it right. The one thing that empirically I think is really interesting about the Qen3 RL results is that they are actually doing this RL only on 3,995 examples.

**中文**: 强化学习（RL）和人类反馈（HF）在推理之后进行，随后才是知识蒸馏，是的，就在那之后。我们已经掌握了大量相关操作流程，因此我可以较快地过一遍。与Kimi类似，他们主要通过筛选“最佳事件”来按难度过滤数据。例如，若你的基础模型（尚未经过RL训练）已能回答某个问题，那么当你采样n次后，便可直接将其剔除，对吧？此外，他们还进行了一些“去污染”处理，即移除与验证数据过于相似的内容。同时，他们还会人工筛选监督微调（SFT）数据集：对于初始SFT数据中的长思维链（COT）样本，他们会人工判断其中的答案是靠猜测得出，还是真正正确解答。实证上，Qwen3的RL结果中一个非常有趣的现象是：其RL训练仅基于3995个样本。

## 段落 70

**英文**: Which is a very small number of examples to be doing this on. And they get pretty good gains out of that RL process. And so you can view this as RL on verified rewards as being very efficient. You could also think of this as being analogous to many sample efficiency results in the past. Like people have shown that you can instruction tunumodel with very few samples or that you can distill long COTs with very few samples. But that doesn't necessarily mean that it doesn't continue to scale. We don't really know why. What this does show is that even with very few examples, you can sometimes do RL, which is surprising and cool. So what is the two new sort of Qen-specific stuff that they do? The thing that they do is this thing called thinking mode fusion, which I think is kind of interesting. And where I think the field or the various trends are going is in controlling inference.

**中文**: 这用于训练的样本数量非常少。而他们通过该强化学习（RL）过程获得了相当可观的性能提升。因此，可以将这种基于已验证奖励的强化学习视为效率极高的方法。你也可以将其类比为过去许多关于样本高效性的研究成果，例如，已有研究表明，仅需极少样本即可对模型进行指令微调，或仅用极少样本即可蒸馏出长链思维（COT）推理过程。但这并不意味着其性能提升不会随样本量增加而持续扩展——我们目前尚不清楚其背后原因。然而，这一结果确实表明：即使仅有极少样本，有时也能成功开展强化学习，这一点令人惊讶且颇具启发性。那么，他们所提出的两项新式、专属于Qwen模型的技术是什么？其中一项技术被称为“思维模式融合”（thinking mode fusion），我认为颇具趣味性；而我认为当前研究领域或各类发展趋势正朝着“控制推理过程”的方向演进。

## 段落 71

**英文**: So what they do is they want to have both a thinking model and a non-thinking model in the same single set of parameters. So what do they do?. Well, after they train a model with RL, they have a model that can do thinking. And now they're going to fine tune it again to do one of two things. They're going to fine tune the model with some data that has a think tag. And then it's going to do the normal COT thing. And you can get this data from yourself, right? Like the original thinking model can generate this. Or you can have a no think tag, in which case it should immediately kind of emit an answer. And in this case, they're going to have to sort of supervise fine tune the model to know what no think means and to immediately try to emit the answer. And one kind of interesting side effect of this is that they found that if they do this training where they train the model to have a think and a no think tag, then what they can do is they can sort of, if the model continues to think and you want to terminate the thinking process, you can kind of actually like terminate the thinking process at a special string that's like considering the limited time of the user, I have to give a solution by thinking directly now and then end think tag and then it sort of accurately gives the answer.

**中文**: 因此，他们的目标是在同一组参数中同时实现一种“思考型”模型和一种“非思考型”模型。那么他们具体如何操作呢？首先，在通过强化学习（RL）训练出一个具备思考能力的模型后，他们将对该模型再次进行微调，使其具备以下两种行为之一：一是使用带有“思考”标签的数据进行微调，从而使模型执行常规的思维链（CoT）推理；这类数据可由原始思考模型自行生成。二是使用带有“不思考”标签的数据进行微调，使模型直接、立即输出答案；此时需通过监督式微调，让模型明确理解“不思考”的含义，并立即生成答案。这一方法带来了一个颇为有趣的现象：当模型持续思考而用户希望终止该思考过程时，研究者发现，可通过插入一段特殊字符串（例如：“考虑到用户时间有限，我必须立即直接思考并给出解答”），随后紧跟“结束思考”标签，从而有效终止思考过程，模型随即准确输出答案。

## 段落 72

**英文**: So this gives them a control knob by which they can sort of more precisely control a maximum number of thinking tokens. And this gives them pretty clean test time scaling from a single model. And so they can set like a maximum thinking budget. And of course the sort of very, very maximum out to infinity is just the original thinking model. But they can kind of early terminate to the left and they can get graceful degradations in model performance. That's not too bad at the very sort of beginning. You can like half the thinking tokens and still get some pretty good performance. And so you can look at, Q1,3 also does a nice ablation where they give you the performance at the reasoning RL stage, at the thinking mode fusion stage, and at the general RL stage. And one thing that's very interesting to me here is that if you look at the first two sets of rows, the general task and the instruction following ones, reasoning RL helps, thinking mode fusion helps, and of course RLH have continues to help here as well. Kind of everything helps in this regime.

**中文**: 因此，这为他们提供了一个控制旋钮，使其能够更精确地控制最大思考词元数量。由此，他们仅凭单一模型即可实现相当干净的测试时扩展性。他们可以设定一个最大思考预算。当然，理论上极限情况（即思考词元数量趋于无穷）就等同于原始思考模型。但他们也可以在左侧提前终止推理过程，从而实现模型性能的平滑退化——在初始阶段，性能下降并不严重：例如，将思考词元数量减半，仍能获得相当不错的性能。此外，Q1,3 还进行了出色的消融实验，分别展示了推理强化学习（Reasoning RL）阶段、思考模式融合（Thinking Mode Fusion）阶段以及通用强化学习（General RL）阶段的性能表现。此处一个非常有趣的现象是：观察前两组行（即通用任务和指令遵循任务），可发现推理强化学习有帮助，思考模式融合有帮助，而RLHF在此处也持续发挥作用——在此类场景下，所有这些方法均有效。

## 段落 73

**英文**: But if you look at kind of math or STEM performance, you know, in the thinking case, general RL hurts performance. And in the non-thinking case, it helps performance. So there really is, seems to be at least some of a trade-off in, you know, do I optimize for general purpose instruction following up here, or do I optimize for like math and coding kind of down here?. Right. And so those are kind of interesting sort of properties that are emerging. And it'll be cool to see sort of more future models kind of sidestep these trade-offs somehow. Okay. Cool. So to put that all together, our sort of initial motivation wants to say RL is very powerful. We kind of figured out that we can do RL with language models in the RLHF domain.

**中文**: 但如果你观察数学或STEM领域的表现——以“需要思考”的任务为例，通用强化学习（RL）反而会损害性能；而在“无需思考”的任务中，它却能提升性能。因此，这里确实存在某种权衡：我究竟是要在此处优化通用指令遵循能力，还是要在下方专门优化数学和编程等特定能力？没错。这些正是一些颇为有趣的新兴特性。未来若能看到更多模型以某种方式规避此类权衡，将十分令人期待。好的，很好。综上所述，我们最初的动机在于：强化学习非常强大，而我们在RLHF（基于人类反馈的强化学习）领域已基本掌握了如何将强化学习应用于语言模型。

## 段落 74

**英文**: But you can't just like kill climb on noisy, pairwise preferences forever, right? So one solution is to pick domains in which you can't do reward hacking and then just, you know, go for it, right? RL and narrow domains is one good solution. And GRPO is one very simple algorithm. And hopefully you all kind of have a sense of like, okay, I can just do policy gradients with some good baselines. And that will enable RL on all these kinds of verifiable reward domains. And then finally, there's lots of successful recipes in the wild and hopefully now you've seen, you know, what's in common, what's different, what implementation tricks matter. Cool. Thanks a lot. And I will see you all next week.

**中文**: 但你总不能一直靠嘈杂的、成对的偏好比较来训练吧？所以一种解决方案是选择那些无法进行奖励黑客行为的领域，然后放手去做，对吧？在狭窄领域中应用强化学习就是一种不错的方案。而GRPO则是一种非常简单的算法。希望各位已经大致有了这样的认识：没错，我只需结合一些优秀的基线方法来使用策略梯度，就能在所有这类可验证奖励的领域中开展强化学习。最后，现实中已有许多成功的实践方案，希望现在大家已能看出它们的共性与差异，以及哪些实现技巧至关重要。很好，非常感谢！下周见！

---

*共 74 个段落，738 句话*
